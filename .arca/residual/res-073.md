# Residual Record

```yaml
residual-id: "res-073"
goal-requirement-ref: "RBS-003"
frozen-goal-bundle-revision: "git:3eb28a2+goal-sha256:ad1007250cdf00097b5ebd79dded5eef4135f025fe64d589cc6a22a38eeb41c2"
implementation-revision: "git:079b4fc"
concrete-evidence-refs:
  - "`.arca/runbook-spec.md`, under `## Ownership`, still names the superseded flat `.arca/state.toml` and `.arca/evidence.toml` paths even though `FDC-004` scopes the State File and Run evidence under `.arca/runs/<id>/`"
  - "`test/qa/tests/t055_runbook_spec.rs::ownership_rules_name_their_enforcer` (PT-054-04, RBSV-004) resolves named enforcers, but accepts any row containing `prose-only`; it cannot catch the stale row that says the agent-writable guard rule is prose-only until the doctor reports it even though `src/doctor.rs::lint_guards` now emits `RB302`"
  - "`src/scaffold.rs::write_scaffold` is now an Engine writer for a caller-selected runbook path, while the ownership table still says no runbook writer exists in `src/`"
classification-rationale: "partial: the ownership section has the required shape, but its flat Scheduler-owned paths and two prose-only descriptions no longer state the current ownership contract truthfully."
status: "partial"
required-test-refs:
  - ".arca/goal/test-list.md#RBSV-004"
```

> **Owning ticket**: the runbook-specification ticket (`t-054`) remains this residual's one active ticket for the ownership-table correction.
