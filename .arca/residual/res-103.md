# Residual Record

```yaml
residual-id: "res-103"
goal-requirement-ref: "FDC-011"
frozen-goal-bundle-revision: "git:be7e906+goal-sha256:93bc2362b04ce8bc15b96869fafd449c67f3f339b5956ece6d02c821d0e72119"
implementation-revision: "git:1c9fb2b (green landing 92ab301)"
concrete-evidence-refs:
  - "src/ledger.rs (the Scheduler-owned append/annotate-only per-run ledger; strict read refuses any malformed entry by name; `LedgerEntry.workspace` is an optional non-empty string that serializes only when a binding exists)"
  - "src/scheduler.rs (spawn appends one entry with the recorded fields, now including the canonical child workspace; respawn appends the successor naming the superseded id and carrying the same workspace binding forward; the join reads the ledger's live children as its expected set)"
  - "src/abandon.rs (confirmed abandonment flips only that entry's abandoned mark, and the entry stays addressable so a partially retired child still resolves its own class and workspace)"
  - "src/scheduler.rs::Scheduler::workspace_from_ledger_entry (child guards and motion resolve against the recorded workspace; a vanished, redirected, or non-directory path refuses by name and never falls back to the caller's directory)"
  - "test/qa/tests/t066_spawn_ledger.rs (PT-066-01..03 green at the landing)"
  - "test/qa/tests/t074_child_workspace.rs (ENSV-007 and ENSV-013 green at the landing: the recorded workspace field and Run-scoped sibling receipts that survive an ordinary Git merge)"
  - ".arca-private/t-066/ (HT-066-01..06 green at the landing)"
  - ".arca-private/t-074/ (HT-074-01..06 green at the landing, covering the workspace field through spawn, respawn, abandonment, and overlapping sibling children)"
  - ".arca/residual/manifest/2026-08-06-res-111-103.txt (reviewable snapshot: every path, its tracking state, and its SHA-256 under src/, test/, and .arca/, all tracked or staged; the manifest's own path is the single declared exception - untracked at the moment it was written and committed by this same landing)."
classification-rationale: "satisfied: the Scheduler-owned append/annotate ledger now carries every field the contract names, including the child workspace path. `rtm spawn --workspace <path>` records the canonical directory, an omitted flag inherits the parent's, respawn carries the binding to the successor, and the recorded path - not the invoking directory - decides where a child's guards and motion run. The remaining ledger behavior is unchanged from the t-066 landing."
status: "satisfied"
mutation-kill:
  - "disabling the duplicate-id refusal in ledger read fails mangled_ledger_refuses_strictly_at_read (HT-066-02)"
  - "flipping the annotate mark to abandoned = false fails abandon_flips_only_the_mark_and_respawn_appends_the_successor (PT-066-02)"
  - "counting abandoned entries as live at the join fails join_counts_only_live_passed_children (HT-066-03)"
  - "writing `workspace: None` at spawn, or dropping the workspace clone from the respawn successor entry, fails HT-074-01 and HT-074-04: the durable entry loses the binding the contract requires it to carry"
  - "filtering abandoned entries out of the child ledger lookup fails HT-074-04: a partially retired child silently inherits the caller's directory"
  - "each t-074 mutation was restored byte-for-byte and proved by SHA-256 before the lane was run green again"
required-test-refs:
  - "MCV-007"
  - "ENSV-007"
```
