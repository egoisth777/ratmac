# Residual Record

```yaml
residual-id: "res-118"
goal-requirement-ref: "SVC-003"
frozen-goal-bundle-revision: "git:4f78de5+goal-sha256:a98320c3730c32a38f88afb889948ec150fb7778a244355dc081d9d7c9a30f4f"
implementation-revision: "git:4f78de5 (Engine source unchanged since ae49ff7; machine.rs sha256:b5d9493cf00ff8a2eda4ff6ae7ce9a8cedf578d86c26fa4c8486457189b0f0d4)"
concrete-evidence-refs:
  - "src/machine.rs:13, 35, 41: the parsed Machine Class holds `phases: BTreeMap<String, PhaseDefinition>` at the top level and the same map inside `ChildClass`, so the accepted runbook keys are still `phases` and `classes.<name>.phases`"
  - "src/machine.rs:22-27 `PhaseDefinition`: a declaration carries its prompt, inputs, guards, and spawns under the pre-cutover type name"
  - "src/contract.rs:532 and src/doctor.rs:298-495: every consumer reaches the declarations through `.phases()`"
  - "src/graph.rs:117-135, 245-255: transition `from` and `to` resolve to `Phase` values"
  - "cargo test -p ratmac-qa --test t055_runbook_spec at 4f78de5: `specification_is_routed_and_states_the_shape` FAILED with `RBS-001: the specification must carry the section \"## Phases\"` - the check pins the pre-cutover heading, and the settled format now says States, so the documented format and the Engine disagree in the Engine's favour"
  - "no runbook written with `states` has ever parsed: `SVCV-002` and `SVCV-003` have no implementation under test/qa/tests/"
classification-rationale: "missing: the format is settled on paper (.arca/runbook-spec.md `## States`, `[states.<name>]`, `[[states.<name>.spawns]]`, `[classes.<name>.states]`) but the parser accepts only the pre-cutover keys, every consumer reads `.phases()`, and the agreement check that guards the format is currently red against the new wording. Nothing in the Engine has moved."
status: "missing"
required-test-refs:
  - "SVCV-002"
  - "SVCV-003"
```
