# Residual Record

```yaml
residual-id: "res-118"
goal-requirement-ref: "SVC-003"
frozen-goal-bundle-revision: "git:4f78de5+goal-sha256:a98320c3730c32a38f88afb889948ec150fb7778a244355dc081d9d7c9a30f4f"
implementation-revision: "git:9b1a60b (the t-080 green landing on main); src/machine.rs sha256:c8fea251cadea66f7d3aaec5237e427fc2014569eaa6311c86a07c25ce76d513, src/graph.rs sha256:822c65a0b65ef32412ca2b8c586d20e659c64822d6fe3afb72f635e872f46930, src/doctor.rs sha256:368fed0e76e82156de08c7a5922e313d7cbebc0a10a11c0bba54137c0c86cf90, src/contract.rs sha256:7ad4932b46de3f4dbba72bcac1d6565d7fa7fc899dbba7381b4911b7a77f48ec, src/scheduler.rs sha256:78e12b23386e98cd9e713818b3d9ab660de88db7869534bc73144f39bca9c7f4"
concrete-evidence-refs:
  - "src/machine.rs:13, 21, 34-41: the parsed Machine Class holds `states: BTreeMap<String, StateDefinition>` at the top level and the same map inside `ChildClass`; the accepted runbook keys are `states` and `classes.<name>.states`, and the declaration type is `StateDefinition`"
  - "src/graph.rs:134: the graph exposes `states()` and transition `from`/`to` resolve to `State` values"
  - "src/contract.rs and src/doctor.rs:298-495: every consumer reaches the declarations through `.states()`; no `.phases()` accessor remains in src/"
  - "test/qa/tests/t080_states_table.rs::a_states_runbook_parses_passes_the_doctor_and_runs and ::the_child_class_and_its_spawn_are_read_from_the_states_tables: a runbook with `states`, `[[states.<name>.spawns]]`, and `[classes.<name>.states]` parses, `rtm doctor` reports no finding, and a Run walks to its terminal State - `SVCV-002` green"
  - "cargo test -p ratmac-qa --test t055_runbook_spec: `specification_is_routed_and_states_the_shape` is green - the documented format and the Engine now agree on `## States`, `[states.<name>]`, `[[states.<name>.spawns]]`, `[classes.<name>.states]`"
  - "all 22 runbook fixtures under test/qa/fixtures/ and the repository's own `.ratmac/ratmac.toml` are written with `states`, and the whole workspace suite runs on them"
  - "hidden lanes HT-080-01 through HT-080-06 (.arca-private/t-080/tests/hidden.rs) all green: every seeded format defect still reports its own code, an undeclared endpoint still refuses RB108, a class body keeps its one-level cap, a runbook swapped under the lock refuses before any State write, parsing writes nothing, and spawn/join/blocked-route resolve through the renamed map"
  - "the closing clause landed with `t-082` at `d11cc0d`: a runbook declaring `phases` is refused by the dedicated `RB111` at every entry point and at the parser door (src/scheduler.rs:680-724, src/machine.rs:1159-1183), naming the file and the rename and migrating nothing - `SVCV-003` is green in test/qa/tests/t082_precutover_residue.rs `a_precutover_runbook_refuses_by_its_own_code`"
classification-rationale: "satisfied: the settled format is the Engine's format. The parser, the model, the graph, and every consumer read `states`, `[[states.<name>.spawns]]`, and `[classes.<name>.states]`; a runbook written the settled way parses, doctors clean, and drives a Run start to terminal, and no other key, guard kind, validation rule, or diagnostic code of the format changed. The requirement's second listed check, `SVCV-003`, is green since `t-082`: the pre-cutover spelling is refused by its own dedicated code, ahead of the generic unknown-key error."
status: "satisfied"
mutation-kill:
  - "M1 - `src/machine.rs`: read the top-level declaration map under the pre-cutover key (`root.get(\"phases\")` in place of `root.get(\"states\")`). Reds `t080_states_table::a_states_runbook_parses_passes_the_doctor_and_runs` and `::the_child_class_and_its_spawn_are_read_from_the_states_tables` (RB103 `unknown key \"states\" in top-level`) and hidden `HT-080-01`, `HT-080-02`, `HT-080-03`, `HT-080-04`, `HT-080-06`; only `HT-080-05` survives, because a parse that refuses still writes nothing."
  - "M2 - `src/machine.rs` class-body parse: let a class body accept its own `classes` table, lifting the one-level composition cap. Reds `HT-080-03` (`a class body declaring its own classes must refuse; got []`)."
  - "M3 - `src/machine.rs` transition validation: stop resolving `transition.from()` against the declaration map. Reds `HT-080-02` (`an undeclared source must refuse with RB108; got [\"RB204\", \"RB205\", \"RB205\"]`)."
  - "M4 - `src/scheduler.rs::verify_open_runbook_snapshot`: drop the locked re-read comparison and return `Ok(())`. Reds `HT-080-04` (a runbook swapped under the lock routed `delegate -> intake` instead of refusing before the State write)."
  - "M5 - `src/doctor.rs::diagnose_with_roots`: create `<engine-root>/runs` before reporting. Reds `HT-080-05` (`diagnosing a accepted runbook must write nothing`)."
required-test-refs:
  - "SVCV-002"
  - "SVCV-003"
```

> **Owning ticket:** `t-080` owns this record and delivered the format itself (`SVCV-002`). Its one
> outstanding clause, the dedicated pre-cutover refusal (`SVCV-003`), landed with `t-082` at
> `d11cc0d`, and this record closed with it.
