# Residual Record

```yaml
residual-id: "res-129"
goal-requirement-ref: "PCR-002"
frozen-goal-bundle-revision: "git:4ac18a1+goal-sha256:74ccde5aefbf2a5b5c4a773e6438ddb20f1d6c123d095f5bb3dc51b2e73e8440"
implementation-revision: "git:800fe0d (t-093 green landing; .arca/index.md, engine source unchanged)"
concrete-evidence-refs:
  - "test/qa/tests/t093_stage_answer.rs `the_live_run_names_the_stage_and_the_lookup_is_a_fallback`: the cycle has one entry and one end, so one Run answers for a sprint; with the Run live the addressed report names its stage and follows it to the next one; with no Run the report names no stage at all"
  - ".arca/index.md:14-31: `rtm status --run <id>` is the answer, and the by-hand derivation is introduced by its `no-live-Run fallback` label and stated never to compete with a live Run"
  - ".arca-private/t-093/tests/hidden.rs `ht_093_03`: entry, mid-cycle, and the terminal rest State each name their own stage - the end is reported, not derived"
  - ".arca-private/t-093/tests/hidden.rs `ht_093_04`: a truncated record, a State the runbook no longer declares, and an empty record each refuse with the record's own diagnostic and never substitute the by-hand answer"
  - ".arca-private/t-093/tests/hidden.rs `ht_093_05`: reporting writes nothing, and exactly one live document carries the derivation - under its label, at its head"
classification-rationale: "satisfied: the engine half was already true once the cycle shipped, so the whole gap was the second answer. The by-hand derivation is now introduced by its fallback label in the one document that carries it, and a test fails if any live document offers it unlabelled or if a second one starts deriving. One limit is stated rather than papered over: this cycle declares no blocked route, so a sprint cannot be paused - `rtm hold` refuses at every stage. That is filed as a wish, not fixed here."
status: "satisfied"
mutation-kill:
  - "M1 the fallback loses its label (the heading renamed, the trailing sentence left) - killed by `ht_093_05_output_no_document_offers_the_lookup_unlabelled`"
  - "M2 the tree is offered as the answer again - killed by `ht_093_05_output_no_document_offers_the_lookup_unlabelled`"
  - "M3 the report stops naming the stage by its own word (`State` renamed `Stage`) - killed by `the_live_run_names_the_stage_and_the_lookup_is_a_fallback`"
  - "M4 the report's fields change order (a second `Status` line appended) - killed by `ht_093_01_regression_the_report_shape_is_unchanged`"
  - "M5 a second document also derives the stage - killed by `the_live_run_names_the_stage_and_the_lookup_is_a_fallback`"
required-test-refs:
  - "PCRV-002"
```
