# Residual Record

```yaml
residual-id: "res-129"
goal-requirement-ref: "PCR-002"
frozen-goal-bundle-revision: "git:4ac18a1+goal-sha256:74ccde5aefbf2a5b5c4a773e6438ddb20f1d6c123d095f5bb3dc51b2e73e8440"
implementation-revision: "git:97371d9 (Engine source unchanged since d0f3ed1; src/state.rs sha256:d54dd75469b50a0cbd5a6901)"
concrete-evidence-refs:
  - ".arca/index.md:14-19: the tree-derived lookup is the whole answer to \"where are we\" - open tickets mean building, ownerless gap records mean cutting work, a stale gap set means the gap check, a pending bundle means planning - and it is not labelled a fallback because today it is the only oracle"
  - "src/state.rs:253-284: the addressed report already prints State, Status, the three revisions, active references, the blocker, and the pending guards, so the reporting surface exists; it has nothing to report here because no Run of the cycle can be started"
  - "res-128: the Machine Class declares no cycle stage, so there is no State for the report to name"
classification-rationale: "missing: the requirement is that the addressed report answers the stage question while a sprint Run is live and the tree lookup is demoted to a labelled no-live-Run fallback. Neither half holds: no Run of the cycle can exist, and the lookup still presents itself as the answer rather than as the between-sprints fallback."
status: "missing"
required-test-refs:
  - "PCRV-002"
```
