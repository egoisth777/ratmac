# Residual Record

```yaml
residual-id: "res-154"
goal-requirement-ref: "THK-001"
frozen-goal-bundle-revision: "git:d31eae6+goal-sha256:69d76bc6001de4765ff25358cd3cc181ea84a8880edf49f673b148ec45424fd9"
implementation-revision: "git:4013cecffae4d5167204693d8b383655fc4e70d8 (the t-107 green landing)"
concrete-evidence-refs:
  - "src/cli.rs:83 enumerates the whole command surface - `start, status, step, hold, abandon, spawn, respawn, doctor, scaffold, skill` - and the dispatch at src/cli.rs:228 admits the same ten; no verb opens a work item's turn, creates an item-named branch or worktree, or copies a lanes root"
  - "tools/ holds exactly rtm.ps1 (bootstrap), check_links.py, and trial.ps1; trial.ps1 is the only worktree-creating script and it opens trials only, from the fixed experiment base `exp/ratmac-deterministic` on `trial-<nnn>-<slug>` branches (lines 9-35, 258, 470) - nothing addresses a work item binding or skips a declared build output"
  - "the duty is prose today: schema.md:118-133 (Ticket worktrees; Hidden lanes travel with the turn) states the worktree rule and the lanes copy-in with `target/` skipped as shop discipline, with no enforcing verb anywhere in the tree"
classification-rationale: "missing: no turn-open verb exists on any surface - the CLI enumerates ten commands, none of which performs the precondition checks, worktree creation, or lanes copy THK-001 asks for."
status: "satisfied"
mutation-kill:
  - "2026-08-25: neutralizing open's clean-trunk precondition in tools/turn.ps1 let a dirty trunk open without refusing - test/qa/tests/t107_turn_lifecycle.rs::open_creates_the_turn_and_every_precondition_refuses_with_zero_mutation failed (the dirty-trunk twin mutated instead of refusing, so the snapshot was not byte-identical); restored from the checkpoint"
required-test-refs:
  - "THKV-001"
```

Owned by ticket `t-107`.
