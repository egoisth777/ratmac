# Residual Record

```yaml
residual-id: "res-093"
goal-requirement-ref: "FDC-004"
frozen-goal-bundle-revision: "git:3eb28a2+goal-sha256:ad1007250cdf00097b5ebd79dded5eef4135f025fe64d589cc6a22a38eeb41c2"
implementation-revision: "git:7dbf766"
concrete-evidence-refs:
  - "`test/qa/tests/t059_run_residency.rs::runs_reside_under_the_plural_path` (PT-058-02, RRV-002) locates one minted `.arca/runs/<id>/`, its per-Run State File, and the absence of flat `.arca/state.toml`; this is run-directory evidence only, and its current `verdict.toml` existence assertion does not prove the frozen requirement that the nested Verdict slot address remain available while the live file is absent"
  - "`test/qa/tests/t059_run_residency.rs::run_addressing_is_always_required` (PT-058-03, RRV-002) proves that missing `--run` refuses with the roster and that named `status` and `step` operate on the chosen ordinary id"
  - "`src/cli.rs::addressed_run` joins caller input to `Scheduler::runs_dir` and tests only `is_dir`; an absolute id replaces the base and a traversing id can escape it when the target exists"
  - "`src/scheduler.rs::open_run` repeats the unchecked join before loading the State File and evidence, while `Scheduler::start` still materializes a `verdict.toml` placeholder instead of leaving only its nested slot address"
  - "hidden HT-058-02 covers unknown, malformed, empty, duplicate, and valueless input only; it does not seed an existing absolute or traversing directory, and it does not require the live Verdict file to remain absent after start"
mutation-kill: "pending: require start to leave the nested Verdict slot unmaterialized, then seed an existing directory outside `.arca/runs` and prove absolute and traversing `--run` values refuse with the roster while the whole tree stays byte-identical; both checks fail the current implementation."
classification-rationale: "partial: plural residency, the Verdict slot address, roster listing, and required addressing are specified, but start materializes the Verdict placeholder and caller-controlled run ids are not constrained to one canonical roster child."
status: "partial"
required-test-refs:
  - ".arca/goal/test-list.md#RRV-001"
  - ".arca/goal/test-list.md#RRV-002"
```

> **Owning ticket**: `t-058` remains this residual's one active ticket. Its prior `7dbf766` landing is preserved in the ticket notes; the ticket reopens for the Verdict-slot absence and canonical address-containment proofs and repairs.
