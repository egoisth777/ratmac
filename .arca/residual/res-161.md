# Residual Record

```yaml
residual-id: "res-161"
goal-requirement-ref: "LNR-001"
frozen-goal-bundle-revision: "git:d31eae6+goal-sha256:69d76bc6001de4765ff25358cd3cc181ea84a8880edf49f673b148ec45424fd9"
implementation-revision: "git:d31eae6 (the P1 integration landing)"
concrete-evidence-refs:
  - "test-hidden/ holds 48 crates t-058..t-105 (directory listing) and nothing sweeps them: grep of tools/, test/, and test-hidden/ for `sweep` finds only the Run-evidence archive sweep (test/qa/src/grown.rs:44-46) and lock-wait `expired` strings - no command runs every crate from the primary checkout or reports a per-crate verdict"
  - "tools/ holds exactly rtm.ps1 (bootstrap), check_links.py, and trial.ps1 - none runs crate test suites or produces pass/expired/red verdicts with lane ids and a total"
  - "the only re-run duty is prose: schema.md:133 - re-run the hidden lanes once from `main` last - with no command, no verdict vocabulary, and no naming of a crate the folder lacks"
classification-rationale: "missing: no sweep command exists; landed lanes are re-run by hand per working-rules prose or not at all, and a missing crate is silently skipped because nothing enumerates the folder."
status: "missing"
mutation-kill:
  - "2026-08-25: teaching the sweep to skip a crate the folder lacks (dropping its missing row) let a rostered-but-absent crate vanish silently - test/qa/tests/t108_lane_sweep.rs::the_sweep_reports_one_verdict_per_crate_and_names_a_missing_crate failed (the twin with t-105 removed found no missing row, so nothing named what the folder lacked); restored from the checkpoint"
required-test-refs:
  - "LNRV-001"
  - "LNRV-002"
  - "LNRV-005"
```

Owned by ticket `t-108`.
