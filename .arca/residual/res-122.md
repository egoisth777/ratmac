# Residual Record

```yaml
residual-id: "res-122"
goal-requirement-ref: "SVC-007"
frozen-goal-bundle-revision: "git:4f78de5+goal-sha256:a98320c3730c32a38f88afb889948ec150fb7778a244355dc081d9d7c9a30f4f"
implementation-revision: "git:4f78de5 (Engine source unchanged since ae49ff7; no behavior has been touched, only documents)"
concrete-evidence-refs:
  - "cargo test -p ratmac-qa --no-fail-fast at 4f78de5 (312s, 73 suites): exactly six checks red, all of them documentation-agreement checks - t055_runbook_spec `specification_is_routed_and_states_the_shape` and `ownership_paths_match_canonical_run_residency`, t057_deep_doctor `emitted_codes_and_documented_codes_are_one_set`, and t058_authoring_loop `repair_table_covers_exactly_the_engine_codes`, `every_documented_code_is_seeded_and_repaired`, `seeded_defects_are_repaired_by_code`. No behavioral suite failed"
  - "cargo test -p ratmac-qa --test t050_blocked_route at 4f78de5: 7 passed, 0 failed - routing, holding, and the pre-write snapshot barrier behave exactly as before the document cutover"
  - "the behavioral suites that must stay meaning-for-meaning identical exist and are green today: t047_receipts, t048_contract_gates, t049_completion_gate, t050_blocked_route, t051_abandon, t059_run_residency, t061_input_routing, t063_run_completion, t066_spawn_ledger, t067_cycle_termination, t073_lock_split, t075_engine_transition_log"
  - "defect found while taking this baseline: `cargo test --workspace` cannot prove the suite. Both packages declare a bin named `rtm` (Cargo.toml `[[bin]]` at the root and test/qa/Cargo.toml:20 pointing at the same ../../src/bin/rtm.rs), so cargo warns `output filename collision at target/debug/rtm.exe` and the featureless root build overwrites the QA build. `grep -c RATMAC_TEST_HOLD_BARRIER target/debug/rtm.exe` returns 0 after a workspace build and 3 after `cargo build -p ratmac-qa --bin rtm`, and t050's barrier check fails in the first case and passes in the second"
  - "no cutover has happened, so no before-and-after comparison exists yet: `SVCV-008` has no implementation under test/qa/tests/"
classification-rationale: "partial: the requirement is that the rename changes names only. The baseline half is real and recorded above - every behavioral suite is green at the freeze and the six red checks are all documents disagreeing with the Engine, not behavior drifting. The other half cannot be claimed: nothing has been renamed, so nothing proves behavior survived a rename. One obstacle must clear first - the duplicate `rtm` bin makes the one-command workspace run order-dependent, so the sprint's own regression evidence has to be taken with `cargo test -p ratmac-qa` until that is fixed (deferred issue i-027)."
status: "partial"
required-test-refs:
  - "SVCV-008"
```
