# Residual Record

```yaml
residual-id: "res-121"
goal-requirement-ref: "SVC-006"
frozen-goal-bundle-revision: "git:4f78de5+goal-sha256:a98320c3730c32a38f88afb889948ec150fb7778a244355dc081d9d7c9a30f4f"
implementation-revision: "git:4f78de5 (Engine source unchanged since ae49ff7; doctor.rs sha256:fdb64c784d93f0899989e35ea5761b43a6b85777156372794dd7a6b88fb19484)"
concrete-evidence-refs:
  - "test/qa/tests/t057_deep_doctor.rs `emitted_codes_and_documented_codes_are_one_set`: the mechanism the requirement needs already exists - the documented table and the Engine's emitted set are compared as one set, so a code invented on either side is caught"
  - "test/qa/tests/t058_authoring_loop.rs `repair_table_covers_exactly_the_engine_codes`, `every_documented_code_is_seeded_and_repaired`, `seeded_defects_are_repaired_by_code`: every documented code is seeded as a real defect and repaired by its code, so code identity is enforced, not asserted"
  - "cargo test -p ratmac-qa at 4f78de5: those four checks FAILED - `.arca/runbook-spec.md:174` documents `RB111` and the Engine emits no such code, which is the disagreement the checks are built to catch"
  - ".arca/runbook-spec.md `SVC-006` back-reference row: `RB111` is the sole added code and every other code keeps its exact number and severity"
  - "`grep -rn RB111 src/` returns nothing: the new code exists only in the documentation half of the pair"
classification-rationale: "partial: diagnostic identity is already mechanized in both directions - one set equality with the Engine, and a seed-and-repair lane per code - and that machinery is exactly what the requirement asks to survive the rename. It is currently red for the honest reason: the documentation added `RB111` ahead of the Engine. Message text changing while codes hold is untested because no message has changed yet."
status: "partial"
required-test-refs:
  - "SVCV-006"
```
