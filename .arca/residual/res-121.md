# Residual Record

```yaml
residual-id: "res-121"
goal-requirement-ref: "SVC-006"
frozen-goal-bundle-revision: "git:4f78de5+goal-sha256:a98320c3730c32a38f88afb889948ec150fb7778a244355dc081d9d7c9a30f4f"
implementation-revision: "git:LANDING (the t-082 landing on main)"
concrete-evidence-refs:
  - "src/state.rs:27-52 `StateError`: a refusal now carries the documented code it reports under, so the doctor relays identity instead of re-deriving it"
  - "test/qa/tests/t057_deep_doctor.rs `emitted_codes_and_documented_codes_are_one_set`: the documented table and the Engine's emitted set are equal again with `RB111` inside both"
  - "test/qa/tests/t058_authoring_loop.rs `seeded_defects_are_repaired_by_code`: `RB111` is seeded as a real defect and repaired by its own row (`rename-states`), so the new code is proven reachable, not merely listed"
  - "test/qa/tests/t082_precutover_residue.rs `the_documented_table_equals_the_engine_table`: every pre-existing code keeps its exact number and severity across the rename; `RB111` is the sole addition"
  - ".arca-private/t-082/tests/hidden.rs HT-082-01 `every_documented_defect_keeps_its_code_and_severity`: twelve documented defects are seeded from bytes the test writes and each still reports its own code at the severity the specification documents"
  - ".arca/runbook-spec.md:174, 225-226 and .arca/runbook-authoring.md:53: the specification documents `RB111` and its precedence, and the authoring table carries its repair row"
classification-rationale: "satisfied: diagnostic identity survived the cutover. The one-set equality between documented and emitted codes is green again, every older code holds its number and severity under a seeded defect, and the one added code is reachable through the authoring drill. Message text moved while codes stayed put - which is exactly what the row asks."
status: "satisfied"
mutation-kill:
  - "Renumbering the new code (`RB111` -> `RB112` in src/scheduler.rs:710 or src/machine.rs:1165) is killed by `the_documented_table_equals_the_engine_table` and by `emitted_codes_and_documented_codes_are_one_set`."
  - "Reporting the pre-cutover runbook under the generic `RB103` is killed by HT-082-01 and HT-082-02, which read the documented severity and code for every seeded defect."
  - "Letting the doctor re-classify a coded refusal instead of relaying it (src/doctor.rs:186-197) is killed by `a_precutover_runbook_refuses_by_its_own_code` and by the one-set equality check."
required-test-refs:
  - "SVCV-006"
```
