# Residual Record

```yaml
residual-id: "res-130"
goal-requirement-ref: "PCR-003"
frozen-goal-bundle-revision: "git:4ac18a1+goal-sha256:74ccde5aefbf2a5b5c4a773e6438ddb20f1d6c123d095f5bb3dc51b2e73e8440"
implementation-revision: "git:be6c872 (t-091 green landing; src/contract.rs sha256:8a2aa8e8ac01e31007b59b05)"
concrete-evidence-refs:
  - "src/contract.rs: `work_items` and `work_items_at` classify every item under the declared ticket root as open, awaiting archive, or landed, from two facts on disk - where the item sits and whether every gap record it owns is proven - and read no field a contributor wrote as prose"
  - "src/contract.rs: a tree whose two facts contradict each other yields named defects instead of a preferred signal - both places at once, archived while a gap is unproven, an item owning no gap, a cited gap that is not on disk, an unproven gap no item owns, and an item outside the working root or its archive"
  - "test/qa/tests/t091_open_work_item.rs: `PCRV-003` builds a tree with two archived items, one open item, and one proven-but-unarchived item, and proves that seeding one unproven gap record flips exactly one item to open and moves nothing else"
  - ".arca-private/t-091/tests/hidden.rs: `HT-091-01` through `HT-091-06` cover the six adversarial lanes, including the predicate running over this repository's own tree"
mutation-kill:
  - "M1 unproven work counts as proven - killed by `open_items_are_computed_from_the_tree`"
  - "M2 identifiers are addressed with regard to case - killed by `ht_091_02_input_routing_every_address_shape_has_a_stated_answer`"
  - "M3 an archived item is landed whatever its gaps say - killed by `ht_091_03_lifecycle_a_contradiction_is_named_not_resolved`"
  - "M4 a half-finished move prefers the archive copy - killed by `ht_091_04_durability_a_half_finished_move_is_refused`"
  - "M5 an item outside the flat root is dropped - killed by `ht_091_02_input_routing_every_address_shape_has_a_stated_answer`"
  - "M6 an orphaned unproven gap is ignored - killed by `ht_091_04_durability_a_half_finished_move_is_refused`"
  - "M7 containment is not enforced - killed by `ht_091_02_input_routing_every_address_shape_has_a_stated_answer`"
  - "M8 an item with no evidence is called proven - killed by `ht_091_03_lifecycle_a_contradiction_is_named_not_resolved`"
classification-rationale: "satisfied: whether work remains open is computed from the tree. One limit is stated rather than hidden - a landing sits in the working root with every gap proven until the cycle-end archive move, so that state is named `awaiting archive` and is not a defect; only the archive move itself makes an item landed. The Engine still learns nothing about what a work item contains: the predicate reads location, the item's gap list, and each gap record's status field, never a document's shape or prose."
status: "satisfied"
required-test-refs:
  - "PCRV-003"
```
