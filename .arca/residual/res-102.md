# Residual Record

```yaml
residual-id: "res-102"
goal-requirement-ref: "FDC-010"
frozen-goal-bundle-revision: "git:e9dbf46+goal-sha256:c96ba1541352a9261b8bfe23d2cf41d34049f8642c9cd750902bf40f4ed8e7c6"
implementation-revision: "git:71d6f45"
concrete-evidence-refs:
  - "src/scheduler.rs (external verdict consumption exists - FDC-003 - but no child Run can author one, because no Run can be spawned)"
  - "src/main.rs (no witnessed verdict verb exists in the surface - the deferred half is vacuously true, with nothing recording it as a deliberate deferral in behavior)"
classification-rationale: "Child-as-reviewer requires spawn and join mechanics that do not exist; the judgment a branching Phase consumes can only come from a human courier today, so the first-increment mechanism is absent."
status: "partial"
mutation-kill:
  - "disabling the child-class scope lookup in resolve_phase_scope fails the public reviewer flow (mutA)"
  - "returning the parent machine as the child scope fails all six hidden lanes (mutB)"
  - "reverting status to the top-level-only lookup fails the public reviewer flow (mutC)"
required-test-refs:
  - "MCV-005"
  - "MCV-001"
```
