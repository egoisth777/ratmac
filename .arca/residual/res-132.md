# Residual Record

```yaml
residual-id: "res-132"
goal-requirement-ref: "PCR-007"
frozen-goal-bundle-revision: "git:4ac18a1+goal-sha256:74ccde5aefbf2a5b5c4a773e6438ddb20f1d6c123d095f5bb3dc51b2e73e8440"
implementation-revision: "git:97371d9 (Engine source unchanged since d0f3ed1; src/machine.rs sha256:26f1ac26d59cbbbc9f0e2044, src/scheduler.rs sha256:20e90688c35aff69ab0c5044)"
concrete-evidence-refs:
  - "src/machine.rs:119-126: the receipt gate and the completion gate each carry a literal address field, so the identifier of the item they judge must be written into the read-only runbook"
  - "src/scheduler.rs:1708-1720: spawn already accepts binding values and refuses a name the child class did not declare, and src/ledger.rs:186 records each value once in the append-only spawn ledger - the carrier exists and no guard reads it"
  - "src/scheduler.rs:2739-2771: guard dispatch resolves the receipt and completion kinds straight from the declared address; there is no path from a binding name to the value the ledger holds"
  - "no test spawns two turns with different addresses and grades each against its own receipts: `PCRV-005` has no implementation under test/qa/tests/"
classification-rationale: "missing: the bound address does not exist. Both per-item gates can only be addressed by a literal, which a read-only runbook of the cycle may not carry, so the cycle cannot declare its ticket turns at all. The spawn-side half of the mechanism is already built, which makes this a connection to make rather than a system to invent, but nothing of the requirement holds today."
status: "missing"
required-test-refs:
  - "PCRV-005"
```
