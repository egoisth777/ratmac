# Residual Record

```yaml
residual-id: "res-126"
goal-requirement-ref: "NRR-001"
frozen-goal-bundle-revision: "git:3fbcd1b+goal-sha256:e0cdb47e9caeb203984ddf21c09dc2e4ffbd886502e098d4cac8d2538e4d9a52"
implementation-revision: "git:3fbcd1b (no work attempted yet; the ruling landed this commit)"
concrete-evidence-refs:
  - "src/blocked.rs:390 `apply_hold` opens the addressed Run, resolves the ticket beneath the declared `ticket` root, takes the root lock and the Run lock, reads the ticket file, parses its `status` field, and rewrites it - the Engine writing and reading a contributor's document"
  - "src/blocked.rs:315,360,373: the Engine itself decides what a blocker may be, refusing anything that is not `a complete five-file issue folder or a named residual record` - workflow knowledge no generic runner can hold"
  - "src/completion.rs:368 `crate::blocked::held_against(&source)` learns that work is paused by reading that same contributor document, so the gate's verdict rests on a file the agent under test can write"
  - "src/blocked.rs:632: the history entry the Engine appends is phrased in work-item words (`Hold: ticket {} held against {}`)"
  - ".arca/index.md:105 documents the Engine's blocked module as performing `ticket/blocker checks`, and .arca/runbook-spec.md:148 justifies taking the root lock by the shared-ticket read-modify-write this requirement removes"
classification-rationale: "missing: every clause of NRR-001 is false today. The hold writes a file under a workflow root, the Engine reads that file back to learn the pause, the blocker rule is a work-item shape the Engine invented, and the transition-log wording names a ticket. Nothing about the paused mark exists in the Run Record. The rule the requirement supersedes was itself the reason the code looked correct, so this is a fresh gap opened by the ruling, not a defect that was hiding."
status: "missing"
required-test-refs:
  - "NRRV-001"
  - "NRRV-002"
```
