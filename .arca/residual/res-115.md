# Residual Record

```yaml
residual-id: "res-115"
goal-requirement-ref: "ENS-010"
frozen-goal-bundle-revision: "git:be7e906+goal-sha256:93bc2362b04ce8bc15b96869fafd449c67f3f339b5956ece6d02c821d0e72119"
implementation-revision: "git:b99b7f7"
concrete-evidence-refs:
  - "`test/qa/tests/t078_resolved_root_reporting.rs::status_and_doctor_report_the_actual_resolved_engine_root` (ENSV-011) parses the `Engine root:` fact out of `rtm status`, plain `rtm doctor`, `rtm doctor --json`, and both path-addressed doctor forms across a primary checkout, a linked worktree, a no-Git fixture, an unrelated working directory, and an addressed child Run, and requires byte-identical trees around every call"
  - "`src/scheduler.rs` renders its own Engine root and `src/doctor.rs`'s `Diagnosis` carries findings with the root they were computed against; the CLI has no function that prints a caller-supplied path as the Engine root, so a report cannot name a root its own invocation did not resolve"
  - "`test/qa/tests/t077_presplit_residue.rs::open_run_preflights_residue_before_invalid_run_address` holds the `ENS-009` ordering that this ticket's refactor briefly broke: the pre-split residue refusal beats an invalid run address at the public `Scheduler::open_run` boundary"
  - "`.arca-private/t-078/tests/hidden.rs` passed all six lanes against the landed source with fixture-authored expectations - every expected Engine root is the `.ratmac/` directory the fixture itself created, never a path obtained from the resolver under test"
  - "`.arca-private/t-078/EVIDENCE.md` records a mutant kill for each of the six lanes with its command, verbatim failure text, and hash-verified restore, including the resolver mutant that drops `.join(ENGINE_DIR)` and makes both commands print the checkout directory"
  - "`.arca/residual/manifest/2026-08-06-res-115.txt` records the reconstructable src/test/.arca acceptance snapshot"
  - "`test/qa/tests/t079_root_spelling.rs` (ENSV-011) holds nine tests: the Git route and the no-Git fallback must report one spelling character for character; the whole `rtm doctor` report, its JSON escaping, and the diagnostic findings must render every path the same way; an authored runbook identifier, an authored guard field, and a stored spawn-ledger binding must reach the reader verbatim rather than normalized; and two source scans hold `crate::root` as the only place the Engine turns a path into text or writes the separator substitution"
  - "`.arca-private/t-079/tests/hidden.rs` passed all six lanes at `b99b7f7` with fixture-authored expected roots - every expectation is the `.ratmac/` directory the fixture itself created, never a value read back from the code under test"
  - "`.arca-private/t-079/EVIDENCE.md` records, per lane, the green command, the exact one-line source mutant, its verbatim failure text, the restore command, and the post-restore SHA-256, against a thirteen-file frozen-source guard"
  - "an independent read-only review APPROVED `b99b7f7` after re-hashing all thirteen published files character by character; it found and drove four guard defects and two real second renderers, all repaired: `src/pin.rs` rendered the gate-pin mismatch message with its own substitution, and `src/completion.rs` and `src/ownership.rs` repeated it outside the owner"
  - "clean-tree verification ran 280 workspace tests across 75 suites with one ignored and none failed, plus `cargo fmt --check`, warning-denying workspace clippy, and `python tools/check_links.py`"
  - "`.arca/residual/manifest/2026-08-08-res-115.txt` records the reconstructable src/test/.arca acceptance snapshot for this landing"
  - "four independent read-only reviews judged the landing: three of the source, which found the JSON and path-addressed routes rendering no root, a residue-ordering regression, and revertible de-duplication, and one of the hidden lanes, which found every lane's oracle circular"
  - "clean-checkout verification ran 271 workspace tests across 75 suites with one ignored and none failed, plus `cargo fmt --check`, warning-denying workspace clippy, and `python tools/check_links.py`"
mutation-kill: "passed: reverting the pin display to its own `.replace('\\\\', \"/\")` names `src/pin.rs:31` and reds the one-owner scan; the same revert in `src/ownership.rs` reds it too; a `path.display /* gap */ ()` and a `to_string_lossy /* gap */ ()` with a comment wedged into the call are both named; the qualified `std::path::Path::display(path)` is named; a second whole-path conversion riding beside the one pinned stored binding is named; and each of the six private lanes reds under a one-line source mutant - the raw-separator root renderer, caller-root routing, a raw stored spawn-ledger binding, a write during report rendering, a raw JSON quote, and the linked checkout standing in for the fixture primary."
classification-rationale: "satisfied: every rendered Engine-root path leaves the Engine in one spelling. `crate::root::displayed` is the single renderer and `crate::root::component` the single component conversion; the Git route, the no-Git fallback, the human report, the JSON report, and the diagnostic findings all read the same characters, and the resolved value is unchanged. Two source scans keep it that way: no module names the standard `Path::display` in code, and no module writes the backslash-to-slash substitution outside `src/root.rs`. Both scans state in their own comments what they are - a guard against re-introduction over the spellings this Engine uses - and what they are not, since reading source text cannot prove no other route to text exists. Values a human authored - a runbook phase identifier, a guard field, a stored binding - are still delivered verbatim; only paths the Engine itself renders are normalized."
status: "satisfied"
required-test-refs:
  - "ENSV-011"
```

> **Owning ticket:** `t-079` owns the open spelling clause; `t-078` supplied the resolved-root
> reporting and its green landing hash.

> **Reopened:** 2026-08-06 by human judgment on the rendered report; the resolved value is
> correct and every prior evidence ref still holds, so only the spelling clause is open.

> **Closed:** 2026-08-08 at `b99b7f7`. `t-079` landed the one owned renderer, its use at every
> reporting site, and the two scans that hold it; independent review approved the freeze and all
> six hidden lanes were earned against it.
