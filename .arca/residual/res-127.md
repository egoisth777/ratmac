# Residual Record

```yaml
residual-id: "res-127"
goal-requirement-ref: "NRR-002"
frozen-goal-bundle-revision: "git:9526098+goal-sha256:0b22bfe33cb72d06acfd25abf2184137616c5f0c2762408e0d145565a239a77c"
implementation-revision: "git:9526098 (src/scheduler.rs unchanged since b213ad5)"
concrete-evidence-refs:
  - "src/scheduler.rs:26 `const LEGACY_WORKFLOW_DIR: &str = \".arca\";` - the one declaration, reached only through `legacy_workflow_dir()` (src/scheduler.rs:29) and used once, by the pre-split residue refusal at src/scheduler.rs:644"
  - "src/scheduler.rs:23-25 the declaration's comment: it names the exception and the ENS-009 refusal that consumes it, but assigns the owner as `t-077 owns retiring it` - a ticket that landed, leaving the declaration owned by nobody"
  - "test/qa/tests/t076_roots_table.rs:17-19 pins the literal, the file that may hold it, and the exact declaration text, so a second literal and a rename each fail (ENSV-009, green at b213ad5)"
classification-rationale: "partial: two of the three clauses hold today and are mechanized. Exactly one literal exists, in one declaration, consumed only by the ENS-009 residue refusal, and the check states that exception by its own name and fails on a second literal or a rename. The clause that does not hold is the owner: the declaration says a now-landed ticket owns retiring it, which was true while the exception was temporary and is false now that the ruling makes it permanent for as long as ENS-009 stands. A reader following that comment looks for work that no longer exists. The repair is the declaration's own words plus a check that fails when the owner clause goes missing - small, and inside this record's own requirement."
status: "partial"
required-test-refs:
  - "NRRV-003"
```
