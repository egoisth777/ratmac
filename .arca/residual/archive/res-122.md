# Residual Record

```yaml
residual-id: "res-122"
goal-requirement-ref: "SVC-007"
frozen-goal-bundle-revision: "git:4f78de5+goal-sha256:a98320c3730c32a38f88afb889948ec150fb7778a244355dc081d9d7c9a30f4f"
implementation-revision: "git:2dc6ae5 (test/qa/src/baseline.rs sha256:80156ae2ce113659, test/qa/tests/t085_rename_is_behavior_free.rs sha256:60ef2c448c5d3f5b)"
concrete-evidence-refs:
  - "test/qa/src/baseline.rs: builds the Engine as it stood at the freeze commit 4f78de5 from a `git archive` extraction in a temporary directory, with the same feature set the harness build uses, and never touches the checkout, index, or worktree"
  - "test/qa/src/baseline.rs: one vocabulary table with three columns - the freeze's word, today's word, and the neutral token both collapse to - so a comparison asks only whether anything differs once the renamed words are set aside; the doctor's roster label has its own narrow row because the bare word `State` meant the file at the freeze, which is the collision this cutover resolved"
  - "test/qa/tests/t085_rename_is_behavior_free.rs::the_rename_changed_names_only: the inventory half reads all twelve behavioral suites out of the freeze commit and requires every check to still exist and still assert the same claim; formatter reflow is ignored, an added `#[ignore]` is caught, and a lost assertion is named"
  - "test/qa/tests/t085_rename_is_behavior_free.rs::the_rename_changed_names_only: the behavior half runs thirteen scenarios - doctor, status, start, four steps, hold, two abandons, and the reports after each - through both Engines in two identical projects, and compares exit codes, reports, and every file left on disk; at least four scenarios must have succeeded at the freeze, so a run of pure refusals cannot pass"
  - "cargo test --workspace --no-fail-fast at 2dc6ae5: 294 passed, 0 failed, exit 0 - one command, the whole repository, with the duplicate engine binary defect (i-027) repaired by t-086 so the run is order-independent"
  - "the six private lanes HT-085-01..06 pass from .arca-private/t-085 with `cargo test --offline`: 6 passed, 0 failed"
  - "the two surfaces that must spell the retired word - the comparison module and the proof - are enumerated in test/qa/fixtures/rebrand-audit/allowlist.tsv with their reasons, so t034_rat005 and t037_rat008 still refuse an unallowlisted occurrence"
mutation-kill:
  - "D1 src/cli.rs: the empty-roster line reworded (`no Run on the roster` -> `the roster has no Run yet`). SVCV-008 red naming one report difference; restored from checkpoint 2dc6ae5"
  - "D2 src/bin/rtm.rs: every refusal exit code floored at 2. HT-085-05 red naming `an unknown verb`: exit code 1 at the freeze, 2 today; restored"
  - "D3 src/graph.rs: the edge chooser compares only whether an input is present, not which one. HT-085-02 red - the Run no longer sits where the input pointed it; restored"
  - "D4 src/mint.rs: the minted identifier loses its fixed width (`run-{next:03}` -> `run-{next}`). HT-085-04 red with 21 differences; restored"
  - "D5 src/ledger.rs: the spawn ledger stops recording the child's class. HT-085-06 red with 20 differences; restored"
  - "D6 src/scheduler.rs: every transition-log record is truncated by twelve bytes. HT-085-03 red with 2 differences; restored"
  - "a weaker first attempt at D3 - fall back to the first out-edge when no edge matches the delivered input - survived, and is recorded as unobservable rather than as a hole: the Engine validates a delivered input against the position's declared list before it routes, so that fallback is unreachable from any legal runbook"
classification-rationale: "satisfied: both halves now exist and both are green. The inventory half proves no check was deleted, parked, or weakened between the freeze and today across all twelve behavioral suites; the behavior half proves the freeze Engine and today's produce the same exit codes, the same reports, and the same files for the same scenarios, with routing, holding, abandoning, completion, contention, an interrupted append, minting, spawning, and joining all exercised. The obstacle this record was held on - the duplicate `rtm` bin that made a workspace run order-dependent - was repaired under i-027 by t-086, so the evidence is one command's output rather than a stitched-together set of package-scoped runs. Six deliberate damages, one per lane, each turned the proof red and each was restored from the safety checkpoint."
status: "satisfied"
required-test-refs:
  - "SVCV-008"
```
