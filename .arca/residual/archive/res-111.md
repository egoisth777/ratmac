# Residual Record

```yaml
residual-id: "res-111"
goal-requirement-ref: "ENS-006"
frozen-goal-bundle-revision: "git:be7e906+goal-sha256:93bc2362b04ce8bc15b96869fafd449c67f3f339b5956ece6d02c821d0e72119"
implementation-revision: "git:1c9fb2b (green landing 92ab301)"
concrete-evidence-refs:
  - "src/cli.rs accepts `rtm spawn <name> --run <parent id> [--bind name=value ...] [--workspace <path>]`; the flag refuses when repeated, when its value is missing, and when the value is empty, and its absence means inheritance rather than an empty binding."
  - "src/scheduler.rs::Scheduler::spawn_to_with_workspace canonicalizes the given path before the mint, confines it to the repository, and records the canonical string in the child's ledger entry; an omitted flag copies the parent's recorded workspace, so a relative spelling of the same directory is the same binding and not a second one."
  - "src/scheduler.rs::Scheduler::workspace_from_ledger_entry resolves child guards and motion against the recorded path only: a path that no longer exists, one that now resolves elsewhere through a symlink or junction, and one that is not a directory each refuse by name and say they will not fall back to the caller's directory."
  - "src/scheduler.rs::Scheduler::ledger_record_of_at keeps an abandoned entry addressable, so a partially retired child still supplies its own class and workspace instead of silently inheriting the invoking directory."
  - "test/qa/tests/t074_child_workspace.rs::spawn_records_canonical_or_inherited_workspace_and_uses_it (ENSV-007) green at the landing; it drives the compiled `rtm` binary in a real Git repository and proves canonical recording, relative-spelling identity, inheritance, child motion ignoring a later caller's directory, and roster-and-ledger-byte-identical refusal for every invalid workspace input."
  - ".arca-private/t-074: six lanes green - HT-074-01 recorded binding, HT-074-02 repository confinement plus in-repository redirect refusal, HT-074-03 child motion against the recorded path, HT-074-04 respawn inheritance plus partial abandonment, a deleted binding, and same-path recreation, HT-074-05 Run-scoped receipt paths, HT-074-06 class provenance for overlapping sibling children."
  - ".arca/residual/manifest/2026-08-06-res-111-103.txt (reviewable snapshot: every path, its tracking state, and its SHA-256 under src/, test/, and .arca/, all tracked or staged; the manifest's own path is the single declared exception - untracked at the moment it was written and committed by this same landing)."
classification-rationale: "satisfied: `rtm spawn` accepts `--workspace <path>`, canonicalizes it, records it in the child's Scheduler-owned ledger entry, and inherits the parent's binding when the flag is absent; the child's guards and motion resolve against that recorded path, and every way the path can stop meaning what it meant refuses by name instead of falling back to the caller. A binding is deliberately the canonical pathname alone, so deleting and recreating a directory at exactly that path keeps the binding and later guards judge its contents; that limit is stated in src/scheduler.rs and in the ticket."
status: "satisfied"
mutation-kill:
  - "Writing `workspace: None` at spawn instead of the bound path fails HT-074-01: the ledger entry carries no workspace."
  - "Removing the repository-confinement check lets a child bind outside the repository, and removing the changed-resolution refusal lets an in-repository symlink or junction swap redirect the child; each fails HT-074-02."
  - "Storing the caller's directory instead of the child's recorded workspace fails HT-074-03 on the guarded file that only the bound directory holds."
  - "Dropping the workspace clone from the respawn successor entry fails HT-074-04, as does filtering abandoned entries out of the child ledger lookup, deleting the missing-path refusal, or letting a recreated same-path directory route anywhere but its own guards."
  - "Omitting the Run id from the receipt path collides sibling receipts and fails HT-074-05."
  - "Restoring first-match child-class resolution instead of carrying the live ledger entry's class fails HT-074-06 with `observed {\"right\"}; expected {\"left\"}`; this defect was live in the implementation and the lane caught it before review."
  - "Each mutation was restored byte-for-byte and proved by SHA-256 before the lane was run green again."
required-test-refs:
  - "ENSV-007"
  - "ENSV-013"
```
