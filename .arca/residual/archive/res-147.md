# Residual Record

```yaml
residual-id: "res-147"
goal-requirement-ref: "AOP-001"
frozen-goal-bundle-revision: "git:03eacee+goal-sha256:5d03d8cd5a62dc1f7849d4debee3022d20f0d137bade7c4173876153f393eb4e"
implementation-revision: "git:17f3c75 (the t-103 green landing)"
concrete-evidence-refs:
  - "src/machine.rs GuardKind::reads derives each guard's declared reads from the one parsed declaration the dispatch consumes (contract kinds through the parsed [roots] table); src/scheduler.rs pending_guard_labels renders the pending-guard line from it, so status prints e.g. 'pending guard: files_exact root=\"work\" path=\"release\" entries=[\"proof.txt\"]' (observed green 2026-08-21)"
  - "the focused test t103_self_describing_cli::status_names_what_each_guard_reads passes - all eight guard-kind goldens plus the rename golden pair, where renaming the declared entry in the runbook alone moves the rendering and the verdict together - and the hidden lanes ht_103_01 (inherited wording) and ht_103_06 (one rename moves rendering and verdict) pass at the green landing"
classification-rationale: "satisfied: the guard-expectation rendering is derived from the parsed guard declaration at the green landing - the focused golden-pair test and the hidden regression and cross-feature lanes are green."
status: "satisfied"
mutation-kill:
  - "making pending_guard_labels emit a hardcoded artifact label (path=\"proof.txt\" for every guard) instead of the parsed declaration's reads is killed by t103_self_describing_cli::status_names_what_each_guard_reads (panicked at t103_self_describing_cli.rs:477 - the pinned line no longer names each guard's declared artifacts)"
  - "freezing the pending-guard label list at the process's first render (a OnceLock cache inside pending_guard_labels), so a runbook rename moves the verdict but not the rendering, is killed by ht_103_06_cross_feature_one_rename_moves_rendering_and_verdict (panicked at hidden.rs:626 - the re-render after the rename replays the frozen labels instead of following the declaration)
required-test-refs:
  - "AOPV-001"
```

Owned by ticket `t-103`.
