# Residual Record

```yaml
residual-id: "res-133"
goal-requirement-ref: "PCR-008"
frozen-goal-bundle-revision: "git:4ac18a1+goal-sha256:74ccde5aefbf2a5b5c4a773e6438ddb20f1d6c123d095f5bb3dc51b2e73e8440"
implementation-revision: "git:f31fe76 + t-088 green landing (src/contract.rs sha256:f0659594776a5ee4d5a7cac9, test/qa/tests/t088_intake_authority.rs sha256:69775fd08f621326049eda2f)"
concrete-evidence-refs:
  - "src/contract.rs::gate_intake: the working-authority root is resolved from the reviewed runbook's roots table and is optional - a project that declares none keeps exactly the one place it had"
  - "src/contract.rs::gate_intake_at: an accepted ask resolves against the goal rows or the working-authority headings, and the refusal names the ask and both places it looked"
  - "src/contract.rs::authority_requirement_ids: only a markdown heading directly under the declared root declares a requirement; a fenced sample, a table row, and an ordinary line do not"
  - "src/contract.rs::contract_roots_with_table: the roots table is read once and the optional role is resolved from it, so no path is hard-coded in the gate"
  - "src/scheduler.rs:2775-2796: the runbook's `intake_contract` guard passes the same optional root through, so the Engine's gate and the contributor's gate read one rule"
  - "test/qa/tests/t088_intake_authority.rs::intake_resolves_a_requirement_in_either_authority (PCRV-006): an ask resolving only to a working-authority heading passes, and the refusal for an unresolvable ask names both places (red evidence: commit f31fe76)"
  - "test/qa/tests/t088_intake_authority.rs::an_undeclared_authority_resolves_nothing: with no authority root declared, a heading-only ask still refuses, so the second place never becomes a blanket pass"
  - ".arca-private/t-088/tests/hidden.rs: HT-088-01 through HT-088-06 - intake defect regression, heading boundary, disposition lifecycle, absent/empty/duplicate authority, whole-tree byte oracle, and intake/record gate agreement (6 passed)"
  - "cargo test --workspace: all packages green; cargo fmt --check clean; cargo clippy --workspace --all-targets reports no warning"
classification-rationale: "satisfied: the gate now knows two places a requirement can live and says so when it refuses. The second place is a declared runbook root rather than a hard-coded path, it is optional, and it never widens into a pass when it is absent or empty. Every intake defect the gate found before is still found, and the heading boundary is pinned by adversarial near misses rather than by the happy path."
mutation-kill:
  - "Deliberate damage (safety checkpoint `t-088: checkpoint - not a landing`, amended away before the landing). The five-file shape check stopped reporting a missing file - killed by `.arca-private/t-088/tests/hidden.rs::ht_088_01_regression_every_pre_existing_intake_defect_still_refuses`. Restored from the checkpoint; tree clean."
  - "Deliberate damage: `authority_requirement_ids` returned early and never scanned the declared root - killed by `test/qa/tests/t088_intake_authority.rs::intake_resolves_a_requirement_in_either_authority`. Restored; tree clean."
  - "Deliberate damage: any line whose first word is an identifier counted as a declaration, not only a heading - SURVIVED the first check, because every near miss then in the lane hid the identifier behind another word or a table pipe. The lane was widened with a paragraph and a list item that begin with the identifier, and the same mutation is now killed by `ht_088_02_input_only_a_real_requirement_heading_resolves`. Restored; tree clean."
  - "Deliberate damage: a heading inside a fenced code block counted as a declaration - killed by `ht_088_02_input_only_a_real_requirement_heading_resolves`. Restored; tree clean."
  - "Deliberate damage: deferred asks were required to resolve like accepted ones - killed by `ht_088_03_lifecycle_disposition_rules_are_unchanged`. Restored; tree clean."
  - "Deliberate damage: with no working-authority root declared, every accepted ask resolved - killed by `test/qa/tests/t088_intake_authority.rs::an_undeclared_authority_resolves_nothing`. Restored; tree clean."
  - "Deliberate damage: the refusal named only the goal rows and dropped the second place - killed by `ht_088_02_input_only_a_real_requirement_heading_resolves`. Restored; tree clean."
status: "satisfied"
required-test-refs:
  - "PCRV-006"
```
