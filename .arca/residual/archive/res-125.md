# Residual Record

```yaml
residual-id: "res-125"
goal-requirement-ref: "DEB-002"
frozen-goal-bundle-revision: "git:c98e896+goal-sha256:c424fc95698599c78e106969c2be4e9a3ca9d4889ff59e9a8a2219d476b545f1"
implementation-revision: "git:0770778 (test/qa/src/targets.rs sha256:e914a3cbee6200aa, test/qa/tests/t086_single_engine_binary.rs sha256:35acbb5b62aeeb75)"
concrete-evidence-refs:
  - "test/qa/src/targets.rs: one walk reads every `Cargo.toml` in the tree - skipping only cargo's own `target/` output and `.git` - and resolves each package's build targets from the declarations: explicit `[[bin]]` and `[lib]` rows plus cargo's conventions (`src/main.rs` under the package name, `src/bin/<name>.rs`, `src/bin/<name>/main.rs`), honouring `autobins = false`. A manifest that cannot be read or parsed is an error, never a silent skip"
  - "test/qa/src/targets.rs `Target::output`: two targets clash only when they share a workspace, a kind, and a name, because the build directory belongs to the workspace root; a library writes `lib<name>.rlib` and a command writes `<name>`, so a library never falsely collides with a command"
  - "test/qa/tests/t086_single_engine_binary.rs `no_two_build_targets_write_one_file`: the whole repository, private lane crates included, resolves with an empty collision list"
  - "test/qa/tests/t086_single_engine_binary.rs `a_planted_duplicate_target_fails_by_name`: a second target declared over the same output path is refused, and the failure line names both declarations - package, manifest path, and source - plus the file they would fight over. The check reads the declarations, so it does not depend on cargo emitting or keeping its warning"
  - ".arca-private/t-086/tests/hidden.rs: six lanes seed real duplicates - the same name twice in one package, a second package re-declaring the shipped command, an auto-discovered duplicate, a nested member, two libraries of one name, and a duplicate inside a private lane crate - and each reds with the offending declarations named; separate workspaces and a manifest under `target/` never red"
  - ".arca-private/t-086/tests/hidden.rs `ht_086_01`: the tree is byte-identical before and after a full audit run, so the check writes nothing of its own"
classification-rationale: "satisfied: the shop now states the rule in its own words and enforces it by reading the manifests, so a duplicate output path fails a named check instead of printing a warning nobody reads. Deliberate damage confirmed the check is load-bearing: ignoring discovered commands, dropping the declaration names from the failure line, merging separate workspaces into one key, skipping the private lane directories, and collapsing the library output path onto the command path each turned a lane red."
status: "satisfied"
required-test-refs:
  - "DEBV-001"
  - "DEBV-004"
```
