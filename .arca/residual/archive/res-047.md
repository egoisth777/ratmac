# Residual Record

```yaml
residual-id: "res-047"
goal-requirement-ref: "ETB-003"
frozen-goal-bundle-revision: "git:3eb28a2+goal-sha256:ad1007250cdf00097b5ebd79dded5eef4135f025fe64d589cc6a22a38eeb41c2"
implementation-revision: "git:e68bc5156b8a377f2a9c56361409241660d17c06+worktree:2026-07-24-staged"
concrete-evidence-refs:
  - "src/goal.rs: the goal revision is a SHA-256 over every file under `.arca/goal/` - relative path and bytes in sorted order - so the goal's shape is part of its identity"
  - "src/graph.rs + src/machine.rs: a transition may carry `freeze = \"goal\"`, the only recognised freeze; any other value is a Machine Class parse error"
  - "src/scheduler.rs start records `[goal] baseline` in Run evidence and leaves `frozen` empty; the marked transition computes the frozen revision, writes evidence before the State File, and mirrors the value into the existing `goal_revision` field that `rtm status` prints"
  - "src/scheduler.rs step re-computes the revision on every later transition request and appends a `goal drift` failure naming observed and expected revisions instead of short-circuiting the guard failures"
  - "test/qa/tests/t043_goal_freeze.rs: PT-042-01 freeze_is_post_integration, PT-042-02 drift_refuses_and_revert_clears pass through the real rtm CLI"
  - "hidden lanes pass: HT-042-01 added_and_removed_goal_files_are_drift (add, rename with identical bytes, delete), HT-042-02 interrupted_freeze_leaves_readable_state, HT-042-03 drift_and_pin_tamper_are_both_reported"
  - "mutation kills: freezing at Run start fails PT-042-01; hashing bytes without names fails HT-042-01's rename lane; short-circuiting the drift refusal fails HT-042-03; ignoring the freeze write error fails HT-042-02"
  - "gates: cargo test --workspace 92 passed / 0 failed / 1 ignored, cargo fmt --all -- --check clean, cargo clippy --workspace --all-targets --all-features -D warnings clean"
  - "snapshot manifest: .arca/residual/manifest/2026-07-24-t042.txt"
classification-rationale: "satisfied: the freeze is mechanized at the intake-completion boundary rather than by contributor convention, baseline and frozen revisions are distinct recorded fields, gap analysis prints the frozen value, and post-freeze goal edits - including shape-only changes - refuse the next transition naming both revisions while leaving Scheduler-owned files byte-identical."
status: "satisfied"
required-test-refs:
  - ".arca/goal/test-list.md#ETBV-007"
  - ".arca/goal/test-list.md#ETBV-008"
```
