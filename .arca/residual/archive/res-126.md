# Residual Record

```yaml
residual-id: "res-126"
goal-requirement-ref: "NRR-001"
frozen-goal-bundle-revision: "git:9526098+goal-sha256:0b22bfe33cb72d06acfd25abf2184137616c5f0c2762408e0d145565a239a77c"
implementation-revision: "git:d0f3ed1"
concrete-evidence-refs:
  - "src/blocked.rs: `HoldRequest` carries a run id, a blocker, and a confirmation - no ticket path, no ticket read, no `status` field parse; `apply_hold` writes `status` and `blocker` into the Run Record under the root and Run locks with the same rollback discipline"
  - "src/blocked.rs::verify_blocker: the blocker is checked for existence and containment beneath a declared root, and nothing else - the five-file-issue-folder rule is gone"
  - "src/completion.rs::gate_completion: the paused verdict reads the Run Record's `status`, never a contributor document"
  - "src/cli.rs: `rtm hold --run <id> --blocker <ref> --confirm \"hold <run-id>\"`; the confirmation phrase names the Run"
  - "src/blocked.rs: the transition-log entry names the Run and its blocker, never a work item"
  - "test/qa/tests/t087_no_work_item.rs::hold_records_the_pause_in_engine_state_alone (NRRV-001): a confirmed hold leaves every declared workflow root byte-identical, records the pause in the Run Record, appends one transition-log entry, and the completion gate refuses that Run by reading it"
  - "test/qa/tests/t087_no_work_item.rs::the_engine_names_no_work_item_document (NRRV-002): the source audit finds no work-item filename construction, field parse, or blocker-shape rule in `src/`, and a runbook that never mentions a work item holds, routes, and has its completion refused"
  - ".arca-private/t-087/tests/hidden.rs: HT-087-01 through HT-087-06 - refusal regression, blocker containment, record lifecycle, interrupted hold, whole-tree byte oracle, and spawn/join/abandon/gate composition (6 passed)"
  - "test/qa/tests/t085_rename_is_behavior_free.rs: the freeze comparison declares this hold change as t-087's, so the behavior difference is owned rather than silent"
  - "cargo test --workspace (297 passed, 0 failed); cargo fmt --all --check; cargo clippy --workspace --all-targets -D warnings"
classification-rationale: "satisfied: every clause of NRR-001 now holds and is proven by executable evidence. The hold writes only Engine-owned state, the gate learns the pause from the Run Record, the blocker is an opaque reference judged by existence and containment alone, and no Engine argument, message, refusal, field, or path names a work-item document. The shop still writes its own `status: \"held\"` mark by hand; nothing reads it back."
mutation-kill:
  - "Deliberate damage (working-session safety checkpoint `t-087: checkpoint - not a landing`, amended away before the landing). Containment: `verify_blocker` accepted any resolvable path - killed by `.arca-private/t-087/tests/hidden.rs::ht_087_02_the_blocker_boundary_is_containment_and_existence` (a path outside every declared root was accepted and the pause was written). Restored from the checkpoint; `git status --porcelain` clean."
  - "Deliberate damage: `apply_hold` stopped copying the blocker into the Run Record - killed by `test/qa/tests/t087_no_work_item.rs::hold_records_the_pause_in_engine_state_alone` (the record carried the paused mark with an empty blocker). Restored; tree clean."
  - "Deliberate damage: the completion gate stopped reading the pause out of the Run Record - SURVIVED the check as written, because the suite only asked `rtm status` to report the blocker and never invoked the gate itself. The check was strengthened first (a `completion_gate`-guarded fixture is held and stepped, and the verdict must name the pause and the blocker), the same damage was re-applied, and it was then killed by the same test. Restored; tree clean."
  - "Deliberate damage: the hold rewrote a document under a declared workflow root again - killed by the whole-tree byte oracle in `hold_records_the_pause_in_engine_state_alone` (`NRR-001: a hold writes no file under a workflow root`). Restored; tree clean."
  - "Deliberate damage: `apply_hold` constructed a work-item filename (`format!(\"ticket/{}.md\", plan.run_id)`) - killed by `the_engine_names_no_work_item_document`, which named the offending source line. Restored; tree clean."
  - "Deliberate damage: the transition-log entry stopped naming the blocker - killed by `hold_records_the_pause_in_engine_state_alone` (`the entry names the Run and its blocker`). Restored; tree clean."
status: "satisfied"
required-test-refs:
  - "NRRV-001"
  - "NRRV-002"
```
