# Residual Record

```yaml
residual-id: "res-063"
goal-requirement-ref: "TWL-003"
frozen-goal-bundle-revision: "git:3eb28a2+goal-sha256:ad1007250cdf00097b5ebd79dded5eef4135f025fe64d589cc6a22a38eeb41c2"
implementation-revision: "git:6d966bca1d0aa727839972ef98357aec74013f2d"
concrete-evidence-refs:
  - "`::start_rolls_back_completely` (PT-050-04) blocks .git/worktrees to fail `git worktree add` after the ref exists; the tree returns snapshot-identical with no ref, tag, registration, or sibling directory"
  - "`::rollback_removes_only_the_new_branch` (HT-050-02) keeps an unrelated trial branch at its commit through the same failure"
  - "`::rollback_never_deletes_a_moved_ref` moves the new ref with a reference-transaction hook: rollback leaves it, names where it points, and prints the manual recovery commands"
  - "mutation kills: skipping rollback fails PT-050-04, and a blind `update-ref -d` fails the moved-ref lane"
classification-rationale: "satisfied: start is atomic - it completes or removes exactly what it created, and it never deletes a ref that moved under it."
status: "satisfied"
required-test-refs:
  - ".arca/goal/test-list.md#TWLV-002"
  - ".arca/goal/test-list.md#TWLV-005"
```
