# Residual Record

```yaml
residual-id: "res-110"
goal-requirement-ref: "ENS-005"
frozen-goal-bundle-revision: "git:be7e906+goal-sha256:93bc2362b04ce8bc15b96869fafd449c67f3f339b5956ece6d02c821d0e72119"
implementation-revision: "git:9d98086 (green landing 7f78df4)"
concrete-evidence-refs:
  - "src/lock.rs owns both lock kinds: `root_path` is `<engine-root>/locks/root.lock`, `run_path` is `<engine-root>/locks/runs/<id>.lock`, ownership is a kernel claim held on the holder's open handle, and `acquire_root_then_run` takes root, tries the Run once, releases root before any wait, and re-enters under one monotonic deadline."
  - "src/scheduler.rs::Scheduler::step acquires only the addressed Run lock on every path: guards evaluate, the ledger bytes they read are snapshotted and revalidated before the commit, and no root lock is taken, so a long guard cannot block an unrelated mint."
  - "src/abandon.rs::apply_abandon and src/blocked.rs::apply_hold take root then the addressed Run unconditionally; hold holds root across the shared ticket read-compare-replace and drops it before its durability diagnostic and history append."
  - "test/qa/tests/t073_lock_split.rs::per_run_motion_serializes_without_blocking_other_runs_or_root_work (ENSV-006) proves overlapping motion on two Runs, serialized motion on one Run, and a concurrent mint completing while a long guard runs."
  - ".arca-private/t-073: six lanes green - HT-073-01 pathname ownership and kernel claim, HT-073-02 hostile Run addresses, HT-073-03 lifecycle plus the same-ticket two-Run hold race, HT-073-04 bounded pair waits and crash residue, HT-073-05 sixteen concurrent abandons and an unopenable root path, HT-073-06 root release during a spawn wait and Join-ledger revalidation."
  - ".arca/residual/manifest/2026-08-06-res-110.txt (reviewable snapshot: 544 rows of path, tracking state, and SHA-256 under src/, test/, and .arca/, every one tracked or staged; the manifest's own path is the single declared exception - untracked at the moment it was written and committed by this same landing)."
classification-rationale: "satisfied: the single `.arca/rtm.lock` invocation lock is gone; a short root lock covers minting and shared roster, ledger, and ticket mutation, a per-Run lock covers motion on one Run, acquisition is root before Run with root released before any wait, and guard evaluation holds no root lock. The Unix window between the identity check and the pathname unlink is documented in src/lock.rs and is out of the cooperative threat model."
status: "satisfied"
mutation-kill:
  - "Removing the kernel claim, unlocking immediately after claiming, resetting the pair deadline each retry, deleting the apply_hold stale-plan comparison, unlocking before unlinking, deleting the Join-ledger revalidation, and releasing root early around the shared ticket write each failed their lane and passed again after a byte-identical restore."
  - "Post-claim pathname-identity revalidation is unexercised on Windows: the platform refuses external rename or delete while the live no-delete-share handle exists, so its removal is unobservable on this host and needs a Unix runner."
required-test-refs:
  - "ENSV-006"
```
