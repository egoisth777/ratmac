# Residual Record

```yaml
residual-id: "res-127"
goal-requirement-ref: "NRR-002"
frozen-goal-bundle-revision: "git:9526098+goal-sha256:0b22bfe33cb72d06acfd25abf2184137616c5f0c2762408e0d145565a239a77c"
implementation-revision: "git:869a471 (src/scheduler.rs sha256:679b9b6add678e0c, test/qa/tests/t076_roots_table.rs sha256:fac2dc12bb792d04)"
concrete-evidence-refs:
  - "src/scheduler.rs:23-31: the declaration's own comment now names the exception (NRR-002), the refusal that consumes it (ENS-009), the owner clause `owner: the ENS-009 pre-split residue refusal in `open_run` below`, and that no cleanup ticket is coming - the misdirection that made this record partial is gone"
  - "test/qa/tests/t076_roots_table.rs::source_scan_pins_the_one_named_legacy_exception (NRRV-003): the shipped tree passes; three copies of real Engine source - one with a second literal, one with the declaration renamed, one with the owner clause deleted - each fail, and the scan names which of the four clauses broke"
  - "test/qa/tests/t076_roots_table.rs: the ENSV-009 audit now calls the same scan, so the older check and the new one cannot drift apart"
  - ".arca-private/t-076/tests/hidden.rs::ht_076_05_output_filesystem_is_repository_bounded_and_literal_scoped: the private lane requires the owner clause verbatim and refuses `t-077 owns`, `awaiting`, and `temporary` in the declaration's documentation"
  - "red evidence: before the source change both checks failed naming the same clause - `OwnerClauseMissing { path: \"scheduler.rs\", line: 26 }`"
classification-rationale: "satisfied: all three clauses of NRR-002 hold and each is mechanized. Exactly one literal exists, in one declaration, consumed only by the ENS-009 residue refusal; the declaration states its own name and its owner; and the check that proves it fails on a second literal, on a rename, and on a deleted owner clause, naming which one it caught. The scan is one function judging any source tree, so the three failure modes are proven against real Engine source rather than a hand-built string. The public and private checks share that function's contract, so the older ENSV-009 audit cannot drift away from it."
status: "satisfied"
required-test-refs:
  - "NRRV-003"
```
