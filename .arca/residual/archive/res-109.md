# Residual Record

```yaml
residual-id: "res-109"
goal-requirement-ref: "ENS-004"
frozen-goal-bundle-revision: "git:be7e906+goal-sha256:93bc2362b04ce8bc15b96869fafd449c67f3f339b5956ece6d02c821d0e72119"
implementation-revision: "git:0d30388 (green landing 061dd5a)"
concrete-evidence-refs:
  - "src/mint.rs owns the strict one-key `.ratmac/mint.toml` record: malformed TOML, missing highest, a non-integer, a negative integer, or an unknown key returns a named StateError rather than resetting or repairing the record."
  - "src/mint.rs::next bootstraps an absent record from the direct canonical roster, advances max(recorded, roster) plus one, writes `highest = <non-negative TOML integer, 0..=i64::MAX>` through a same-directory temporary file and a platform replacement, and returns only after that write; an already-maximal record refuses with `mint namespace exhausted:` and mints nothing."
  - "src/scheduler.rs::Scheduler::mint_run is the shared start/spawn/respawn allocation path and calls mint::next under its already-held root lock; it leaves a reserved ordinal durable if later Run creation fails."
  - "test/qa/tests/t072_repository_namespace.rs::mint_record_never_reissues_a_deleted_run_id (ENSV-005) proves that deleting the sole Run directory preserves `highest` and the next start returns a larger canonical id."
  - ".arca-private/t-072: HT-072-04 passed its fresh-process restart, deletion of the newest Run directory, refusal of malformed, unknown-key, negative, string, and empty records with the record left byte-identical and no Run created, roster reconciliation from an absent and from a stale lower record, and the exhausted-namespace refusal at the largest readable ordinal."
classification-rationale: "satisfied: the repository-wide high-water mark is strict, reconciles old roster entries, and is written before an id is returned, so no readable record reissues an ordinal. Durability is platform-stated in src/mint.rs: Unix syncs the temporary file, renames, then syncs the parent directory; Windows syncs the temporary file and requests a write-through replacement, and preserves that temporary file, naming both paths, when a failed replacement leaves no readable target. An absent record with an empty roster starts a new namespace at one - the only reset, and it is indistinguishable from losing both the record and the roster."
status: "satisfied"
mutation-kill:
  - "Deliberate damage from the t-072 safety checkpoint: replacing the max-plus-one calculation with `highest.min(0) + 1` made ENSV-005 fail after deletion with `second mint was run-001`. The checkpoint restore returned the durable high-water calculation and left the tree clean."
  - "Independent review (t072-review) REJECTED the first green attempt on two source defects: `read_highest` went through TOML's signed integer while `persist` wrote a `u64`, so seeding the largest signed value minted `run-9223372036854775808` and the next start could not read the Engine's own record; and the Windows path replaced without write-through and deleted the temporary file on every failure, including the ambiguous case where no readable target remained. Both were repaired in this landing, and HT-072-04 now fails against the pre-fix source by minting `run-9223372036854775808`."
required-test-refs:
  - "ENSV-005"
```
