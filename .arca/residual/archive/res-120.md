# Residual Record

```yaml
residual-id: "res-120"
goal-requirement-ref: "SVC-005"
frozen-goal-bundle-revision: "git:4f78de5+goal-sha256:a98320c3730c32a38f88afb889948ec150fb7778a244355dc081d9d7c9a30f4f"
implementation-revision: "git:d11cc0d (the t-082 landing on main)"
concrete-evidence-refs:
  - "src/scheduler.rs:680-724 `refuse_precutover_runbook`: a runbook whose top level declares `phases`, or whose child class body does, is refused with `RB111` naming the file and the repair; the check reads the bytes and writes nothing"
  - "src/scheduler.rs:726-772 `refuse_precutover_records`: a Run Record still sitting at `state.toml`, or still carrying the pre-cutover position field, is refused naming the record and the rename"
  - "src/scheduler.rs:671-672: both classes run inside `refuse_flat_residue_at`, the preflight already wired into every entry point (19 call sites, src/scheduler.rs:519 through 1941), so the refusal precedes any read, join, parse, or Run work"
  - "src/machine.rs:1159-1183 `refuse_precutover_states`: the parser is the second door - a caller that reads a runbook directly still gets `RB111`, taking precedence over the generic unknown-key code"
  - "src/doctor.rs:186-197: the doctor relays the coded refusal as a finding instead of re-classifying it, so one rule speaks with one code everywhere"
  - "test/qa/tests/t082_precutover_residue.rs `a_precutover_runbook_refuses_by_its_own_code`, `every_entry_point_refuses_precutover_records`: both classes refuse at every entry point, name the artifact and the repair, and leave both trees byte-identical"
  - ".arca-private/t-082/tests/hidden.rs HT-082-02..06: dedicated-code precedence at both doors, no Run/lock/evidence minted under refusal, identical repeat refusals, byte-and-mtime snapshots of the addressed project and the invoking checkout, and three residue classes that never shadow one another"
classification-rationale: "satisfied: a project written the old way is told so by name and left untouched. Two new residue classes join the pre-split layout class, each named by its own words, none migrated in place. The refusal is a preflight, not a parse result, so it lands before any Run work at every entry point, and the doctor speaks the same code."
status: "satisfied"
mutation-kill:
  - "Dropping the dedicated parser refusal (src/machine.rs:1183) so the pre-cutover table falls back to the generic unknown-key code is killed by HT-082-02 `the_dedicated_code_wins_only_where_it_should`."
  - "Dropping the record-residue preflight (src/scheduler.rs:672) is killed by test/qa/tests/t082_precutover_residue.rs `every_entry_point_refuses_precutover_records` and by HT-082-03, HT-082-04, and HT-082-06."
  - "Silently migrating the runbook while refusing (writing `[states.` over `[phases.` in src/scheduler.rs:706) is killed by `a_precutover_runbook_refuses_by_its_own_code` and by HT-082-05 `both_trees_survive_untouched`."
required-test-refs:
  - "SVCV-003"
  - "SVCV-005"
```
