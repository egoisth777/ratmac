# Residual Record

```yaml
residual-id: "res-153"
goal-requirement-ref: "CGD-002"
frozen-goal-bundle-revision: "git:03eacee+goal-sha256:5d03d8cd5a62dc1f7849d4debee3022d20f0d137bade7c4173876153f393eb4e"
implementation-revision: "git:b9163fd (the t-105 green landing)"
concrete-evidence-refs:
  - "test/qa/src/ticket_tags.rs declared_checks refuses a present-but-malformed tag list naming the field and the offending entry: a scalar where a list belongs, an empty-string entry, an entry outside the quoted-id shape, a duplicate id across the tag fields, and front matter truncated mid-list - never a silent drop, never a partial set (observed green 2026-08-21)"
  - "the focused test a_malformed_tag_list_refuses_naming_field_and_entry passes - each refusal names the field and the offending entry with no declared set; the hidden lanes ht_105_02 (each shape judged by its own named rule) and ht_105_04 (truncation refuses, never half-parses) pass at the green landing"
classification-rationale: "satisfied: the named refusal for a malformed tag list exists where the format is owned - the workflow-side qa reader - and the focused test and all six hidden lanes are green at the green landing."
status: "satisfied"
mutation-kill:
  - "an empty-string list entry silently skipped instead of refusing (2026-08-21): killed by a_malformed_tag_list_refuses_naming_field_and_entry at test/qa/tests/t105_ticket_check_tags.rs:154 - 'CGDV-002: an empty-string entry refuses instead of declaring' (mutant restored, tree clean)"
  - "truncated front matter returned the partially-parsed set instead of refusing (2026-08-21): killed by ht_105_04_durability_truncated_front_matter_refuses_never_half_parses at test-hidden/t-105/tests/hidden.rs:482 - 'a truncated front matter never declares: DeclaredTagSet { ... }' (mutant restored, tree clean)"
required-test-refs:
  - "CGDV-002"
```

Owned by ticket `t-105`.
