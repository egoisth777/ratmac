# Residual Record

```yaml
residual-id: "res-149"
goal-requirement-ref: "AOP-003"
frozen-goal-bundle-revision: "git:03eacee+goal-sha256:5d03d8cd5a62dc1f7849d4debee3022d20f0d137bade7c4173876153f393eb4e"
implementation-revision: "git:8cfcdec (the t-104 green landing)"
concrete-evidence-refs:
  - "src/skill.rs write_skill writes the one ratmac-operator skill folder (SKILL.md plus references/) at a caller-given fresh path by building it at a hidden partial sibling and moving it into place with one rename, refuses an occupied path - even an empty folder - having written nothing, refuses a missing parent by the no-directories rule, and refuses naming the leftover an interrupted write leaves; src/cli.rs dispatches the sibling `skill` subcommand beside scaffold and its help roster line (observed green 2026-08-21)"
  - "the focused test t104_operator_skill::the_skill_subcommand_writes_once_and_never_overwrites passes - whole-tree snapshots around both writes, every added byte under the caller's path, the doctor-reported stamp contained in SKILL.md, the occupied refusal naming the path with a byte-identical tree - and the hidden lanes ht_104_02 (each path shape judged by its own rule), ht_104_03 (write-once lifecycle, sibling writes clean), ht_104_04 (an interrupted write leaves no half folder; the refusal names the leftover), and ht_104_05 (exactly one folder, nothing else anywhere) pass at the green landing"
classification-rationale: "satisfied: the skill subcommand writes one identity-stamped folder at a fresh path and refuses occupied ones having written nothing at the green landing - the focused write-discipline test and the hidden routing, lifecycle, durability, and output lanes are green."
status: "satisfied"
mutation-kill:
  - "occupied-path refusal replaced with remove_dir_all-and-continue (overwrite instead of refuse, 2026-08-21): killed by t104_operator_skill::the_skill_subcommand_writes_once_and_never_overwrites at test/qa/tests/t104_operator_skill.rs:373 - 'an occupied path refuses, even an empty folder: Wrote skills/occupied-skill.' (mutant restored, tree clean)"
  - "atomicity broken - write_skill builds directly at the caller's path, no hidden partial sibling, no rename, all refusal checks intact (2026-08-21): SURVIVED the landed ht_104_04, which judged only a planted leftover (whole suite 6/6 hidden + 2/2 focused stayed green under the mutant); strengthened as a test fix - write_partial gained the cfg-gated test-fault-injection seam RATMAC_TEST_SKILL_FAULT=after-entry (abort mid-write, production builds compile a no-op stub) so a spawned probe build really dies between SKILL.md and references/, and ht_104_04 now first interrupts a real write - killed at tests/hidden.rs:552 'a real interrupted write never leaves a half folder at the caller's path' (mutant restored over the seam, all suites green again)"
required-test-refs:
  - "AOPV-003"
```

Owned by ticket `t-104`.
