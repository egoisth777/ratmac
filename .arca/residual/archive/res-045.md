# Residual Record

```yaml
residual-id: "res-045"
goal-requirement-ref: "ETB-001"
frozen-goal-bundle-revision: "git:be7e906+goal-sha256:93bc2362b04ce8bc15b96869fafd449c67f3f339b5956ece6d02c821d0e72119"
implementation-revision: "git:6d966bca1d0aa727839972ef98357aec74013f2d"
concrete-evidence-refs:
  - "src/pin.rs is the pin boundary: `Evidence` reads and writes the Scheduler-owned `.arca/evidence.toml`, `engine_identity` pins the running Engine, `resolve_program` refuses directories and symlinks, `build_invocation_reason` names rebuild-style guard commands"
  - "src/scheduler.rs `start` records the Stable Engine pin at Run creation; `verify_gate_pin` resolves, hashes, records on first use, and refuses a mismatch with observed and expected identity before the child is spawned"
  - "src/machine.rs accepts the `exempt = true` guard marker so a toolchain probe is identifiable as such (ETB-001)"
  - "test/qa/tests/t042_gate_pin.rs: PT-041-01 pin_is_recorded, PT-041-02 tamper_refuses_and_restore_proceeds, PT-041-03 build_at_evaluation_rejected_probe_exempt pass through the real rtm CLI"
  - "hidden lanes pass: HT-041-01 symlink_and_directory_programs_refuse, HT-041-02 recorded_pin_survives_a_crashed_run, HT-041-03 pin_refusal_carries_identity_and_diagnostic_framing"
  - "mutation kills: disabling the symlink refusal fails HT-041-01; making `build_invocation_reason` always return None fails PT-041-03; the pin-mismatch refusal is proven by PT-041-02's tamper-then-restore pair"
  - "test defect found and fixed while poking holes: the symlink lane originally matched the word 'symlink' inside its own fixture path, so it passed under mutation; it now asserts the refusal phrase 'no stable identity' and the fixture label avoids that word"
  - "existing probe fixtures (r028, r029, t017) are now marked `exempt = true`; rustc on this machine resolves to a rustup symlink, which the boundary refuses to pin"
  - "gates: cargo test --workspace 87 passed / 0 failed / 1 ignored, cargo fmt --all -- --check clean, cargo clippy --workspace --all-targets --all-features -D warnings clean"
  - "snapshot manifest: .arca/residual/manifest/2026-07-24-t041.txt"
classification-rationale: "satisfied: guard evaluation now runs only pinned or explicitly exempt programs, the pin is recorded in Run evidence at Run start and first guard use, tamper is refused with observed-versus-expected identity and no state or history mutation, and a rebuild-style guard is rejected before anything is executed."
status: "satisfied"
required-test-refs:
  - ".arca/goal/test-list.md#ETBV-002"
  - ".arca/goal/test-list.md#ETBV-003"
  - ".arca/goal/test-list.md#ETBV-004"
```
