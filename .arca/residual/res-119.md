# Residual Record

```yaml
residual-id: "res-119"
goal-requirement-ref: "SVC-004"
frozen-goal-bundle-revision: "git:4f78de5+goal-sha256:a98320c3730c32a38f88afb889948ec150fb7778a244355dc081d9d7c9a30f4f"
implementation-revision: "the t-081 landing on main, stamped below"
concrete-evidence-refs:
  - "src/model.rs:96, 212-222 `RunState`: the per-Run path set names the record `run.toml` and the struct's first field is `state`"
  - "src/state.rs:16-24 `REQUIRED_FIELDS` and src/state.rs:73: the strict parse still demands exactly seven fields, now with `state` first, and the store resolves `.ratmac/runs/<run-id>/run.toml`"
  - "src/abandon.rs:155, 190, 462; src/blocked.rs:411; src/scheduler.rs:573, 1422, 1638, 2097, 2774; src/cli.rs:788: every writer and reader joins the settled filename"
  - "src/state.rs:118-160: the replacement still writes `.run.toml.tmp-<pid>-<seq>` and renames it into place, so the record is whole or untouched"
  - "test/qa/tests/t081_run_record.rs `a_started_run_writes_run_toml_with_a_state_field`: a started Run's record is `run.toml`, its first field is `state`, all seven fields are present, no file anywhere under the Engine root carries the pre-cutover name, and a refusal names the record by its settled filename"
  - "cargo test -p ratmac-qa --test t055_runbook_spec: `ownership_paths_match_canonical_run_residency` passes - the ownership contract and the Engine now name the same path"
  - ".arca-private/t-081/tests/hidden.rs HT-081-01..06: strict parse by field name, one record per addressed command, the position field across start/step/hold, interrupted writes before and after the record write, a whole lifecycle free of the pre-cutover name, and sibling Runs plus a linked worktree each addressing their own record"
classification-rationale: "satisfied: the Run Record is the file `.ratmac/runs/<run-id>/run.toml` and the field that says where the Run sits is `state`. Everything the row leaves untouched is proven untouched - seven required fields, strict refusal on a corrupt or short record, atomic replacement, and the Scheduler as the only writer. No file is created at the pre-cutover name anywhere under the Engine root."
status: "satisfied"
mutation-kill:
  - "Resolving the addressed record at the pre-cutover filename (src/state.rs:73 `run.toml` -> `state.toml`) is killed by test/qa/tests/t081_run_record.rs `a_started_run_writes_run_toml_with_a_state_field` and by all six private lanes HT-081-01..06."
  - "Writing and requiring the position under the pre-cutover field name (`#[serde(rename = \"phase\")]` on `RunState::state` plus `REQUIRED_FIELDS[0] = \"phase\"`) is killed by `a_started_run_writes_run_toml_with_a_state_field` and by HT-081-03 `every_life_moment_names_the_position_state`."
  - "Replacing the record by copy instead of by rename (src/state.rs:145 `fs::rename` -> `fs::copy`), which leaves the temporary beside the record, is killed by HT-081-04 `interrupted_write_keeps_a_whole_record`, HT-081-02, and HT-081-06."
required-test-refs:
  - "SVCV-004"
```
