# Residual Record

```yaml
residual-id: "res-119"
goal-requirement-ref: "SVC-004"
frozen-goal-bundle-revision: "git:4f78de5+goal-sha256:a98320c3730c32a38f88afb889948ec150fb7778a244355dc081d9d7c9a30f4f"
implementation-revision: "git:4f78de5 (Engine source unchanged since ae49ff7; state.rs sha256:d05b837974e4577a82d01f3ef4d9584f227ea213390931878b304481dc514575)"
concrete-evidence-refs:
  - "src/state.rs:16-24 `REQUIRED_FIELDS`: the strict parse demands exactly seven fields and the first is `phase`, so the position field still carries the pre-cutover name"
  - "src/abandon.rs:155, 190, 462 and src/blocked.rs:411-414: every writer and reader joins the filename `state.toml` under `.ratmac/runs/<run-id>/`"
  - "src/model.rs:103-120 `run_dir`, `state_path`, `log_path`, `lock_path`: the per-Run path set names the record `state.toml`"
  - ".arca/tpl/state.toml moved to .arca/tpl/run.toml with `state = ...` at 4f78de5: the blank form the shop fills in already carries the settled name and field, so the form and the Engine now disagree"
  - "cargo test -p ratmac-qa --test t055_runbook_spec at 4f78de5: `ownership_paths_match_canonical_run_residency` FAILED with `RBSV-004: ownership must name the canonical path .ratmac/runs/<id>/state.toml` - the check pins the pre-cutover filename"
  - "no test writes or reads a Run Record at the settled path: `SVCV-004` has no implementation under test/qa/tests/"
classification-rationale: "missing: the Run Record is still the file `.ratmac/runs/<run-id>/state.toml` with a `phase` field. The strict seven-field parse, the atomic replacement, and the single Engine writer that the requirement leaves untouched are all in place and stay in place; only the two names have to move, and neither has. The blank form moved ahead of the code, which is why the ownership check reads red."
status: "missing"
required-test-refs:
  - "SVCV-004"
```
