# Residual Record

```yaml
residual-id: "res-134"
goal-requirement-ref: "PCR-009"
frozen-goal-bundle-revision: "git:4ac18a1+goal-sha256:74ccde5aefbf2a5b5c4a773e6438ddb20f1d6c123d095f5bb3dc51b2e73e8440"
implementation-revision: "git:f2e8013 (t-090 green landing; src/scheduler.rs sha256:1a9c1f64cbca13fae17bb156)"
concrete-evidence-refs:
  - "src/scheduler.rs:3232-3258: the command-class guard captures both of the child's reporting channels and renders the observed-against-expected pair from them, so a refusal names the file that is not checkpointed instead of an exit code alone"
  - "src/scheduler.rs:3672-3694: stderr wins, stdout is the declared and labelled fallback, and silence on both is stated rather than omitted (ETB-002)"
  - ".arca/runbook-spec.md: the fallback channel and its label are written into the format authority, so the rule is readable before a runbook is authored"
  - "test/qa/tests/t090_damage_checkpoint.rs: `PCRV-007` steps into a damage stage on a dirty tree, reads the refusal, proves the Run Record, the transition log, and every tracked byte are unchanged, and proves the identical step passes once committed"
  - ".arca-private/t-090/tests/hidden.rs: `HT-090-01` through `HT-090-06` cover the six adversarial lanes"
mutation-kill-list:
  - "M1 the reporting channel is never captured (stdout back to `Stdio::null()`) - killed by `the_damage_step_refuses_until_the_tree_is_committed`"
  - "M2 stdout always wins over stderr - killed by `ht_090_01_regression_command_verdicts_are_unchanged`"
  - "M3 stderr always wins, no fallback - killed by `the_damage_step_refuses_until_the_tree_is_committed`"
  - "M4 the channel is read but not labelled - killed by `the_damage_step_refuses_until_the_tree_is_committed`"
  - "M5 silence on both channels is an omission - killed by `ht_090_01_regression_command_verdicts_are_unchanged`"
  - "M6 a channel's trailing newline survives as a dangling separator - killed by `ht_090_01_regression_command_verdicts_are_unchanged`"
classification-rationale: "satisfied: the safety-commit rule is now a machine refusal a runbook declares, and the refusal carries the observed fact. Two limits stay stated rather than papered over - a file never added to version control is invisible to the guard, and the guard reports what the command reports, so a runbook that declares a silent command gets an exit code and the explicit no-diagnostic statement. Both are recorded in ADR-0015 and in the ticket."
status: "satisfied"
required-test-refs:
  - "PCRV-007"
```
