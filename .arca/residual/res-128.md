# Residual Record

```yaml
residual-id: "res-128"
goal-requirement-ref: "PCR-001"
frozen-goal-bundle-revision: "git:4ac18a1+goal-sha256:74ccde5aefbf2a5b5c4a773e6438ddb20f1d6c123d095f5bb3dc51b2e73e8440"
implementation-revision: "git:f89af91 (t-092 green landing; .ratmac/ratmac.toml sha256:964e9f2db0ee7506d03483b8)"
concrete-evidence-refs:
  - ".ratmac/ratmac.toml:1-185: the shipped Machine Class is the P1-P5 cycle - States `intake`, `gap-check`, `cut-tickets`, `ticket-turns`, `close`, `rest`, a `ticket` child class for one turn, and Exit Guards over `.arca/` only"
  - "test/qa/tests/t092_plan_build_runbook.rs `the_cycle_runs_from_intake_to_rest`: a Run starts on the shipped file and reaches `rest` by stepping alone, with no rule supplied from outside it"
  - ".arca-private/t-092/tests/hidden.rs `ht_092_02`..`ht_092_04`: one entry, one ending, nothing unreachable, only the two declared words route out of the gap check, and a runbook that drifted under a live Run refuses"
  - ".arca-private/t-092/tests/hidden.rs `ht_092_06`: one traversal fires the working-authority intake ask, two bound ticket turns, the dirty-tree refusal at the damage step, and the archive-move contradiction at close"
  - "test/fixtures/demonstration-class/.ratmac/ratmac.toml: the retired demonstration machine is preserved byte-for-byte as a fixture"
classification-rationale: "satisfied: the demonstration machine is retired to a fixture and the file the Engine reads now declares this repository's own cycle, proven by a Run that walks it end to end. Two limits are stated rather than papered over: the join waits on the turns a stage spawned, not on the tickets on disk, and `close` cannot refuse a still-open work item within the closed guard vocabulary - the record gate names the contradiction only once an item takes the archive move with its gap unproven. Both are recorded in the ticket."
status: "satisfied"
mutation-kill:
  - "M1 the gap check keeps only one way on (`clean` dropped) - killed by `the_cycle_runs_from_intake_to_rest`"
  - "M2 the ending gains an edge back to intake - killed by `the_doctor_is_clean_on_the_shipped_machine_class`"
  - "M4 the join stops requiring every child to pass - killed by `the_cycle_runs_from_intake_to_rest`"
  - "M6 the damage step stops checking the tree (the version-control probe swapped for a toolchain probe) - killed by `the_cycle_runs_from_intake_to_rest`"
  - "M7 the retired demonstration machine is edited in place - killed by `ht_092_01_regression_the_retired_machine_and_its_readers_survive`"
  - "M8 the gap check also answers to a second spelling (`Gaps`) - killed by `the_cycle_runs_from_intake_to_rest`"
required-test-refs:
  - "PCRV-001"
```
