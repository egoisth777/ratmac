# Residual Record

```yaml
residual-id: "res-128"
goal-requirement-ref: "PCR-001"
frozen-goal-bundle-revision: "git:4ac18a1+goal-sha256:74ccde5aefbf2a5b5c4a773e6438ddb20f1d6c123d095f5bb3dc51b2e73e8440"
implementation-revision: "git:97371d9 (Engine source unchanged since d0f3ed1; .ratmac/ratmac.toml sha256:4b54f66aaca35d8c0c7bb408)"
concrete-evidence-refs:
  - ".ratmac/ratmac.toml:1-2: the file still calls itself a Project Runbook demonstration; its three States are `build`, `build-review`, and `build-done`, and its guards check `artifacts/release.txt` and `rustc --version`"
  - ".ratmac/ratmac.toml:11: the build prompt still directs evidence to `.arca/evidence/`, the pre-split spelling the Engine no longer uses"
  - ".arca/schema.md:736-746: the cycle this repository runs is written as prose in the working rules - a five-step route with its own entry and exit conditions - and no machine reads it"
  - "no States named for intake, gap check, cutting the work items, a ticket turn, close, or rest exist anywhere in a runbook in this repository"
classification-rationale: "missing: the Machine Class this repository ships declares a demonstration process, not the process the shop follows. Nothing in the file names a cycle stage, so no Run can advance through the cycle at all, and the prose route in the working rules is the only carrier."
status: "missing"
required-test-refs:
  - "PCRV-001"
```
