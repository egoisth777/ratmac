# Residual Record

```yaml
residual-id: "res-076"
goal-requirement-ref: "TRP-001"
frozen-goal-bundle-revision: "git:3eb28a2+goal-sha256:ad1007250cdf00097b5ebd79dded5eef4135f025fe64d589cc6a22a38eeb41c2"
implementation-revision: "git:2aeebb4"
concrete-evidence-refs:
  - "`src/doctor.rs::diagnose` first reads the runbook and calls `MachineClass::from_toml`, then passes only the path to `doctor::audit_ownership`; `src/ownership.rs::runbook_instructions` reads the same file again and calls `MachineClass::from_toml` a second time"
  - "`test/qa/tests/t056_typed_parser.rs::the_runbook_has_exactly_one_reader` (PT-055-03, TRPV-003) rejects a second parser implementation, but does not count two invocations of the one parser or require the already parsed `MachineClass` to reach ownership auditing"
classification-rationale: "partial: every TOML walk is centralized in `MachineClass::from_toml`, but the doctor deserializes one runbook twice instead of passing its existing typed value to every consumer."
status: "partial"
required-test-refs:
  - ".arca/goal/test-list.md#TRPV-003"
```

> **Owning ticket**: the typed-parser ticket (`t-055`) remains this residual's one active ticket for passing the parsed Machine Class into ownership auditing.
