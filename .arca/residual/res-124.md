# Residual Record

```yaml
residual-id: "res-124"
goal-requirement-ref: "DEB-001"
frozen-goal-bundle-revision: "git:c98e896+goal-sha256:c424fc95698599c78e106969c2be4e9a3ca9d4889ff59e9a8a2219d476b545f1"
implementation-revision: "git:0770778 (test/qa/Cargo.toml sha256:a6b73fa85b3ff4b4, test/qa/src/lib.rs sha256:da7512390a168a26, test/qa/tests/t086_single_engine_binary.rs sha256:35acbb5b62aeeb75)"
concrete-evidence-refs:
  - "test/qa/Cargo.toml: the harness target is now named `rtm-qa` and still builds `../../src/bin/rtm.rs` with the `test-fault-injection` feature, so the two builds write `target/<profile>/rtm` and `target/<profile>/rtm-qa` and never fight; the root package's shipped command keeps its name, its path, and its source"
  - "test/qa/src/lib.rs: one macro, `ratmac_qa::engine_bin!()`, is the single place the harness names the command it launches; it expands to `env!(\"CARGO_BIN_EXE_rtm-qa\")`, so a test that tried to launch a differently-built command would not compile"
  - "test/qa/tests/t086_single_engine_binary.rs `a_test_launches_the_build_it_was_compiled_against`: the launched path ends in the harness target name and that file carries the pause-point string; then a real hold is driven twice against a throwaway started Run - through the launched command it parks at the pre-State pause point, through the freshly built shipped command it cannot, so the check proves behavior and cannot pass by claiming success"
  - "test/qa/tests/t086_single_engine_binary.rs `the_shipped_command_keeps_its_name_and_stays_pause_free`: the audit resolves exactly one command in package `ratmac`, named `rtm`, writing `target/<profile>/rtm`, and its bytes contain no pause-point marker"
  - "measured repair: `cargo build --workspace --all-targets` prints no `output filename collision` line at 0770778, where the same command printed four at f9692cf; `target/debug/rtm.exe` and `target/debug/rtm-qa.exe` both exist, and only the second contains `RATMAC_TEST_HOLD_BARRIER`"
  - "the 45 harness call sites across 37 public test files now go through the macro; `grep -rn CARGO_BIN_EXE_rtm test/` returns the single definition in test/qa/src/lib.rs"
  - "test/qa/tests/t031_rat002.rs: the rebrand metadata check now requires the harness manifest to declare `name = \"rtm-qa\"` and to declare no target named `rtm`, keeping its original meaning - the harness builds the canonical Engine source, never the retired one - while pinning the split"
  - ".arca-private/t-086/tests/hidden.rs `ht_086_04_build_order_stops_deciding_what_a_test_launches`: a shipped `cargo build --bin rtm` leaves the harness command's bytes hash-identical, the shipped bytes never gain a pause point, and after that build a hold driven through the harness command still parks at the pre-State pause point while the same drive through the shipped command runs past it"
classification-rationale: "satisfied: the two builds write two files, the harness names its own target in exactly one place, and both halves are checked - the shipped command keeps name, path, and pause-point freedom, and a test provably launches the build it was compiled with. Deliberate damage six put the old name back in test/qa/Cargo.toml: the harness then failed to compile at all, naming the missing target, and the audit reported the collision by both package names."
status: "satisfied"
required-test-refs:
  - "DEBV-001"
  - "DEBV-002"
  - "DEBV-003"
```
