# Residual Record

```yaml
residual-id: "res-124"
goal-requirement-ref: "DEB-001"
frozen-goal-bundle-revision: "git:c98e896+goal-sha256:c424fc95698599c78e106969c2be4e9a3ca9d4889ff59e9a8a2219d476b545f1"
implementation-revision: "git:0116fb4 (no repair attempted yet; the declarations are as they were when the issue was filed)"
concrete-evidence-refs:
  - "Cargo.toml: the root package `ratmac` ships the command from `src/bin/rtm.rs`; test/qa/Cargo.toml declares a second target named `rtm` pointing at that same `../../src/bin/rtm.rs`, built with the `test-fault-injection` feature, and both are members of one workspace, so both write `target/debug/rtm.exe`"
  - "cargo build --workspace at f9692cf: four collision warnings - `target/debug/rtm.exe`, `target/debug/rtm.pdb`, and both under `deps/` - each naming the bin target `rtm` in `ratmac-qa` and the bin target `rtm` in `ratmac`"
  - "measured flip at f9692cf: after `cargo build -p ratmac` the file at `target/debug/rtm.exe` contains no `RATMAC_TEST_HOLD_BARRIER` string and the already-built check `t050_blocked_route::runbook_swap_before_hold_state_write_refuses_without_a_half_route` fails with `hold did not reach the pre-State snapshot barrier` (6 passed, 1 failed); after a test-package build the string is present and the same check passes"
  - "104 occurrences of `CARGO_BIN_EXE_rtm` across 37 public test files name the colliding target today, so the rename has a mechanical, one-way edit as its cost"
  - "no test, gate, or tool asserts which of the two builds it launched; nothing in the tree measures this"
classification-rationale: "missing: the requirement asks that no two build targets write one output file and that a test always launches the build it was compiled against. Both halves are false today, and the failure is not theoretical - the hold barrier check flips colour with build order, measured above. Nothing in the tree names the shipped command's freedom from pause points either; that half happens to hold, but it is unproven."
status: "missing"
required-test-refs:
  - "DEBV-001"
  - "DEBV-002"
  - "DEBV-003"
```
