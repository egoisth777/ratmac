# Residual Record

```yaml
residual-id: "res-116"
goal-requirement-ref: "SVC-001"
frozen-goal-bundle-revision: "git:4f78de5+goal-sha256:a98320c3730c32a38f88afb889948ec150fb7778a244355dc081d9d7c9a30f4f"
implementation-revision: "git:4f78de5 (Engine source unchanged since ae49ff7; model.rs sha256:9375e41f2ca6b71171821e27edae10eff7624ba3459418f98a5733788054b30f, graph.rs sha256:bab0eea34107843611f2690869876326af6a66813c074d260b3641a4e1ba3ceb)"
concrete-evidence-refs:
  - ".arca/goal/ubi-lang.md: State, Run Record, Run, and State Prompt each have their own entry, and `status` keeps its Engine-owned lifecycle meaning; the pre-cutover spellings are recorded as two dedicated residue entries rather than deleted"
  - ".arca/schema.md `SVC-009`: the same three names bind the contributor side, and the section that used to be called State is now Runtime layout so the word no longer names two things in the working rules"
  - ".arca/dict.md: the `Phase` glossary entry is replaced by State and Run Record entries; `SVC-010` records that the delete-every-mention rule applies to live documents only"
  - "src/graph.rs:6 `pub struct Phase(String)`: the Engine's own type for a position in the graph is still named Phase"
  - "src/model.rs:122-146 `pub struct Run { phase: Phase, status: Status }`: the live instance holds the position under the pre-cutover name, so the program still trades the two words"
  - "no test asserts the separation: `SVCV-001` has no implementation under test/qa/tests/"
classification-rationale: "partial: the three names are settled and separated in every written authority that landed at 4f78de5, which is what the wish demanded before any rename. The requirement binds the running program too, and there the position is still `Phase` in the Engine's types and in the Run's own field, so the words have not yet stopped trading places where a caller can observe them. No proof exists either - `SVCV-001` is unimplemented - and no proof means never satisfied."
status: "partial"
required-test-refs:
  - "SVCV-001"
```
