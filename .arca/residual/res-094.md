# Residual Record

```yaml
residual-id: "res-094"
goal-requirement-ref: "FDC-005"
frozen-goal-bundle-revision: "git:89071f1+goal-sha256:f0b4219fe572ef4b5bb655acfb12673986f3d747f8a52bd3dc129a7e552fd16b"
implementation-revision: "git:7dbf766"
concrete-evidence-refs:
  - "test/qa/tests/t060_runbook_pin.rs::pin_is_hash_only_with_no_per_run_copy (PT-059-01, RRV-003): after rtm start the run's evidence records the runbook's SHA-256 as [runbook] sha256 — one hex digest, no path, no bytes; a byte-equality scan finds no copy of the runbook under .arca/runs/<id>/ or anywhere else; editing the runbook then stepping refuses naming observed and expected hash and leaves the whole tree byte-identical"
  - "test/qa/tests/t060_runbook_pin.rs::flat_layout_residue_refuses_and_instructs (PT-059-02, RRV-003): a planted flat .arca/state.toml refuses every engine invocation, names the residue path and the migrate-or-remove repair, and modifies nothing on disk — refuse and instruct, never auto-migrate"
  - "test/qa/tests/t059_run_residency.rs::requirements_trace_to_seed_and_research (PT-058-01, RRV-001): FDC-005 traces to its adopted-default record in the split seed's design (i-016-fsm-doctrine-convergence/design.md, Adopted defaults, batch human sign-off 2026-07-29) and the research sections it condenses under .arca/research/re-ratmac-FSM/"
  - "hidden lanes 6 passed / 0 failed at .arca-private/t-059/tests/hidden.rs: ht_059_01_regression, ht_059_02_input_routing (four residue variants — flat state alone, plus flat evidence.toml, plus stale rtm.lock, garbage flat-state bytes — each refuses start, status --run, step --run with the same fact-and-repair instruction; whole-tree snapshots prove nothing migrated, rewritten, or deleted), ht_059_03_lifecycle, ht_059_04_durability (byte-identical trees around both refusals; removing the residue or restoring the runbook bytes lets the same command proceed), ht_059_05_output_filesystem (the pin lands inside evidence.toml and nowhere else; byte-equality scan finds no runbook copy anywhere), ht_059_06_cross_feature (the pin blocks the transition, never the retirement)"
  - "mechanism: src/pin.rs (Run evidence gains the runbook_sha256 field, hash only), src/scheduler.rs (refuse_flat_residue at open, open_run, and the top of start, so the refusal names the residue, never the cap; start hashes the runbook under the invocation lock; verify_runbook_pin compares on every addressed read and refuses a mismatch naming observed and expected identity, writing nothing)"
  - "gates re-run at this closure: cargo test --workspace --no-fail-fast — 57 suites, 204 passed, 0 failed (release-acceptance lane skipped as designed without RATMAC_RELEASE_ACCEPTANCE=1); hidden tally across .arca-private/t-058, t-059, t-060 — 18 passed, 0 failed"
  - "the hash-only-pin-and-residue ticket's closing account: .arca/ticket/t-059.md, section 'P5 notes (2026-07-30)' — its review named this record's evidence-ref update as the step still owed before closure; this closure is that update"
  - "snapshot manifest: .arca/residual/manifest/2026-07-30-t058-t060.txt (path, tracking state, SHA-256 per file; the .arca-private rows are appended by hand with state untracked because gitignored paths never reach git status --porcelain, the tool's only tracking source)"
  - "program landing: git:7dbf766 — the inseparable green implementation for t-058, t-059, and t-060; the three tickets were built and verified in one worktree"
mutation-kill: "mutation kill: an implementation that records the pin but never compares fails PT-059-01's step-refusal assert; one that refuses naming only one identity fails the both-hashes assert (expected and observed computed independently); one that auto-heals by rewriting the pin fails the whole-tree byte-compare; one that copies the runbook into the run directory fails the byte-equality copy scan and HT-059-05's write-site enumeration; an exit-0 residue adoption, a migrating or deleting refusal, or a bare report with no instruction each fail PT-059-02."
classification-rationale: "satisfied: the runbook pin is hash-only — recorded at start as one SHA-256 in Run evidence, verified on every addressed read, no per-run copy exists anywhere by byte-equality scan — and a flat-layout residue refuses naming the path and the repair while modifying nothing: both FDC-005 prongs proven by visible suite and hidden lanes."
status: "satisfied"
required-test-refs:
  - ".arca/goal/test-list.md#RRV-001"
  - ".arca/goal/test-list.md#RRV-003"
```

> **Note**: The goal integration landing is `git:89071f1`; the goal-sha256 half names the folded goal bytes exactly.

> **Owning ticket**: the hash-only-pin-and-residue ticket (`t-059`) owned this gap — `.arca/ticket/t-059.md`; closed satisfied at this pass's gap-check redo (2026-07-30).
