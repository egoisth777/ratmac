# Residual Record

```yaml
residual-id: "res-107"
goal-requirement-ref: "ENS-002"
frozen-goal-bundle-revision: "git:be7e906+goal-sha256:93bc2362b04ce8bc15b96869fafd449c67f3f339b5956ece6d02c821d0e72119"
implementation-revision: "git:t-071-engine-root"
concrete-evidence-refs:
  - "src/root.rs::Roots::resolve: the primary worktree comes from the first `git worktree list --porcelain` entry, gated on `rev-parse --is-inside-work-tree`, with GIT_DIR, GIT_COMMON_DIR, and GIT_WORK_TREE scrubbed and the invoking checkout as the subprocess working directory; every failure falls back to the invoking checkout's own `.ratmac/`"
  - "src/root.rs::Roots::machine_class_path: the Machine Class is always addressed in the invoking checkout, never in the resolved shared root"
  - "test/qa/tests/t071_engine_root.rs::linked_worktree_shares_primary_runtime_but_reads_its_own_class (ENSV-002): a real linked worktree observes the primary checkout's roster and Run, receives no private `.ratmac/runs/`, and an edit to its own Machine Class refuses on FDC-005 pin drift with the tree byte-identical"
  - "test/qa/tests/t071_engine_root.rs::non_git_checkout_uses_its_current_root (ENSV-003): a checkout with no `.git` resolves its own `.ratmac/` and never the parent sentinel root"
  - "test/qa/tests/t071_engine_root.rs::separate_git_dir_uses_worktree_engine_root: a `git init --separate-git-dir` repository resolves the work tree's `.ratmac/`, not the directory beside its external Git storage"
  - "test/qa/tests/t071_engine_root.rs::ambient_git_dir_and_work_tree_do_not_divert_non_git_checkout: ambient GIT_DIR and GIT_WORK_TREE pointing at an unrelated repository do not divert a non-Git checkout from its own root"
  - "test/qa/tests/t071_engine_root.rs::bare_repository_worktrees_share_a_non_bare_engine_root: two linked worktrees of a bare repository agree on the first non-bare worktree's `.ratmac/`, and the bare Git store never receives an Engine root; before the src/root.rs record-parsing fix this test failed with `ENS-003: the bare Git store must never receive an Engine root`"
  - ".arca-private/t-071/tests/hidden.rs::invocation_contexts_choose_only_their_contractual_engine_root (HT-071-02) and ::linked_machine_class_drift_refuses_existing_run_without_mutation (HT-071-06): six hidden lanes green"
  - "cargo test --workspace (250 passed, 0 failed, 1 opt-in release-lane ignored)"
  - "cargo fmt --all -- --check and cargo clippy --workspace --all-targets --all-features -- -D warnings (both green)"
  - ".arca/residual/manifest/2026-08-06-res-106-107.txt (reviewable snapshot: 538 files, all tracked)"
  - "Independent review (p5-review): the first pass REJECTED on the common-directory parent assumption and the second on the unparsed bare porcelain record; the resolver was rebuilt on the worktree roster, taught to skip bare records, and re-reviewed"
classification-rationale: "satisfied: a linked worktree resolves the primary checkout's Engine root while reading its own tracked Machine Class, a checkout without Git resolves its own root, and no ambient Git environment or external Git storage can divert either choice."
status: "satisfied"
mutation-kill:
  - "Deliberate damage against the ticket-branch source (working-session safety checkpoint, amended away before the landing): resolving the Engine root from the invoking checkout only, ignoring the primary worktree, failed linked_worktree_shares_primary_runtime_but_reads_its_own_class (the linked status could not see run-001) and hidden lane HT-071-02; then the file was restored and the tree confirmed clean."
  - "Deliberate damage against the ticket-branch source (working-session safety checkpoint, amended away before the landing): reading the Machine Class from the shared Engine root instead of the invoking checkout failed the same test's FDC-005 pin-drift assertion, which observed a successful status instead of a refusal; then the file was restored and the tree confirmed clean."
required-test-refs:
  - "ENSV-002"
  - "ENSV-003"
```
