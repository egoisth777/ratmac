# Residual Record

```yaml
residual-id: "res-113"
goal-requirement-ref: "ENS-008"
frozen-goal-bundle-revision: "git:9526098+goal-sha256:0b22bfe33cb72d06acfd25abf2184137616c5f0c2762408e0d145565a239a77c"
implementation-revision: "git:b213ad5 (landed on main, scheduler.rs sha256:c4223da3b330ac0e27b574335a1a5028df32ca89d880daf2c0259614257be0de)"
concrete-evidence-refs:
  - "src/roots.rs: typed `[roots]` parsing, per-role canonical resolution, and the RB601..RB604 diagnostics; `WorkflowRoots::validate` returns the canonical mapping the operation then uses, and the Engine root canonicalizes through its nearest existing ancestor so the overlap refusal holds before `.ratmac/` exists"
  - "src/scheduler.rs: one runbook snapshot per operation - the bytes parsed, the hash pinned, and the roots validated all come from a single read, and that snapshot is re-read and compared under the lock immediately before every State write"
  - "src/blocked.rs: the hold resolves its ticket and blocker only beneath declared roots, refusing absolute and parent-escaping references, and its plan is opaque so no caller can apply a hold that no human confirmed"
  - "test/qa/tests/t076_roots_table.rs (ENSV-009): an undeclared role, a missing root path, and a root overlapping the Engine root each refuse with their own diagnostic; the source scan admits exactly one named `.arca` literal and refuses a second one or a rename"
  - "test/qa/tests/t050_blocked_route.rs: a runbook swapped between parse and the locked write refuses before any State write, and a blocker naming a path outside the declared root refuses"
  - ".arca-private/t-076/tests/hidden.rs (HT-076-01..06): six lanes, each killed by a real mutation with its captured failure text and restored byte-identically, plus both directions of the named-exception lane. The crate was proved once inside the ticket worktree and then destroyed by an out-of-order worktree removal; it was re-authored from the lane owner's own record into the primary checkout and every lane was re-proved from landed main - real red, restore to a clean tree, cache-clean green - so the cited bytes exist and a reviewer can re-run them"
  - "cargo test --workspace (261 passed, 73 suites, 1 opt-in release-lane ignored); cargo fmt --check clean; cargo clippy --workspace --all-targets -D warnings clean; tools/check_links.py clean"
  - ".arca/residual/manifest/2026-08-06-res-113.txt (reviewable snapshot: 552 rows under src/, test/, and .arca/; the manifest declares its own path as its single exception, since it cannot list itself)"
classification-rationale: "satisfied at this freeze by re-judgment, not by new code. The three diagnostics, the roots table, and the single-snapshot discipline were green at `b213ad5` and are unchanged. The one clause that held this record open - `ENSV-009` asking that a source scan find no `.arca` literal in `src/` at all - was ruled on 2026-08-10 (`NRR-002`) and is superseded: Engine source may name the retired folder exactly once, in a declaration the check pins by name. That declaration exists (src/scheduler.rs:26) and the check already refuses a second literal or a rename, so nothing about `ENS-008` is unmet. The freshly opened obligation - that the declaration state its own owner - is `NRR-002`'s, recorded as res-127, not this record's."
status: "satisfied"
required-test-refs:
  - "ENSV-009"
```
