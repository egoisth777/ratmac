# Residual Record

```yaml
residual-id: "res-120"
goal-requirement-ref: "SVC-005"
frozen-goal-bundle-revision: "git:4f78de5+goal-sha256:a98320c3730c32a38f88afb889948ec150fb7778a244355dc081d9d7c9a30f4f"
implementation-revision: "git:4f78de5 (Engine source unchanged since ae49ff7; scheduler.rs and state.rs carry the only residue refusals that exist)"
concrete-evidence-refs:
  - "`grep -rn RB111 src/ test/` at 4f78de5 returns nothing: the diagnostic the settled format reserves for a runbook that still declares `phases` does not exist in the Engine"
  - "src/machine.rs:13: a `phases` table is not residue to the parser today - it is the accepted spelling, so nothing can refuse it"
  - "src/state.rs:16-24: a Run Record carrying `phase` is likewise the accepted shape, and a record carrying `state` would be refused by the strict seven-field parse - the refusal points the wrong way"
  - "src/scheduler.rs:25 `LEGACY_WORKFLOW_DIR`: the one refuse-and-instruct residue class that exists covers the pre-split `.arca/` layout (ENS-009), proving the posture is already mechanized and can be extended to a third class"
  - "no test plants pre-cutover residue: `SVCV-003` and `SVCV-005` have no implementation under test/qa/tests/"
classification-rationale: "missing: nothing in the Engine recognizes pre-cutover residue, so nothing refuses and instructs over it. The requirement is written as an extension of a posture that already works for the pre-split layout, which makes the shape of the work clear, but not one line of it exists. `.arca/runbook-spec.md` already documents `RB111` and its precedence over the generic unknown-key code; the Engine has to catch up."
status: "missing"
required-test-refs:
  - "SVCV-003"
  - "SVCV-005"
```
