# Issue specification

Dispositions below record the author's proposed decision; P1 confirms or revises them at integration.

## Requirement Records

| Requirement ID | Requirement | Disposition | Rationale | Accepted Forward Authority Refs |
| :--- | :--- | :--- | :--- | :--- |
| `IAS-001` | The intake gate refuses an issue folder whose `index.md` carries an `issue-id` different from its folder name, an empty or placeholder `provenance`, or any unfilled template placeholder in any of its five files, naming the offending file and the first offending token. | accepted | `.arca/index.md` states these three front-matter rules and says the schema gate mechanizes them, but `gate_intake` checks none of the three: it passed a folder holding 30 unfilled placeholders. This closes a hole in accepted requirement PGE-001 rather than adding new surface. | `.arca/current/spec.md`, `.arca/current/design.md` |
| `IAS-002` | A contributor can run one authoring check over an issue that is still `pending`, which applies every shape rule except the terminal-status rule, and reports the same defects the intake gate would report after integration. | accepted | The only runnable check today refuses `pending` outright with `status 'pending' is neither integrated nor rejected`, so a newly authored issue cannot be validated before P1 folds it in. | `.arca/current/spec.md` |
| `IAS-003` | One command scaffolds an issue folder: it derives the next issue number as one greater than the highest occupied by any active or archived issue, refuses on any collision with zero mutation, writes the five files with `issue-id`, folder name, provenance and `status: "pending"` already resolved, and leaves every remaining author field marked as unfilled. | accepted | Authoring is `mkdir` plus `cp` plus 30 hand edits with no stated numbering rule; the gap at i-003 to i-005 shows the rule is neither written nor mechanized. Trials already get exactly this treatment under TWL-002. | `.arca/current/spec.md`, `.arca/current/design.md` |
| `IAS-004` | The issue blanks separate author fields from integrator fields, so that every field an author is asked to fill is one an author can fill honestly at authoring time. | accepted | `.arca/index.md` assigns requirement IDs and dispositions to P1, yet the blank demands both from the author. Five of the seven issues written so far hand-repeat the same disambiguating sentence, which appears nowhere in the blank. | `.arca/current/spec.md` |
| `IAS-005` | Requirement-row disposition is read from the disposition column alone, not from the presence of a word anywhere in the row. | accepted | `accepted_requirements` lowercases the whole row and tests `contains("accepted")`, so a rationale containing the word in any form classifies its row as accepted. | `.arca/current/design.md` |
| `IAS-006` | A requirement prefix is claimed once per issue batch and never collides with a prefix already used by the goal. | deferred | Real, but no collision has occurred and no rule is written; deferring keeps this issue to the four proven defects plus the parser fix. | none |
