# Issue specification

Dispositions below were confirmed or revised at the 2026-08-10 planning pass; the author's proposed decision for every ask was `deferred`.

The formal artifact name is **Plan-Build Runbook**. `PCR` remains this
issue's stable requirement-ID prefix, coined from the earlier working name
**P-Cycle Runbook** and defined in [ubi-lang.md](ubi-lang.md).

## Requirement Records

| Requirement ID | Requirement | Disposition | Rationale | Accepted Forward Authority Refs |
| :--- | :--- | :--- | :--- | :--- |
| `PCR-001` | `.arca/ratmac.toml` declares the P1-P5 cycle this repository runs - its States, prompts, and Exit Guards over `.arca/` - replacing the demonstration machine. Extended 2026-08-03 (safe deliberate-damage integration, [i-022](../i-022-safe-deliberate-damage/index.md)): those Exit Guards include the automatic dirty-tree refusal before any deliberate-damage step, and the Engine's intake gate accepts an accepted ask that resolves to an explicit working-authority requirement-ID heading, not only to a goal row. | accepted | The four dependency contracts the 2026-07-29 pass waited for - run residency, input-routed transitions, input delivery and durability, and Run completion - plus machine composition and the engine-namespace split are all landed and archived, so the entry condition steering records for this issue is met. Two addresses move at integration: the Machine Class is read from `.ratmac/ratmac.toml`, not the pre-split spelling this ask was written against. The 2026-08-03 extension carries two capabilities that are provable on their own, so integration splits them into `PCR-008` and `PCR-009` below; `PCR-001` keeps its identity and its own scope, the runbook itself. | [goal PCR-001](../../../goal/spec.md#integrated-plan-build-runbook-requirements) |
| `PCR-002` | `rtm status` answers “where are we”; the tree-derived lookup becomes only the no-live-Run fallback or is retired. | accepted | The addressed report already prints `State: <name>` from the Run Record, so naming the stage costs a runbook whose State names are the cycle's stages. The reach question the design left open - perpetual Run or per-sprint Run - is settled by the format rather than by preference: a machine whose rest State routes back to intake gives every State an inbound ordinary edge, which is the no-initial-State error `RB202`. A sprint is therefore one Run, and the tree lookup stays as the labelled no-live-Run fallback rather than being retired. | [goal PCR-002](../../../goal/spec.md#integrated-plan-build-runbook-requirements) |
| `PCR-003` | “Open ticket” is machine-checkable, distinguishing landed work from executing work without prose inference. | accepted | The design offered two routes and the shop has since taken route (a) on its own: the authorized ticket archive move is written into the working rules and `.arca/ticket/` now holds only `archive/`. What is left is the machine check itself over the real tree, not another mechanism. | [goal PCR-003](../../../goal/spec.md#integrated-plan-build-runbook-requirements) |
| `PCR-004` | Once a Run is live, `rtm`, not a contributor, appends the landing line to `.arca/log.md`. | rejected | Superseded by later rulings that went the other way, deliberately. The working rules now state that `.arca/log.md` is human-only history and that `rtm` never writes it, recording Run transitions in `.ratmac/log.md` instead ([schema.md, append-only history](../../../schema.md#evidence-and-archive-rules)); the ruling that the Engine has no work-item concept forbids the Engine writing any file under a workflow root ([goal NRR-001](../../../goal/spec.md#integrated-namespace-row-ruling-requirements), [ADR-0014](../../../goal/design.md#the-engine-has-no-work-item-concept)). The property this ask protected - history that whoever is working cannot rewrite - is carried by the Engine-owned transition log, which is the design's own recorded alternative in its stronger form. Nothing is left unowned. | — (rejected 2026-08-10) |
| `PCR-005` | The Plan-Build Runbook is doctor-clean, with no gate whose verdict rests on content writable by the agent under test. | accepted | Measurable today: the shipped demonstration machine makes `rtm doctor` exit `1` with two `RB302` warnings, one per file-shaped guard. The requirement is reachable because the contract- and receipt-class guards raise no such warning, and it is worth pinning because nothing currently stops a later edit from reintroducing one. | [goal PCR-005](../../../goal/spec.md#integrated-plan-build-runbook-requirements) |
| `PCR-007` | The P4/P5 loop keeps per-ticket sensitivity and completion gates while the runbook stays read-only and free of literal ticket ids. | accepted | The design recorded four candidates and recommended binding the gate target from the Run Record's active refs, warning that a value only ever set and never derived lets a stale reference choose what the gates grade. Machine composition landed after that was written and answers it better: a spawning State declares one child turn, the caller supplies the address at spawn time as a binding value, and the Engine records it in the append-only spawn ledger. The runbook holds no id, the value cannot go stale because it is never rewritten, and the Engine reads an opaque string rather than learning what a work item is - which is what the no-work-item ruling requires. | [goal PCR-007](../../../goal/spec.md#integrated-plan-build-runbook-requirements) |
| `PCR-008` | The Engine's intake gate accepts an accepted ask that resolves to an explicit working-authority requirement-ID heading, not only to a goal row. | accepted | Split at integration from `PCR-001`'s 2026-08-03 extension, which folded two independently provable capabilities into one ask. Without it the intake gate refuses this repository's own tree, because working-authority requirements such as the discard-guard rules and the state-vocabulary rules resolve to headings in the working rules and mint no goal row. | [goal PCR-008](../../../goal/spec.md#integrated-plan-build-runbook-requirements) |
| `PCR-009` | An Exit Guard refuses the step into the deliberate-damage stage while the working tree carries uncommitted changes to tracked files. | accepted | Split at integration from `PCR-001`'s 2026-08-03 extension for the same reason. Integrated at the strength the closed guard vocabulary can prove: a command guard observes an exit code, so it can prove the tree carries no uncommitted change to a tracked file, which is exactly the hazard the safety-commit rule names - the composition-format turn destroyed a tracked file by restoring stale index bytes. A file that was never added stays invisible to it, and the restore this rule protects never touches such a file. Closing that remainder needs a repository-state guard kind, which steering already carries as deferred debt and which this issue's own non-goals route to a separate issue. | [goal PCR-009](../../../goal/spec.md#integrated-plan-build-runbook-requirements) |

`PCR-006` was dropped at review on 2026-07-28: extracting the hard-coded `.arca/issue|ticket|residual|goal`
paths from the Engine (R-016) buys nothing until a second project exists, and `.arca/steering.md` already
defers it. The numbering keeps its hole so the decision stays legible.

## Acceptance criteria

- The Plan-Build Runbook describes the P1-P5 cycle, and `rtm start` plus `rtm step` drive it without a human
  restating a rule the runbook should have carried.
- `rtm status` names the current stage of this repository, and any second answer to that question is either
  derived from it or explicitly labelled a no-Run fallback.
- `rtm doctor` exits `0` on the Plan-Build Runbook.
- No State Prompt and no gate contract in it instructs an agent to write a Scheduler-owned file.
- Every guarantee `PGE-003` and `PGE-005` already carry per ticket is still carried per ticket.
