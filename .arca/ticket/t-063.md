---
ticket-id: t-063
residual-ids:
  - "res-098"
behavior-refs:
  - "../goal/spec.md (FDC-002)"
design-refs:
  - "../goal/design.md#run-completion-fdc-002"
planned-test-refs:
  - "PT-063-01"
  - "PT-063-02"
  - "PT-063-03"
  - "PT-063-04"
dependencies: []
status: "approved"
landed-commit: ""
---

# Ticket: t-063 — Run completion

## Vertical Outcome

A Run ends and the Engine says so. Starting in a Phase with no ordinary outgoing edge, or stepping
into one, writes `status = "passed"` in the same atomic State File replacement that records the
position. A passed Run refuses `step` and `hold` by name without touching state. Confirmed
abandonment appends one durable `- Abandoned:` event naming the addressed Run before any active
state retires; `abandoned` never appears in a State File. Guard refusal remains byte-identical and
non-terminal; nothing writes `failed`.

## Worktree Scope

- **Included:** `src/graph.rs` ordinary-outgoing predicate, `src/scheduler.rs` start/step status
  writes plus terminal-Run step refusal, `src/blocked.rs` terminal-Run hold refusal,
  `src/abandon.rs` event literal gains the run id, `test/qa/tests/t063_run_completion.rs`,
  `.arca-private/t-063/` hidden lanes, `.arca/runbook-spec.md` + `.arca/runbook-authoring.md`
  terminality notes.
- **Excluded:** any `failed` trigger or command, `Status` enum changes, `initialize_state` changes,
  composition/join, cycle runbook, signer identity.
- **Expected merge/output:** `passed` is written only by ordinary motion (start-in-terminal,
  arrival-at-terminal) in one atomic State File replacement; the abandoned event names the Run and
  precedes retirement; refusal never mutates.
- **Superseded-test fate:** any existing public or hidden assertion that pins `status = "planned"`
  in a state that is now terminal-passed gets the narrow lifecycle correction only (expected in
  t-062's terminal-branch fixtures and possibly t-058..t-062 hidden crates); all other oracles stay
  intact. `t051_abandon.rs` pins no event literal and should pass unchanged; if it counts
  event-line fields, it updates for the run id.

## P4 Apparent Test Plan

| Apparent Test ID | Goal Contract Ref | Fixture Ref | Executable Target | Oracle |
|---|---|---|---|---|
| `PT-063-01` | `../goal/test-list.md (FDCV-011)` | one-Phase runbook with no transitions | `test/qa/tests/t063_run_completion.rs::terminal_initial_phase_is_passed_on_start` | `rtm start` leaves `state.toml` at the initial Phase with `status = "passed"`; a follow-up `step` refuses naming the terminal Run and state bytes stay identical. |
| `PT-063-02` | `../goal/test-list.md (FDCV-012)` | straight two-Phase runbook and a branch runbook whose one input leads to a terminal Phase | `test/qa/tests/t063_run_completion.rs::step_into_terminal_phase_writes_passed` | The advance into the last Phase writes Phase and `passed` in one replacement; a branch arrival archives its verdict before the passed state; a further step refuses by name. |
| `PT-063-03` | `../goal/test-list.md (FDCV-013)` | started Run, real CLI `abandon` with its confirmation phrase (t-051 pattern) | `test/qa/tests/t063_run_completion.rs::abandonment_records_event_before_state_retirement` | Exactly one new `- Abandoned:` line naming the run id, appended with the prior log bytes preserved; `state.toml` absent afterward; no surviving file contains `status = "abandoned"`. |
| `PT-063-04` | `../goal/test-list.md (FDCV-014)` | failing readiness guard (t-062's guarded runbook pattern) plus every operational verb | `test/qa/tests/t063_run_completion.rs::guard_refusal_is_non_terminal_and_failed_is_never_written` | Refusal leaves the whole Run directory byte-identical; across start/step/refusal/abandon no `state.toml` ever reads `status = "failed"`. |

## P5 Hidden Test Public Coverage Manifest

| Lane | Assessment | Rationale | Hidden IDs |
|---|---|---|---|
| `Regression` | `covered` | Pre-existing straight/branch fixtures must stay green; statuses stay `planned` until a terminal Phase. | `HT-063-01` |
| `Input/Routing` | `covered` | A branching initial Phase is not terminal; only the labelled edge into a terminal Phase writes `passed`; illegal input refuses without status change. | `HT-063-02` |
| `Lifecycle/Model` | `covered` | Passed Runs refuse `step` and `hold`; a blocked-route-only Phase counts terminal. | `HT-063-03` |
| `Durability/Recovery` | `covered` | The event append precedes retirement; an obstructed append leaves state intact and a retry appends exactly one event. | `HT-063-04` |
| `Output/Filesystem` | `covered` | Terminal start/arrival leave only expected Run files with `passed`; abandonment retires `state.toml` while the run dir, `spawn-ledger`, and verdict evidence remain. | `HT-063-05` |
| `Cross-Feature` | `covered` | Sibling Runs, failing guards, goal-freeze evidence, and verdict consumption stay orthogonal to completion. | `HT-063-06` |

| Hidden ID | Goal Contract Ref | Derivation | Category | Oracle | Owner |
|---|---|---|---|---|---|
| `HT-063-01` | `../goal/spec.md (FDC-002)` | Run pre-existing straight/branch fixtures through non-terminal motion. | `Regression` | Statuses stay `planned` until a terminal Phase; only terminal-arrival assertions change. | `t-063 worktree` |
| `HT-063-02` | `../goal/spec.md (FDC-002)` | Start a branching initial Phase, deliver legal and illegal inputs toward a terminal Phase. | `Input/Routing` | Branching start is `planned`; the labelled edge into a terminal Phase writes `passed`; illegal input refuses with no status change. | `t-063 worktree` |
| `HT-063-03` | `../goal/spec.md (FDC-002)` | Drive a Run to `passed`, then attempt `step` and `hold`; arrive at a blocked-route-only Phase. | `Lifecycle/Model` | Passed Run refuses `step` and `hold` by name; a Phase whose only outgoing edge is a blocked route counts terminal (arrival writes `passed`). | `t-063 worktree` |
| `HT-063-04` | `../goal/spec.md (FDC-002)` | Obstruct `.arca/log.md` (read-only) so the abandonment append fails; restore and retry. | `Durability/Recovery` | Failed append leaves state present and log unchanged; retry appends exactly one event, no duplicate. | `t-063 worktree` |
| `HT-063-05` | `../goal/spec.md (FDC-002)` | Enumerate the Run directory after terminal start, terminal arrival, and abandonment. | `Output/Filesystem` | Only expected files with `status = "passed"`; after abandon, `state.toml` absent while the run dir, `spawn-ledger`, and verdict evidence remain. | `t-063 worktree` |
| `HT-063-06` | `../goal/spec.md (FDC-002)` | Combine a passing Run with a sibling Run, a failing guard before terminal arrival, and a verdict-consumed branch into a terminal Phase. | `Cross-Feature` | Sibling bytes untouched; failing guard refuses and stays `planned`; verdict archives before the passed state; goal-freeze ordering unchanged. | `t-063 worktree` |

## Merge Gate

- Dependencies: none.
- Ticket tests: `PT-063-01`–`PT-063-04` pass after expected reds.
- Full tests: `cargo test --workspace` plus all six private crates (`.arca-private/t-058`..`t-063`) pass.
- Formatting: `cargo fmt --all -- --check` passes.
- Lints: `cargo clippy --workspace --all-targets --all-features -- -D warnings` passes.
- Links: `python tools/check_links.py` passes.
- Review: `passed` written only by ordinary motion, event append precedes retirement, refusal never
  mutates, no `failed` write path, and `res-098` carries exact evidence before closure.
