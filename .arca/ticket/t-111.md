---
ticket-id: "t-111"
residual-ids:
  - "res-167"
# Deliberate-damage evidence lives solely in the owning gap record.
behavior-refs:
  - "TCE-001 (../goal/spec.md#integrated-turn-close-expiry-requirements)"
  - "THK-002 (../goal/spec.md#integrated-turn-housekeeping-requirements)"
  - "THK-003 (../goal/spec.md#integrated-turn-housekeeping-requirements)"
  - "THK-004 (../goal/spec.md#integrated-turn-housekeeping-requirements)"
  - "LNR-001 (../goal/spec.md#integrated-landed-lane-runnability-requirements)"
  - "LNR-002 (../goal/spec.md#integrated-landed-lane-runnability-requirements)"
design-refs:
  - "Declared final verification (../goal/design.md#turn-close-verification-delegates-to-the-declared-sweep-adr-0021)"
planned-test-refs:
  - "TCEV-001"
  - "TCEV-002"
  - "TCEV-003"
focused-tests:
  - "test/qa/tests/t111_turn_close_expiry.rs"
hidden-lanes:
  - "HT-111-01"
  - "HT-111-02"
  - "HT-111-03"
  - "HT-111-04"
  - "HT-111-05"
  - "HT-111-06"
quality-commands:
  - "cargo test --workspace"
  - "cargo fmt --check"
  - "cargo clippy --workspace --all-targets"
dependencies: []
status: "approved"
landed-commit: ""
blocker-ref: ""
---

# Ticket: t-111 - Close turns with fresh expiry-aware verification

## Vertical Outcome

A close with passing and explicitly expired crates succeeds without manual
reinterpretation; a current failure or incomplete roster still refuses by
name. The generic turn tool executes declared policy rather than parsing
markers. Final verification can be retried after cleanup without repeating
any earlier mutation, and the old per-lane default still works.

## Worktree Scope

- **Included:** `tools/turn.ps1`; `.ratmac/turn.toml`; the new focused test
  `test/qa/tests/t111_turn_close_expiry.rs`; six lanes in `test-hidden/t-111/`;
  `.ratmac/lanes.toml` adds this new crate under RLR-004; required ticket,
  gap, receipt, snapshot, and landing records.
- **Excluded:** `src/`, the Machine Class `.ratmac/ratmac.toml`, changes to the
  sweep's parser or verdict rules, marker creation/removal/renewal, migration
  of existing per-lane fixture declarations, new dependencies or a journal.
- **Mechanism:** optional `lanes-rerun-scope = "per-lane" | "root-once"`,
  defaulting to the existing per-lane behavior. The repository selects
  root-once and declares `python tools/sweep_lanes.py sweep --report
  target/turn-close-lanes.md && python tools/sweep_lanes.py check --report
  target/turn-close-lanes.md`. The ignored report leaves the tracked
  cycle-close report untouched during routine ticket closure.
- **Retry proof:** no item branch, worktree registration, or worktree folder;
  item stamp equals the current trunk short tip; supplied landing line is
  present; no unrelated tracked dirt. Only then may a branchless close retry
  the whole final verification. No per-crate exactly-once promise is made.
- **Self-upgrade:** the first close loads the old primary script/declaration
  before merge and may finish cleanup then stop at the known expired crate.
  Retry the identical close with the newly merged tool; it must prove the
  completed steps and run only the new verifier. Do not bypass closure by
  manually declaring that expected failure successful.

## P4 Apparent Test Plan

| Apparent Test ID | Goal Contract Ref | Fixture Ref | Executable Target | Oracle |
| :--- | :--- | :--- | :--- | :--- |
| TCEV-001 | [TCE-001](../goal/spec.md#integrated-turn-close-expiry-requirements) | Real throwaway Git turns using the shipped declaration's verifier and real sweep tool, with tiny independent crates. | `test/qa/tests/t111_turn_close_expiry.rs::configured_close_uses_fresh_sweep_and_preserves_expiry` | Pass plus valid expiry succeeds and reports separate counts; red, malformed marker, missing and stray each refuse by name. A stale passing report cannot hide failure. Marker bytes are preserved. |
| TCEV-002 | [TCE-001](../goal/spec.md#integrated-turn-close-expiry-requirements) | A real close whose generic verifier fails once, then is released; twins remove the log proof or change the stamp. | `test/qa/tests/t111_turn_close_expiry.rs::failed_final_verification_resumes_without_replaying_close` | First failure follows cleanup. Identical close then retries only verification, leaving tip, stamp, log count and carried bytes unchanged. Missing or stale evidence refuses without mutation. |
| TCEV-003 | [TCE-001](../goal/spec.md#integrated-turn-close-expiry-requirements) | Generic scope fixtures using the existing turn helper, custom commands, and snapshots. | `test/qa/tests/t111_turn_close_expiry.rs::declared_scope_is_generic_compatible_and_checked_before_writes` | Root command runs once at the primary root; absent/explicit per-lane scope runs in each lane. Bad scope refuses before writes. Status describes scope and preserves the snapshot. |

Review these tests before implementation: refusal must come from the intended
condition, not a broken fixture or unrelated build failure. Real-repository
checks inspect shipped declarations, but every nested sweep uses independent
fixture crates: never sweep a roster that contains the calling test itself.

## P5 Hidden Test Public Coverage Manifest

| Lane | Assessment | Rationale | Hidden IDs |
| :--- | :--- | :--- | :--- |
| `Regression` | `covered` | Existing custom per-lane reruns and ordered copy-back remain intact; valid expired failures no longer block root verification. | HT-111-01 |
| `Input/Routing` | `covered` | Invalid scope types/values and malformed marker bytes cannot become permission to skip a failure. | HT-111-02 |
| `Lifecycle/Model` | `covered` | Root verification occurs after cleanup; an unexpired failure stays a failure and retry does not repeat earlier mutations. | HT-111-03 |
| `Durability/Recovery` | `covered` | Branchless final-only retry requires durable landing evidence; absent log, stale stamp, or surviving worktree refuses. | HT-111-04 |
| `Output/Filesystem` | `covered` | Verification preserves marker bytes, reports separate outcomes, writes its ignored report only, and status remains read-only. | HT-111-05 |
| `Cross-Feature` | `covered` | The shipped declaration composes a fresh sweep and check, rejecting a stale pass and a newly reported stray. | HT-111-06 |

| Hidden ID | Goal Contract Ref | Derivation | Category | Oracle | Owner |
| :--- | :--- | :--- | :--- | :--- | :--- |
| HT-111-01 | [TCE-001](../goal/spec.md#integrated-turn-close-expiry-requirements) | Compatibility and explicit expiry are both required outcomes. | Regression | Default commands still run per lane; a root verifier accepts a valid marked refusal without dropping its report row. | t-111 worktree |
| HT-111-02 | [TCE-001](../goal/spec.md#integrated-turn-close-expiry-requirements) | A marker is validated content, not a filename; scope selects an execution location. | Input/Routing | Empty, unknown, or list scope refuses before mutation; malformed TOML and blank-reason markers refuse by path. | t-111 worktree |
| HT-111-03 | [TCE-001](../goal/spec.md#integrated-turn-close-expiry-requirements) | Final confirmation follows the established close order. | Lifecycle/Model | Generic root verifier observes branch/worktree removed, runs once, and a nonzero exit remains refusal with the exact retry command. | t-111 worktree |
| HT-111-04 | [TCE-001](../goal/spec.md#integrated-turn-close-expiry-requirements) | Earlier completed mutations must not be replayed. | Durability/Recovery | Retry after final failure succeeds with unchanged landed state; invalid evidence and a surviving worktree cannot enter final-only retry. | t-111 worktree |
| HT-111-05 | [TCE-001](../goal/spec.md#integrated-turn-close-expiry-requirements) | Expiry remains an explicit recorded fact and dry-run is read-only. | Output/Filesystem | Markers unchanged, report names pass and expiry separately, tracked report untouched, status snapshot identical. | t-111 worktree |
| HT-111-06 | [TCE-001](../goal/spec.md#integrated-turn-close-expiry-requirements) | Freshness and complete roster compose with existing sweep policy. | Cross-Feature | Shipped sweep-plus-check refuses live red despite an old pass report and refuses a stray even when all rostered crates pass. | t-111 worktree |

## Merge Gate

- **Dependencies:** none; the existing lifecycle and sweep are landed.
- **Focused checks:** all three planned checks and the existing turn/sweep
  regression suites pass from the ticket worktree.
- **History-backed fixture:** the existing real landed expired crates remain
  unchanged, and the new root verification delegates their interpretation
  to the shipped sweep. New nested tests use independent fixture rosters.
- **Hidden lanes:** HT-111-01 through HT-111-06 pass; the complete landed-lane
  sweep reads pass or explicitly expired, with the existing markers checked
  in verification mode. The ticket's crate joins the declared roster.
- **Quality:** workspace tests, formatting, static checks and documentation
  links pass with no concurrent workspace writes during the full tests.
- **Safety:** tracked-code deliberate faults are caught from the safety
  checkpoint, restored from that checkpoint and verified. Evidence lives
  only in [the owning gap record](../residual/res-167.md).
- **Landing:** merge and verify copy-back before removal; resume final
  confirmation using the landed tool if its self-upgrade reaches the old
  verifier. Only the following stamp landing marks the gap satisfied and
  derives its resolving commit citation.
