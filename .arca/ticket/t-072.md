---
ticket-id: "t-072"
residual-ids:
  - "res-108"
  - "res-109"
  - "res-093"
  - "res-095"
behavior-refs:
  - "[ENS-003](../goal/spec.md#integrated-engine-namespace-requirements)"
  - "[ENS-004](../goal/spec.md#integrated-engine-namespace-requirements)"
  - "[FDC-004](../goal/spec.md#integrated-run-residency-requirements)"
  - "[FDC-006](../goal/spec.md#integrated-run-residency-requirements)"
design-refs:
  - "[ADR-0011](../goal/design.md#engine-namespace-and-repository-scoped-runtime-adr-0011)"
planned-test-refs:
  - "ENSV-004"
  - "ENSV-005"
dependencies:
  - "t-071"
status: "approved"
landed-commit: ""
blocker-ref: ""
---

# Ticket: t-072 — One repository-wide id namespace with a durable mint record

## Vertical Outcome

All checkouts and linked worktrees of one repository see one roster and one Run identifier
namespace. Minting reserves the next ordinal in `.ratmac/mint.toml` while holding the root
lock, so removing a Run directory cannot make that identifier available again; uncapped,
explicitly addressed Runs retain the FDC-004 and FDC-006 contracts.

## Worktree Scope

- **Included:** repository-root roster access introduced by `t-071`; durable mint-record
  read, validation, advancement, and atomic persistence; use of that record for start,
  spawn, and respawn identity allocation; cross-worktree Run addressing; replacement QA
  coverage in `test/qa/tests/t072_repository_namespace.rs`; and six private lane checks.
- **Excluded:** the detailed root/per-Run lock implementation belongs to `t-073`; child
  workspace binding belongs to `t-074`; Engine logging, roots-table validation, legacy
  residue refusal, and root reporting belong to their respective later tickets. No Run
  identifier is renumbered or recycled as a migration shortcut.
- **Expected merge/output:** a Run made through any checkout is a direct member of the
  primary root's roster from every other checkout, and the high-water mark in `mint.toml`
  survives deletion of Run directories and process restarts.
- **Superseded-test fate:** roster-derived next-id assertions that rely on retired
  directories staying present are replaced with mint-record assertions; canonical
  `--run <id>` validation, uncapped starts, and independent sibling motion remain
  regression requirements.

## P4 Apparent Test Plan

Public mapping from explicit goal behavior to an executable test and oracle.

| Apparent Test ID | Goal Contract Ref | Fixture Ref | Executable Target | Oracle |
| ---------------- | ----------------- | ----------- | ----------------- | ------ |
| `ENSV-004` | [`ENSV-004`](../goal/test-list.md#integrated-engine-namespace-verification) | A real Git primary checkout plus linked worktree, with a declared child spawn and join path. | `test/qa/tests/t072_repository_namespace.rs::run_created_in_primary_is_addressable_from_linked_worktree` | Spawn in the primary checkout, step the child from the linked worktree, and join from the primary; all three operations identify one Run directory and one identifier in one roster, with no duplicate allocation. |
| `ENSV-005` | [`ENSV-005`](../goal/test-list.md#integrated-engine-namespace-verification) | A primary-root fixture that starts a Run, records its identifier, removes only that Run directory, and starts again. | `test/qa/tests/t072_repository_namespace.rs::mint_record_never_reissues_a_deleted_run_id` | The second mint is a larger canonical identifier, and `.ratmac/mint.toml` records that advancement rather than deriving identity from the remaining directories. |

## P5 Hidden Test Public Coverage Manifest

Public coverage manifest entries. Category must map to one of the six adversarial lanes.

| Lane | Assessment | Rationale | Hidden IDs |
| ---- | ---------- | --------- | ---------- |
| `Regression` | `covered` | Uncapped starts and canonical Run addressing must remain valid after identity stops deriving solely from directories. | `HT-072-01` |
| `Input/Routing` | `covered` | A caller in another checkout and hostile or unknown `--run` values must be routed through the shared roster, not a local path join. | `HT-072-02` |
| `Lifecycle/Model` | `covered` | Allocation, retirement, and a later allocation must preserve monotonic Run identity without changing sibling lifecycle state. | `HT-072-03` |
| `Durability/Recovery` | `covered` | The durable high-water mark must survive a restart and a removed Run directory without a torn or reset mint record. | `HT-072-04` |
| `Output/Filesystem` | `covered` | The mint record and shared roster have a constrained primary-root location and must not create checkout-local mirrors. | `HT-072-05` |
| `Cross-Feature` | `covered` | Spawn, cross-worktree step, and join combine repository-wide addressing with the existing child ledger and motion flows. | `HT-072-06` |

| Hidden ID | Goal Contract Ref | Derivation | Category | Oracle | Owner |
| --------- | ----------------- | ---------- | -------- | ------ | ----- |
| `HT-072-01` | [`FDC-004`](../goal/spec.md#integrated-run-residency-requirements), [`FDC-006`](../goal/spec.md#integrated-run-residency-requirements) | Start several Runs and address each by its minted spelling after the mint-record change. | `Regression` | Every started Run remains independently addressable and no active-Run cap reappears. | `t-072 worktree` |
| `HT-072-02` | [`ENS-003`](../goal/spec.md#integrated-engine-namespace-requirements) | Submit valid cross-worktree, unknown, non-canonical, and traversal-shaped Run identifiers. | `Input/Routing` | The valid shared identifier opens its single roster member; every invalid value refuses before an out-of-roster read or write. | `t-072 worktree` |
| `HT-072-03` | [`ENS-004`](../goal/spec.md#integrated-engine-namespace-requirements) | Create sibling Runs, retire one, then allocate another while stepping an untouched sibling. | `Lifecycle/Model` | The new identifier exceeds every previously minted identifier, and the sibling State File is byte-identical. | `t-072 worktree` |
| `HT-072-04` | [`ENS-004`](../goal/spec.md#integrated-engine-namespace-requirements) | Reopen the fixture after mint persistence and after removal of the newest Run directory. | `Durability/Recovery` | The persisted high-water mark parses cleanly and the next allocation cannot reissue the removed identifier. | `t-072 worktree` |
| `HT-072-05` | [`ENS-003`](../goal/spec.md#integrated-engine-namespace-requirements), [`ENS-004`](../goal/spec.md#integrated-engine-namespace-requirements) | Compare primary and linked checkout trees before and after allocation. | `Output/Filesystem` | Only the resolved primary `.ratmac/` gains the Run and `mint.toml`; the linked checkout receives no runtime duplicate. | `t-072 worktree` |
| `HT-072-06` | [`ENS-003`](../goal/spec.md#integrated-engine-namespace-requirements) | Spawn a child in the primary checkout, move it in a linked worktree, and join it from the primary. | `Cross-Feature` | The child id, ledger reference, Run state, and join expected set all name one shared child without a second roster entry. | `t-072 worktree` |

## Merge Gate

- **Dependencies:** `t-071` is green and merged.
- **Ticket tests:** `ENSV-004` and `ENSV-005` pass after their expected red evidence.
- **Hidden manifest:** `HT-072-01` through `HT-072-06` cover all six required lanes.
- **Quality and closure:** affected run-residency and multi-Run regressions plus workspace quality gates pass; `landed-commit` remains empty until the green ticket landing is merged.
