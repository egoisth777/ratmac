---
ticket-id: t-031
residual-ids:
  - "res-032"
behavior-refs:
  - "../../current/spec.md (RAT-002)"
design-refs:
  - "../../current/design.md#rebrand-compatibility-decisions-rat-007"
planned-test-refs:
  - "PT-031-01"
dependencies: []
status: "approved"
---

# Ticket: t-031 — Cut over ratmac package and rtm binary identity

## Vertical Outcome

A clean checkout builds the project as package/library `ratmac` with executable `rtm`, and no shipped package, crate, dependency, import, or binary target requires the old identity.

## Worktree Scope

- **Included:** root and QA Cargo manifests, Rust library-facing identity, binary source rename `src/bin/schd.rs` → `src/bin/rtm.rs`, imports and package metadata, and focused metadata/build proof.
- **Excluded:** runtime command wording/routes (t-032), QA-wide fixture migration (t-033), generated lock refresh (t-035), compatibility lock behavior (t-036), and final audit (t-037).
- **Expected merge/output:** canonical package/library/binary metadata and a compiling `rtm` target, with no legacy alias or duplicate binary.

## P4 Apparent Test Plan

| Apparent Test ID | Goal Contract Ref | Fixture Ref | Executable Target | Oracle |
|---|---|---|---|---|
| `PT-031-01` | `../../current/spec.md (RAT-002)` | `test/qa/fixtures/metadata/` | `test/qa/tests/t031_rat002.rs::canonical_package_and_binary_metadata` | Cargo metadata reports `ratmac` and `rtm`; source target is `src/bin/rtm.rs`; old package/bin names are absent from active manifests and the target builds. |

## P5 Hidden Test Public Coverage Manifest

| Lane | Assessment | Rationale | Hidden IDs |
|---|---|---|---|
| `Regression` | covered | Existing scheduler library must still compile under the renamed package. | `HT-031-01` |
| `Input/Routing` | not-applicable | Command routing is owned by t-032. | none |
| `Lifecycle/Model` | not-applicable | No state-machine semantics change. | none |
| `Durability/Recovery` | not-applicable | Persistence is outside metadata identity. | none |
| `Output/Filesystem` | covered | Binary source/manifest shape is an observable deliverable. | `HT-031-02` |
| `Cross-Feature` | not-applicable | No feature interaction is introduced. | none |

| Hidden ID | Goal Contract Ref | Derivation | Category | Oracle | Owner |
|---|---|---|---|---|---|
| `HT-031-01` | `../../current/spec.md (RAT-002)` | Build all targets and inspect metadata/dependency names. | `Regression` | Build succeeds and all active package/library/dependency identities are `ratmac`. | `t-031 worktree` |
| `HT-031-02` | `../../current/spec.md (RAT-002)` | Inspect binary target enumeration and source paths. | `Output/Filesystem` | Exactly one canonical `rtm` binary target exists; no `schd` target/source remains. | `t-031 worktree` |

## Merge Gate

- Dependencies: none; base checkout is present.
- Ticket tests: `PT-031-01` passes.
- Full tests: `cargo test --workspace` passes.
- Formatting: `cargo fmt --all -- --check` passes.
- Lints: `cargo clippy --workspace --all-targets --all-features -- -D warnings` passes.
