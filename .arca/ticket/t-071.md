---
ticket-id: "t-071"
residual-ids:
  - "res-106"
  - "res-107"
  - "res-024"
  - "res-025"
behavior-refs:
  - "[ENS-001](../goal/spec.md#integrated-engine-namespace-requirements)"
  - "[ENS-002](../goal/spec.md#integrated-engine-namespace-requirements)"
  - "[R-024](../goal/spec.md#ratmac-specification)"
  - "[R-025](../goal/spec.md#ratmac-specification)"
design-refs:
  - "[ADR-0011](../goal/design.md#engine-namespace-and-repository-scoped-runtime-adr-0011)"
planned-test-refs:
  - "ENSV-001"
  - "ENSV-002"
  - "ENSV-003"
  - "ENSV-012"
dependencies: []
status: "approved"
landed-commit: ""
blocker-ref: ""
---

# Ticket: t-071 — Engine root and resolution

## Vertical Outcome

The Engine has one runtime root, `.ratmac/`, rather than an Engine-owned corner of
`.arca/`. A linked Git worktree shares the primary checkout's runtime root but reads its
own tracked Machine Class; a checkout without Git uses its own `.ratmac/`. State retains
its Scheduler-owned fields and Run-scoped location beneath that resolved root, and the
runtime/tracked-file boundary prevents live Run state from entering ticket branches.

## Worktree Scope

- **Included:** a single Engine-root resolver and its use by Scheduler, State, pin,
  ledger, command-line, and doctor paths; moving Engine-owned Machine Class, Run state,
  runtime evidence, locks, and transition-log addresses under `.ratmac/`; the Git
  worktree and no-Git resolution cases; `.gitignore` tracking rules; replacement QA
  coverage in `test/qa/tests/t071_engine_root.rs`; and six private lane checks.
- **Excluded:** durable mint semantics and repository-wide Run identity (`t-072`), the
  lock split (`t-073`), child workspace binding (`t-074`), the Engine log behavior
  change (`t-075`), runbook roots (`t-076`), pre-split refusal (`t-077`), and root
  reporting (`t-078`). This ticket does not auto-migrate legacy artifacts.
- **Expected merge/output:** start, step, and status use only the resolved `.ratmac/`
  Engine root; a linked invocation shares runtime while its edited Machine Class still
  participates in the existing pin-drift refusal; and Git ignores runtime while tracking
  the Machine Class and run-scoped receipts.
- **Superseded-test fate:** the `.arca/` layout expectations in the reopened R-024 and
  R-025 coverage are replaced with `.ratmac/` root assertions while their State File
  shape, Scheduler-only writer, and no-flat-state assertions remain required.

## P4 Apparent Test Plan

Public mapping from explicit goal behavior to an executable test and oracle.

| Apparent Test ID | Goal Contract Ref | Fixture Ref | Executable Target | Oracle |
| ---------------- | ----------------- | ----------- | ----------------- | ------ |
| `ENSV-001` | [`ENSV-001`](../goal/test-list.md#integrated-engine-namespace-verification) | A fresh two-phase project whose tracked Machine Class is `.ratmac/ratmac.toml`, with before/after snapshots of `.arca/` and `.ratmac/`. | `test/qa/tests/t071_engine_root.rs::engine_root_holds_runtime_and_never_writes_arca` | Start, step, and status succeed; all Engine runtime artifacts occur beneath `.ratmac/` (including the Run subtree, mint record, locks, log, and run-scoped receipts), and the `.arca/` snapshot is byte-identical. |
| `ENSV-002` | [`ENSV-002`](../goal/test-list.md#integrated-engine-namespace-verification) | A real Git primary checkout with a linked worktree whose tracked `.ratmac/ratmac.toml` can be edited independently after a Run starts. | `test/qa/tests/t071_engine_root.rs::linked_worktree_shares_primary_runtime_but_reads_its_own_class` | The linked invocation observes the primary checkout's same Run roster and runtime root; changing its Machine Class causes the existing pin-drift refusal rather than selecting a different class or silently succeeding. |
| `ENSV-003` | [`ENSV-003`](../goal/test-list.md#integrated-engine-namespace-verification) | A valid temporary checkout deliberately containing no `.git` directory. | `test/qa/tests/t071_engine_root.rs::non_git_checkout_uses_its_current_root` | Engine operations resolve the fixture's own `.ratmac/`, create no sibling or guessed root, and do not fail merely because Git metadata is absent. |
| `ENSV-012` | [`ENSV-012`](../goal/test-list.md#integrated-engine-namespace-verification) | A Git fixture containing runtime files, a Machine Class, and receipts under two Run identifiers. | `test/qa/tests/t071_engine_root.rs::git_tracks_class_and_receipts_but_ignores_runtime` | Git treats `runs/`, `mint.toml`, `locks/`, and `log.md` as ignored while `ratmac.toml` and `.ratmac/evidence/<run-id>/` are tracked; a ticket-branch index contains no live Run state. |

## P5 Hidden Test Public Coverage Manifest

Public coverage manifest entries. Category must map to one of the six adversarial lanes.

| Lane | Assessment | Rationale | Hidden IDs |
| ---- | ---------- | --------- | ---------- |
| `Regression` | `covered` | Moving the root must preserve existing addressed-Run State File fields and normal start/step behavior. | `HT-071-01` |
| `Input/Routing` | `covered` | Git, linked-worktree, and no-Git invocation contexts are distinct resolution inputs and must not select an unintended root. | `HT-071-02` |
| `Lifecycle/Model` | `covered` | A Run created and advanced through the relocated State File must retain its legal Phase and Status progression. | `HT-071-03` |
| `Durability/Recovery` | `covered` | Reopening a Run after a process restart must rediscover the same resolved root and durable state without recreating it elsewhere. | `HT-071-04` |
| `Output/Filesystem` | `covered` | The root move and tracking boundary are observable filesystem contracts, including a strict no-Engine-write rule for `.arca/`. | `HT-071-05` |
| `Cross-Feature` | `covered` | Worktree sharing must compose with the hash-only Machine Class pin so a local class edit refuses rather than becoming invisible. | `HT-071-06` |

| Hidden ID | Goal Contract Ref | Derivation | Category | Oracle | Owner |
| --------- | ----------------- | ---------- | -------- | ------ | ----- |
| `HT-071-01` | [`ENS-001`](../goal/spec.md#integrated-engine-namespace-requirements), [`R-025`](../goal/spec.md#ratmac-specification) | Re-run existing State File shape and Scheduler-only writer scenarios after resolving the Engine root. | `Regression` | The exact required State fields and legal transition behavior remain intact beneath `.ratmac/runs/<id>/state.toml`. | `t-071 worktree` |
| `HT-071-02` | [`ENS-002`](../goal/spec.md#integrated-engine-namespace-requirements) | Invoke the same commands from a primary checkout, a linked worktree, and a directory without Git metadata. | `Input/Routing` | Each invocation chooses only its contractually resolved root; no invocation reads or writes a checkout-local substitute in the linked case. | `t-071 worktree` |
| `HT-071-03` | [`R-024`](../goal/spec.md#ratmac-specification), [`R-025`](../goal/spec.md#ratmac-specification) | Start and step a multi-phase Run after relocation, including one guard refusal. | `Lifecycle/Model` | Success advances only the addressed Run, and refusal leaves its Phase and Status unchanged. | `t-071 worktree` |
| `HT-071-04` | [`ENS-001`](../goal/spec.md#integrated-engine-namespace-requirements) | Start a Run, discard the Scheduler process, then reopen and step it from the same fixture. | `Durability/Recovery` | The original `.ratmac/` Run state is reused exactly once; no duplicate root or replacement State File appears. | `t-071 worktree` |
| `HT-071-05` | [`ENS-001`](../goal/spec.md#integrated-engine-namespace-requirements) | Snapshot `.arca/`, inspect the `.ratmac/` tree, and query Git ignore/tracking status after Engine operations. | `Output/Filesystem` | `.arca/` is unchanged by the Engine; runtime is ignored and only the Machine Class plus run-scoped receipts are tracked. | `t-071 worktree` |
| `HT-071-06` | [`ENS-002`](../goal/spec.md#integrated-engine-namespace-requirements) | Edit a linked worktree's Machine Class after the primary has started a Run, then attempt an existing-Run operation. | `Cross-Feature` | Shared runtime remains addressable, but the edited invoking class triggers the FDC-005 pin-drift refusal with no mutation. | `t-071 worktree` |

## Merge Gate

- **Dependencies:** none.
- **Ticket tests:** `ENSV-001`, `ENSV-002`, `ENSV-003`, and `ENSV-012` pass after their expected red evidence.
- **Hidden manifest:** `HT-071-01` through `HT-071-06` cover all six required lanes.
- **Quality and closure:** affected regression targets and the workspace quality gates pass; `landed-commit` remains empty until the green ticket landing is merged.
