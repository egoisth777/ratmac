---
ticket-id: "t-011"
residual-ids:
  - "res-021"
behavior-refs:
  - "../../current/spec.md (R-021)"
design-refs:
  - "../../current/design.md#model-n-runs-allow-1-active-adr-0007"
planned-test-refs:
  - "PT-011-01"
dependencies:
  - "t-010"
status: "approved"
---

# Ticket: t-011 — N-Run data model

## Vertical Outcome
A scheduler run store can represent two or more Run records without a singleton-only format or engine assumption; the separate v1 one-active-run CLI rule remains enforced by t-012.

## Worktree Scope
- **Included:** Run identity/storage model and its serialization needed to represent N Runs; an end-to-end Rust check proving two distinct Run records coexist.
- **Excluded:** v1 active-run admission policy (t-012), run-id CLI arguments (t-013), and unrelated phase/guard behavior.
- **Expected merge/output:** The implementation and `PT-011-01` test merge cleanly into the shared scheduler crate without changing the frozen goal bundle.

## P4 Apparent Test Plan

| Apparent Test ID | Goal Contract Ref | Fixture Ref | Executable Target | Oracle |
| ---------------- | ----------------- | ----------- | ----------------- | ------ |
| `PT-011-01` | `../../current/spec.md (R-021)`; `../../current/design.md#model-n-runs-allow-1-active-adr-0007` | `test/fixtures/t011-two-runs/` | `test/qa/tests/t011_r021.rs::r021_model_represents_multiple_runs` | Creating two distinct Run records succeeds and serialized/run-owned data identifies both without singleton collision. |

## P5 Hidden Test Public Coverage Manifest

| Lane | Assessment | Rationale | Hidden IDs |
| ---- | ---------- | --------- | ---------- |
| `Regression` | `not-applicable` | No prior implementation exists at the frozen revision; PT-011-01 is the direct model contract. | none |
| `Input/Routing` | `not-applicable` | This requirement concerns Run cardinality, not CLI parsing or handler routing. | none |
| `Lifecycle/Model` | `covered` | Cardinality and distinct Run identity are model invariants. | `HT-011-01` |
| `Durability/Recovery` | `not-applicable` | Persistence across crash/restart is outside R-021. | none |
| `Output/Filesystem` | `not-applicable` | The contract does not prescribe a filesystem layout. | none |
| `Cross-Feature` | `not-applicable` | No cross-feature interaction is required to establish N-Run representation. | none |

| Hidden ID | Goal Contract Ref | Derivation | Category | Oracle | Owner |
| --------- | ----------------- | ---------- | -------- | ------ | ----- |
| `HT-011-01` | `../../current/spec.md (R-021)` | Exercise three Run records and verify each remains independently addressable, catching hidden singleton assumptions beyond the apparent two-record case. | `Lifecycle/Model` | All three records remain distinct and retrievable with no overwrite or singleton error. | `t-011 worktree` |

## Merge Gate
Dependency presence: `t-010` is present and green. Ticket test `PT-011-01` passes; full Rust test suite passes; `cargo fmt --check` passes; `cargo clippy --all-targets --all-features -- -D warnings` passes.
