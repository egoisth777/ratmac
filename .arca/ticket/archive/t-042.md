---
ticket-id: t-042
residual-ids:
  - "res-047"
behavior-refs:
  - "../../goal/spec.md (ETB-003)"
design-refs:
  - "../../goal/design.md#engine-trust-boundary-etb-001etb-003"
planned-test-refs:
  - "PT-042-01"
  - "PT-042-02"
dependencies:
  - "t-041"
status: "approved"
landed-commit: "6d966bc"
---

# Ticket: t-042 — Post-integration goal freeze and drift refusal

## Vertical Outcome

Run evidence distinguishes the start baseline revision from the frozen goal revision, the freeze is computed at the intake-completion boundary, gap-analysis-facing output cites the frozen value, and any later change to `.arca/goal/` refuses the next transition naming frozen and observed revisions until the batch closes.

## Worktree Scope

- **Included:** baseline and frozen revision fields; freeze computation at the intake boundary; drift detection at each transition request; refusal output.
- **Excluded:** pin verification (t-041) and gate contracts (t-045+).
- **Expected merge/output:** residual records that cite a freeze describing the requirements they classify.

## P4 Apparent Test Plan

| Apparent Test ID | Goal Contract Ref | Fixture Ref | Executable Target | Oracle |
|---|---|---|---|---|
| `PT-042-01` | `../../goal/spec.md (ETB-003)` | `fixture Run whose intake phase rewrites `.arca/goal/`` | `test/qa/tests/t043_goal_freeze.rs::freeze_is_post_integration` | Evidence records baseline and frozen revisions distinctly, the frozen value equals the post-integration content hash, and gap-analysis output cites the frozen value. |
| `PT-042-02` | `../../goal/spec.md (ETB-003)` | `frozen fixture Run` | `test/qa/tests/t043_goal_freeze.rs::drift_refuses_and_revert_clears` | Editing a `.arca/goal/` file refuses the next transition naming goal drift with both revisions; reverting clears it; Scheduler-owned files are byte-identical across the refusal. |

## P5 Hidden Test Public Coverage Manifest

| Lane | Assessment | Rationale | Hidden IDs |
|---|---|---|---|
| `Regression` | `covered` | Existing state fields and step behavior must be preserved. | `none` |
| `Input/Routing` | `covered` | Added, removed, and renamed goal files must all count as drift. | `HT-042-01` |
| `Lifecycle/Model` | `covered` | The freeze belongs to a phase boundary and must not become a second machine state. | `none` |
| `Durability/Recovery` | `covered` | A crash during freeze must leave a readable state file. | `HT-042-02` |
| `Output/Filesystem` | `covered` | Revision fields are persisted state content. | `none` |
| `Cross-Feature` | `covered` | Drift refusal must compose with pin verification without masking either. | `HT-042-03` |

| Hidden ID | Goal Contract Ref | Derivation | Category | Oracle | Owner |
|---|---|---|---|---|---|
| `HT-042-01` | `../../goal/spec.md (ETB-003)` | Add and delete a file under `.arca/goal/` without editing existing ones. | `Input/Routing` | Both are detected as drift; the refusal names the observed revision. | `t-042 worktree` |
| `HT-042-02` | `../../goal/spec.md (ETB-003)` | Interrupt between freeze computation and state write. | `Durability/Recovery` | State remains parseable and either fully frozen or unchanged. | `t-042 worktree` |
| `HT-042-03` | `../../goal/spec.md (ETB-003)` | Tamper the pinned gate artifact and edit the goal in the same step. | `Cross-Feature` | Both refusals are reported; neither is silently dropped. | `t-042 worktree` |

## Merge Gate

- Dependencies: t-041.
- Ticket tests: `PT-042-01`, `PT-042-02` pass.
- Full tests: `cargo test --workspace` plus the hidden lanes listed above pass.
- Formatting: `cargo fmt --all -- --check` passes.
- Lints: `cargo clippy --workspace --all-targets --all-features -- -D warnings` passes.
- Review: residual records res-047 updated with concrete evidence refs before closure.
