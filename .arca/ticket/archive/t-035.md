---
ticket-id: t-035
residual-ids:
  - "res-036"
behavior-refs:
  - "../../current/spec.md (RAT-006)"
design-refs:
  - "../../current/design.md#rebrand-compatibility-decisions-rat-007"
planned-test-refs:
  - "PT-035-01"
dependencies:
  - "t-031"
status: "approved"
landed-commit: "be7e686"
---

# Ticket: t-035 — Regenerate canonical Cargo metadata

## Vertical Outcome

After canonical manifests are in place, Cargo-owned lock/package metadata is regenerated by Cargo and reports only the `ratmac`/`rtm` identity; ignored build outputs remain excluded from the deliverable.

## Worktree Scope

- **Included:** regenerate `Cargo.lock` and any checked-in package/manifests or generated documentation owned by Cargo/project tooling; verify metadata and clean source deliverable boundaries.
- **Excluded:** hand-editing generated files, production source identity (t-031), QA behavior migration (t-033), lock migration semantics (t-036), and final quality gates (t-037).
- **Expected merge/output:** tool-generated metadata is canonical, reproducible, and free of stale active package/bin records.

## P4 Apparent Test Plan

| Apparent Test ID | Goal Contract Ref | Fixture Ref | Executable Target | Oracle |
|---|---|---|---|---|
| `PT-035-01` | `../../current/spec.md (RAT-006)` | `test/qa/fixtures/metadata/` | `test/qa/tests/t035_rat006.rs::generated_metadata_is_canonical` | Cargo generation and `cargo metadata --no-deps` produce canonical records; `Cargo.lock` has no stale active package/bin identity and no ignored `target/` output is added. |

## P5 Hidden Test Public Coverage Manifest

| Lane | Assessment | Rationale | Hidden IDs |
|---|---|---|---|
| `Regression` | covered | Generated dependency resolution must preserve the existing build. | `HT-035-01` |
| `Input/Routing` | not-applicable | No user input route is changed here. | none |
| `Lifecycle/Model` | not-applicable | No scheduler model behavior changes. | none |
| `Durability/Recovery` | not-applicable | Lock migration behavior is owned by t-036. | none |
| `Output/Filesystem` | covered | Generated files and ignored output boundaries are the contract. | `HT-035-02` |
| `Cross-Feature` | not-applicable | Integration smoke is owned by t-037. | none |

| Hidden ID | Goal Contract Ref | Derivation | Category | Oracle | Owner |
|---|---|---|---|---|---|
| `HT-035-01` | `../../current/spec.md (RAT-006)` | Regenerate metadata from a clean dependency state. | `Regression` | Cargo resolves and builds the canonical package without stale dependency records. | `t-035 worktree` |
| `HT-035-02` | `../../current/spec.md (RAT-006)` | Inspect tracked/generated/ignored boundaries after generation. | `Output/Filesystem` | Only owning-tool outputs are tracked; ignored target artifacts are absent from the ticket deliverable. | `t-035 worktree` |

## Merge Gate

- Dependencies: t-031 is merged.
- Ticket tests: `PT-035-01` passes.
- Full tests: `cargo test --workspace` passes.
- Formatting: `cargo fmt --all -- --check` passes.
- Lints: `cargo clippy --workspace --all-targets --all-features -- -D warnings` passes.
