---
ticket-id: "t-110"
residual-ids:
  - "res-165"
  - "res-166"
# SDC-003 (schema.md, "Deliberate damage and discard safety"): deliberate-damage
# evidence lives solely in these gap records' mutation-kill lists; the ticket
# points and duplicates no evidence bytes.
behavior-refs:
  - "[RLR-001](../schema.md#rlr-001---triage-precedes-the-act)"
  - "[RLR-003](../schema.md#rlr-003---the-post-split-crates-are-ported-or-re-swept)"
  - "[RLR-004](../schema.md#rlr-004---the-roster-follows-the-landings-and-the-close-guard-passes)"
design-refs:
  - "[The triage of 2026-09-17](../issue/i-038-red-lanes-recovered/design.md#the-triage-of-2026-09-17)"
  - "[Landed lanes sweep on demand and expire by in-crate marker (ADR-0020)](../goal/design.md#landed-lanes-sweep-on-demand-and-expire-by-in-crate-marker-adr-0020)"
planned-test-refs:
  - "RLRV-002"
  - "RLRV-003"
  - "RLRV-004"
# CGD-001 (schema.md, "Ticket check tags"): the ticket's declared checks as
# three tag lists of opaque check ids; a checker reads these, never prose.
focused-tests:
  - "test/qa/tests/t110_post_split_ports.rs"
hidden-lanes:
  - "HT-110-01"
  - "HT-110-02"
  - "HT-110-03"
  - "HT-110-04"
  - "HT-110-05"
  - "HT-110-06"
quality-commands:
  - "cargo test --workspace"
  - "cargo fmt --check"
  - "cargo clippy --workspace --all-targets"
dependencies:
  - "t-109"
status: "passed"
landed-commit: "d11deeec50a68305236a3e9447b1d2fded217669"
blocker-ref: ""
---

# Ticket: t-110 - Port the four post-split crates, complete the roster, and pass the close guard

## Vertical Outcome

The four post-split crates `t-083`, `t-085`, `t-092`, `t-093` run green
against today's Engine (retired contract, `RLR-001`): `t-083` and `t-085`
expect the teach line `src/teach.rs::status_next` has printed since `t-103`
and `t-085`'s cutover baseline names the skill verb of `t-104`; `t-092` and
`t-093` cite the goal freeze after the step that mints it. The qa helper
`test/qa/src/baseline.rs::Pair::freeze_paths` stops emitting newline-tipped
entries, so `t-085`'s terminal-path lane finds a listed `.ratmac/log.md`.
`.ratmac/lanes.toml` names `t-108`, `tools/sweep_lanes.py check` refuses a
report whose stray-entries line is non-empty, and the working rules assign
the roster duty to the landing that adds a hidden crate. A fresh sweep is
recorded; with `t-109` landed first, every rostered crate reads `pass` or
`expired`, and the close State's `command_exit` guard exits `0` on this
repository. `t-078` and `t-079` keep their markers and `sweep
--verify-expired` still shows them red when run.

## Triage classes (RLR-001)

| Crate | Class | Named landing | Act |
| :--- | :--- | :--- | :--- |
| `t-083` | retired contract | `t-103` (the `next:` teach line after the State Prompt, AOP-001/002) | port `ht_083_02`'s status expectation |
| `t-085` | retired contract + qa helper defect | `t-103` (teach line and detailed pending-join label), `t-104` (`rtm skill` in the verb list); `Pair::freeze_paths` newline defect | port the exact known additions in the baseline comparison; fix the listing helper; re-derive `ht_085_03/05/06` |
| `t-092` | retired contract | `t-093`/`t-094` freeze citation format; `t-094` edition guard and `t-108` sweep guard | port `ht_092_05/06` to cite the freeze and supply an edition plus a genuinely swept passing fixture at close |
| `t-093` | retired contract | same minting order | port `ht_093_06` |
| `t-095` | none - green today | edition-004 tag repair 2026-08-25 15:22 (-07:00) followed the 10:31Z sweep | re-sweep only |
| `t-096` | none - green today | same repair | re-sweep only |
| `t-100` | none - green today | same repair | re-sweep only |
| `t-078`, `t-079` | expired (marked) | `edition-001` split, marked at `t-108` | markers stay; `--verify-expired` still red |

## Worktree Scope

- **Included:** the four crates `test-hidden/t-083`, `t-085`, `t-092`,
  `t-093`; `test/qa/src/baseline.rs` (`freeze_paths` trims entries and
  `normalize` accepts the exact later join-label, file-guard-label, and skill additions);
  `tools/sweep_lanes.py` (`check` refuses a non-empty stray line);
  `.ratmac/lanes.toml` (`t-108`, then `t-109` and `t-110` as their crates
  land); `.ratmac/evidence/lane-sweep/report.md` (the fresh sweep); the
  roster-duty sentence in `.arca/schema.md` under `RLR-004`; the focused
  test `test/qa/tests/t110_post_split_ports.rs`; six hidden lanes in
  `test-hidden/t-110/`.
- **Excluded:** any change under `src/`; any marker written or removed;
  the pre-split crates (`t-109`); a new guard kind in the runbook.
- **Why now:** with `t-109`'s thirteen ports landed, these four rows and
  the stale report are all that keeps the wired close guard exiting `2`
  (res-165, res-166), and the sprint's close cannot pass until it exits `0`.
- **Closure fixture repair:** the earlier recovery test reads its triage
  table from the active ticket path only. It must resolve the same ticket
  in the archive after the authorized close move, preserving every check
  rather than making the close break its own proof.

## P4 Apparent Test Plan

Public mapping from explicit goal behavior to an executable test and oracle.

| Apparent Test ID | Goal Contract Ref | Fixture Ref | Executable Target | Oracle |
| ---------------- | ----------------- | ----------- | ----------------- | ------ |
| `RLRV-002` | [`RLRV-002`](../issue/i-038-red-lanes-recovered/test-plan.md#verification) | The fresh real sweep report and a throwaway checkout pair whose listing holds `.ratmac/log.md`. | `test/qa/tests/t110_post_split_ports.rs::every_post_split_crate_passes_and_the_baseline_helper_trims_entries` | Each of the seven crates reads pass with its actual landed test count; the helper returns bare paths and preserves unexpected guard arguments in report comparisons. |
| `RLRV-003` | [`RLRV-003`](../issue/i-038-red-lanes-recovered/test-plan.md#verification) | This repository as it stands after a fresh sweep; a fixture lanes root (`t108_lane_sweep.rs::Lanes` pattern) holding one rostered green crate and one crate the roster does not name. | `test/qa/tests/t110_post_split_ports.rs::the_roster_is_complete_and_the_check_refuses_strays` | `.ratmac/lanes.toml` names every `t-NNN` folder under `test-hidden/`; `python tools/sweep_lanes.py check` exits `0` on this repository; on the fixture the report's stray line names the crate and `check` exits `2` naming it. |
| `RLRV-004` | [`RLRV-004`](../issue/i-038-red-lanes-recovered/test-plan.md#verification) | A complete declaration covering the real lanes root, with temporary report output; the two marked crates are also swept alone in plain mode. | `test/qa/tests/t110_post_split_ports.rs::verify_expired_runs_the_marked_twins_and_reports_them_red` | The full verification sweep includes every real crate and exits zero; both marked rows report actual refusing test names, not an unrelated build failure. Plain mode leaves them expired, and marker bytes stay unchanged. An incomplete roster fails before running an incomplete verification. |

## P5 Hidden Test Public Coverage Manifest

### Test review before completion

- The first two focused checks failed on the stale real report and missing
  roster entries. The initial third check passed because it verified only
  two crates; it now first requires a complete real roster and verifies the
  whole repository, as the accepted ask requires. This exposes the actual
  missing roster work rather than inventing a failing expiry condition.
- The four repaired crates contain thirty tests, not twenty-four. No test
  is deleted to fit the original estimate.
- A hidden test cannot sweep the roster containing itself or require its
  own aggregate verdict before that sweep finishes. Hidden regeneration
  and guard checks therefore use genuinely swept independent fixtures;
  focused checks and the final outside sweep prove the real repository.
- Baseline comparison permits only the exact added join and file-guard labels
  and skill entry; altered guard arguments remain visible. Verification
  also checks marker bytes and rejects build failures as expiry proof.

Public coverage manifest entries. Category must map to one of the six adversarial lanes:
1. `Regression`
2. `Input/Routing`
3. `Lifecycle/Model`
4. `Durability/Recovery`
5. `Output/Filesystem`
6. `Cross-Feature`

| Lane | Assessment | Rationale | Hidden IDs |
| ---- | ---------- | --------- | ---------- |
| `Regression` | `covered` | The four ported crates keep all thirty landed test names: six, six, eleven, and seven respectively. | `HT-110-01` |
| `Input/Routing` | `covered` | `check` must refuse by the stray line alone: a report whose rows all read `pass` but whose stray line names a crate exits `2` naming it. | `HT-110-02` |
| `Lifecycle/Model` | `covered` | A marked crate stays `expired` across both sweep modes; only an explicit `unmark` changes its verdict. | `HT-110-03` |
| `Durability/Recovery` | `covered` | A report over passing, failing, expired, and missing fixture crates is regenerable: deleting it and re-sweeping preserves every verdict row. Real repository regeneration is checked outside the sweep's own hidden crate to avoid recursion. | `HT-110-04` |
| `Output/Filesystem` | `covered` | The landed diff touches nothing under `src/`, and the roster lists every `t-NNN` folder under `test-hidden/` in id order with no duplicates. | `HT-110-05` |
| `Cross-Feature` | `covered` | The real runbook's `close` State carries exactly one `command_exit` guard running `tools/sweep_lanes.py check`; its command passes against a genuinely swept fixture report. The separate focused check proves the real repository verdict after the full sweep finishes. | `HT-110-06` |

| Hidden ID | Goal Contract Ref | Derivation | Category | Oracle | Owner |
| --------- | ----------------- | ---------- | -------- | ------ | ----- |
| `HT-110-01` | [`RLR-003`](../schema.md#rlr-003---the-post-split-crates-are-ported-or-re-swept) | Compare each ported crate's `#[test]` names to the frozen list. | `Regression` | Exactly the same thirty names: t-083 six, t-085 six, t-092 eleven, t-093 seven. | `t-110 worktree` |
| `HT-110-02` | [`RLR-004`](../schema.md#rlr-004---the-roster-follows-the-landings-and-the-close-guard-passes) | Hand-write an all-pass report with a stray line; run `check`. | `Input/Routing` | Exit `2`; stderr names the stray crate. | `t-110 worktree` |
| `HT-110-03` | [`RLR-004`](../schema.md#rlr-004---the-roster-follows-the-landings-and-the-close-guard-passes) | Sweep a fixture holding a marked refusing crate in both modes. | `Lifecycle/Model` | `expired` both ways; `unmark` then reads `red`. | `t-110 worktree` |
| `HT-110-04` | [`RLR-003`](../schema.md#rlr-003---the-post-split-crates-are-ported-or-re-swept) | Sweep independent passing, failing, expired, and missing fixture crates twice with the report removed between. | `Durability/Recovery` | Row-for-row equal verdicts; no sweep invokes itself. | `t-110 worktree` |
| `HT-110-05` | [`RLR-004`](../schema.md#rlr-004---the-roster-follows-the-landings-and-the-close-guard-passes) | Read the roster and list the folder. | `Output/Filesystem` | Sorted, duplicate-free, equal sets; no `src/` change in the landed diff. | `t-110 worktree` |
| `HT-110-06` | [`RLR-004`](../schema.md#rlr-004---the-roster-follows-the-landings-and-the-close-guard-passes) | Parse `.ratmac/ratmac.toml`'s `close` guards and run its check command with a fixture declaration override. | `Cross-Feature` | Exactly one `command_exit` guard naming `sweep_lanes.py`; it exits `0` on a genuinely swept fixture report. | `t-110 worktree` |

## Merge Gate

- **Dependencies:** `t-109` (the thirteen pre-split ports must be green or the fresh sweep still reads red rows and the close guard cannot exit `0`).
- **Ticket tests:** `RLRV-002`, `RLRV-003`, `RLRV-004` pass after their expected red evidence.
- **Fixture with a past:** the subjects are landed crates and the tracked 2026-08-25 report (`GPH-001` pattern); the frozen lane-name list is the pre-port state.
- **Hidden manifest:** `HT-110-01` through `HT-110-06` cover all six required lanes.
- **Quality and closure:** `cargo test --workspace`, `cargo fmt --check`, and `cargo clippy --workspace --all-targets` pass; `res-165` and `res-166` are re-judged; `landed-commit` is stamped in the stamp landing after the green landing merges (`RCR-001`).
