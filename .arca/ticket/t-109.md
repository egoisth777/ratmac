---
ticket-id: "t-109"
residual-ids:
  - "res-164"
# SDC-003 (schema.md, "Deliberate damage and discard safety"): deliberate-damage
# evidence lives solely in these gap records' mutation-kill lists; the ticket
# points and duplicates no evidence bytes.
behavior-refs:
  - "[RLR-001](../schema.md#rlr-001---triage-precedes-the-act)"
  - "[RLR-002](../schema.md#rlr-002---the-pre-split-crates-are-ported)"
design-refs:
  - "[The triage of 2026-09-17](../issue/i-038-red-lanes-recovered/design.md#the-triage-of-2026-09-17)"
  - "[Landed lanes sweep on demand and expire by in-crate marker (ADR-0020)](../goal/design.md#landed-lanes-sweep-on-demand-and-expire-by-in-crate-marker-adr-0020)"
planned-test-refs:
  - "RLRV-001"
  - "RLRV-005"
# CGD-001 (schema.md, "Ticket check tags"): the ticket's declared checks as
# three tag lists of opaque check ids; a checker reads these, never prose.
focused-tests:
  - "test/qa/tests/t109_pre_split_ports.rs"
hidden-lanes:
  - "HT-109-01"
  - "HT-109-02"
  - "HT-109-03"
  - "HT-109-04"
  - "HT-109-05"
  - "HT-109-06"
quality-commands:
  - "cargo test --workspace"
  - "cargo fmt --check"
  - "cargo clippy --workspace --all-targets"
dependencies: []
status: "passed"
landed-commit: "496bcf5"
blocker-ref: ""
---

# Ticket: t-109 - Port the thirteen pre-split hidden crates to the current Engine

## Vertical Outcome

The thirteen crates `t-058`..`t-070` under `test-hidden/` run green against
today's Engine, each keeping exactly the lane names it was landed with
(seventy-six across the thirteen - `t-061` and `t-067` were cut with five)
and the requirement each lane was cut for. Every one of them is a retired contract or fixture
drift under `RLR-001` - none has a last-good edition to name, so none may be
expired - and the port is renames and scaffold rewrites only: the engine
root (`.arca/` -> `.ratmac/`, `state.toml` -> `run.toml`, `.arca/rtm.lock`
-> `.ratmac/locks/`), the Run Record and verdict key `phase` -> `state` with
`Phase:` -> `State:` in reports, the blocker-based `rtm hold` in `t-063`,
and a JSON-stability oracle over `rtm doctor --json` where `t-064`, `t-067`,
and `t-070` called the retired public `doctor::render_json`. No assertion is
deleted, no equality loosened, and `src/` is untouched.

## Triage classes (RLR-001)

One row per crate: the class and the landing that changed the contract.

| Crate | Class | Named landing | Act |
| :--- | :--- | :--- | :--- |
| `t-058` | retired contract | `t-071` (engine root `.ratmac/`), `t-081` (`run.toml`, `state`), `t-083` (`State:`) | port; created-file list gains `.ratmac/mint.toml` |
| `t-059` | retired contract | `t-071`, `t-081`, `t-083`; abandon log wording `retired at state` | port |
| `t-060` | retired contract | `t-071` (`.ratmac/locks/root.lock`), `t-081`, `t-083` | port |
| `t-061` | retired contract | `t-081` (`RunState.state`), `t-083` (`state_prompt`, `state "x"` locations) | port |
| `t-062` | retired contract | `t-081` (verdict fields exactly `state`, `input`, `rationale`) | port; each case-table refusal re-derived |
| `t-063` | retired contract | `t-087` (`HoldRequest { blocker, confirmation, run }`, phrase `hold <run-id>`) | port; HT-063-03 rebuilt on the blocker route |
| `t-064` | retired contract | `t-078` (`Diagnosis::render_json` behind `rtm doctor --json`), `t-071` | port; JSON oracle over the CLI |
| `t-065` | fixture drift | scaffold builds `.arca/goal` + `.arca/ratmac.toml`, reads `state.toml` | port |
| `t-066` | fixture drift | same scaffold | port |
| `t-067` | retired contract | `t-078`, `t-071` | port; JSON oracle over the CLI |
| `t-068` | fixture drift | same scaffold | port |
| `t-069` | fixture drift | same scaffold; verdict helper writes `phase =` | port |
| `t-070` | retired contract | `t-078` (findings-only JSON retired for the `engine_root`-bearing document), `t-071` | port; equality against the CLI's own `--json` |
| `t-107` | fixture drift | none - the calendar: `ht_107_05` pinned the day it was written (`2026-08-25`) as "today" and rotted on the first later run, surfaced by this turn's `cargo test --workspace` | port; the lane asks the tool's own clock (`Get-Date`, `yyyy-MM-dd`) |

## Worktree Scope

- **Included:** the thirteen crates under `test-hidden/t-058`..`t-070`
  (fixture scaffolds, lane expectations, helper readers); the focused test
  `test/qa/tests/t109_pre_split_ports.rs`; six hidden lanes in
  `test-hidden/t-109/`; the fresh sweep report the workspace suite records.
- **Excluded:** any change under `src/`; any expiry marker in these crates;
  any lane deleted, renamed, or weakened; `tools/sweep_lanes.py` and the
  roster (`t-110`); the post-split crates (`t-110`) - except `t-107`, whose
  calendar rot surfaced in this turn's workspace run and blocked the quality
  command; its one-line fixture repair rides this landing (class row above).
- **Hole found at P5 (deliberate damage):** `test-hidden/` is gitignored, so
  the safety checkpoint cannot cover a lane file; the two lane-side kills
  (M1, M2 in `res-164`) were undone by exact inverse edits and proved by
  re-deriving the receipts' `tree-sha256` over the ported roots, not by
  `git restore`. SDC-002's checkpoint restore has no reach into the lanes
  root today; a wish records it.
- **Rides along, not scope:** the tracked sweep report
  (`.ratmac/evidence/lane-sweep/report.md`) is rewritten by the workspace
  quality command (`t108`'s first-sweep test runs the real sweep), so this
  landing carries the fresh report; and the rebrand allowlist loses its row
  for that report, which went stale the moment no red row quoted a retired
  key any more (`t034`/`t037` refuse a row that matches nothing).
- **Why now:** these thirteen rows are the bulk of the red set that keeps
  the wired close guard refusing (res-164), and they are the only adversarial
  proof of the routing, completion, delivery, addressing, motion, termination,
  and composition contracts.

## P4 Apparent Test Plan

Public mapping from explicit goal behavior to an executable test and oracle.

| Apparent Test ID | Goal Contract Ref | Fixture Ref | Executable Target | Oracle |
| ---------------- | ----------------- | ----------- | ----------------- | ------ |
| `RLRV-001` | [`RLRV-001`](../issue/i-038-red-lanes-recovered/test-plan.md#verification) | This repository's `test-hidden/t-058`..`t-070` as they stand, run with the sweep's own per-crate command (`cargo test --offline`) and one shared build directory. | `test/qa/tests/t109_pre_split_ports.rs::every_pre_split_crate_passes_keeping_its_lanes` | Each of the thirteen exits `0` with exactly its frozen lane names reported `ok`, and each crate's test source still declares exactly those names in order. |
| `RLRV-005` | [`RLRV-005`](../issue/i-038-red-lanes-recovered/test-plan.md#verification) | The working rules, this ticket's class table, and the thirteen crates. | `test/qa/tests/t109_pre_split_ports.rs::the_class_table_licenses_a_port_and_never_an_expiry` | `.arca/schema.md` carries `### RLR-001 ` naming the three classes; the class table names every one of the thirteen with a class of retired contract or fixture drift and a named landing; no such crate carries `EXPIRED.toml`; and every crate the table licenses to port runs green - the act the class fixes. |

## P5 Hidden Test Public Coverage Manifest

Public coverage manifest entries. Category must map to one of the six adversarial lanes:
1. `Regression`
2. `Input/Routing`
3. `Lifecycle/Model`
4. `Durability/Recovery`
5. `Output/Filesystem`
6. `Cross-Feature`

| Lane | Assessment | Rationale | Hidden IDs |
| ---- | ---------- | --------- | ---------- |
| `Regression` | `covered` | The seventy-six lane names as they stood before the port are frozen in the hidden crate; the ported crates must still declare exactly those names. | `HT-109-01` |
| `Input/Routing` | `covered` | The ported fixtures must address the Engine through the current root: every scaffold writes its runbook at `.ratmac/ratmac.toml`, and no scaffold's runbook home is `.arca/`. | `HT-109-02` |
| `Lifecycle/Model` | `covered` | The ported readers must speak the settled vocabulary: no crate reads `state.toml` or a `phase` key, and Run positions are read from `run.toml`'s `state`. | `HT-109-03` |
| `Durability/Recovery` | `covered` | A port must not depend on stale build output: one ported crate runs green from an empty build directory. | `HT-109-04` |
| `Output/Filesystem` | `covered` | A port never edits the Engine: the turn's landed diff against the trunk touches nothing under `src/`. | `HT-109-05` |
| `Cross-Feature` | `covered` | The sweep's verdict and a direct run agree: a fixture lanes root holding two ported crates reads `pass` with six lanes green each through `tools/sweep_lanes.py`. | `HT-109-06` |

| Hidden ID | Goal Contract Ref | Derivation | Category | Oracle | Owner |
| --------- | ----------------- | ---------- | -------- | ------ | ----- |
| `HT-109-01` | [`RLR-002`](../schema.md#rlr-002---the-pre-split-crates-are-ported) | Compare each crate's declared `#[test]` names against the frozen pre-port list. | `Regression` | Exactly the same names per crate, seventy-six in all. | `t-109 worktree` |
| `HT-109-02` | [`RLR-002`](../schema.md#rlr-002---the-pre-split-crates-are-ported) | Scan each crate's scaffold for the runbook home it writes. | `Input/Routing` | Every scaffold writes `.ratmac/ratmac.toml`; residue plants under `.arca/` appear only in lanes that assert the residue refusal. | `t-109 worktree` |
| `HT-109-03` | [`RLR-002`](../schema.md#rlr-002---the-pre-split-crates-are-ported) | Scan each crate's readers for the retired record spelling. | `Lifecycle/Model` | No `state.toml`, no `phase` key, no `Phase:` expectation remains. | `t-109 worktree` |
| `HT-109-04` | [`RLR-002`](../schema.md#rlr-002---the-pre-split-crates-are-ported) | Run one ported crate with an empty `CARGO_TARGET_DIR`. | `Durability/Recovery` | Six lanes green from a cold build. | `t-109 worktree` |
| `HT-109-05` | [`RLR-001`](../schema.md#rlr-001---triage-precedes-the-act) | List the files the turn changed against the trunk. | `Output/Filesystem` | Nothing under `src/`; no `EXPIRED.toml` under the thirteen crates. | `t-109 worktree` |
| `HT-109-06` | [`RLR-002`](../schema.md#rlr-002---the-pre-split-crates-are-ported) | Sweep a fixture lanes root of two ported crates. | `Cross-Feature` | Both rows read `pass`, `6 lane(s) green`; the summary agrees. | `t-109 worktree` |

## Merge Gate

- **Dependencies:** none.
- **Ticket tests:** `RLRV-001` and `RLRV-005` pass after their expected red evidence.
- **Fixture with a past:** the subjects are thirteen landed crates cut against the pre-split Engine (`GPH-001` pattern: verdicts walking landed history); the frozen lane-name list is their pre-port state.
- **Hidden manifest:** `HT-109-01` through `HT-109-06` cover all six required lanes.
- **Quality and closure:** `cargo test --workspace`, `cargo fmt --check`, and `cargo clippy --workspace --all-targets` pass; `res-164` is re-judged; `landed-commit` is stamped in the stamp landing after the green landing merges (`RCR-001`).
