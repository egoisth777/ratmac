# State File template — written ONLY by the Scheduler (schd); agents read, never write.
phase = "{{Idle|P1|P2|P3|P4|P5}}"
status = "{{planned|executing|blocked|passed|failed}}"
goal_revision = "{{revision-identifier}}"
input_revision = "{{revision-identifier}}"
output_revision = "{{revision-identifier}}"
active_refs = ["{{path/to/artifact}}"]
blocker = "{{empty-or: missing entry prerequisite}}"
