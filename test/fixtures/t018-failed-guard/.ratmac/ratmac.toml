# A valid class whose first phase has an unsatisfied artifact guard.
[states.prepare]
prompt = "Prepare the required output."

[[states.prepare.guards]]
kind = "files_exact"
path = "required"
entries = ["output.txt"]

[states.done]
prompt = "The run is complete."

[[transitions]]
from = "prepare"
to = "done"
