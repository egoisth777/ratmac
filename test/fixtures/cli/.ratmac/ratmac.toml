[states.prepare]
prompt = "Prepare the run."

[[states.prepare.guards]]
kind = "files_exact"
path = "artifacts"
entries = ["required.txt"]

[states.complete]
prompt = "Complete the run."

[[transitions]]
from = "prepare"
to = "complete"
