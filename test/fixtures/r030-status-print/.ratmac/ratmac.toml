[states.prepare]
prompt = "Prepare the run."

[[states.prepare.guards]]
kind = "files_exact"
path = "artifacts"
entries = ["proof.txt"]

[states.done]
prompt = "Complete the run."

[[transitions]]
from = "prepare"
to = "done"
