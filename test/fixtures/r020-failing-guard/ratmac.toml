# PT-021-01: the required artifact remains absent for every refusal attempt.
[states.build]
prompt = "Produce the required artifact."

[[states.build.guards]]
kind = "files_exact"
path = "artifacts"
entries = ["required.txt"]

[states.complete]
prompt = "Complete."

[[transitions]]
from = "build"
to = "complete"
