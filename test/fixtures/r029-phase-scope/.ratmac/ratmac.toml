[states.build]
prompt = "Build the selected artifact only."

[[states.build.guards]]
kind = "files_exact"
path = "artifacts"
entries = ["release.txt"]

[[states.build.guards]]
kind = "file_contains"
path = "artifacts/release.txt"
contains = "ready: true"

[[states.build.guards]]
kind = "command_exit"
program = "rustc"
args = ["--version"]
expected = 0
# ETB-001: a toolchain probe reads no project state and is marked as such.
exempt = true

[states.build-review]
prompt = "Review the selected artifact."

[states.build-done]
prompt = "Finish the selected artifact."

[[transitions]]
from = "build"
to = "build-review"

[[transitions]]
from = "build-review"
to = "build-done"
