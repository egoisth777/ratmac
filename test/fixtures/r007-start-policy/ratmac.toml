[states.prepare]
prompt = "Prepare the run."
guards = [{ kind = "files_exact", path = "required", entries = ["output.txt"] }]

[states.done]
prompt = "Complete the run."

[[transitions]]
from = "prepare"
to = "done"
