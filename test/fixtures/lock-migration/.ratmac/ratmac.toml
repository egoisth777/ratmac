[phases.prepare]
prompt = "Prepare the run."

[[phases.prepare.guards]]
kind = "files_exact"
path = "artifacts"
entries = ["required.txt"]

[phases.review]
prompt = "Review the run."

[[transitions]]
from = "prepare"
to = "review"
