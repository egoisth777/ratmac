[states.prepare]
prompt = "Prepare the run."

[[states.prepare.guards]]
kind = "files_exact"
path = "artifacts"
entries = ["required.txt"]

[states.review]
prompt = "Review the run."

[[transitions]]
from = "prepare"
to = "review"
