[states.build]
prompt = "Produce the build artifact."

[[states.build.guards]]
kind = "files_exact"
path = "artifacts/ready.txt"

[states.review]
prompt = "Review the produced artifact."

[[transitions]]
from = "build"
to = "review"
