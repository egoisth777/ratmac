# Reviewed class used by the active-Run targeting contract.
[phases.prepare]
prompt = "Prepare the active run."

[phases.done]
prompt = "The active run is complete."

[[transitions]]
from = "prepare"
to = "done"
