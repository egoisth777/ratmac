# PT-008-01: bytes must remain exactly this sentinel across all read-only paths.
state = "p4"
status = "executing"
goal_revision = "sentinel-goal"
input_revision = "sentinel-input"
output_revision = "sentinel-output"
active_refs = ["sentinel-ref"]
blocker = ""
