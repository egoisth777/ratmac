[phases.prepare]
prompt = "Prepare the run."

[[phases.prepare.guards]]
kind = "files_exact"
path = "required"
entries = ["output.txt"]
[phases.done]
prompt = "Complete the run."

[[transitions]]
from = "prepare"
to = "done"
