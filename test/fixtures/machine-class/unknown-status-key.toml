# Valid Machine Class declarations plus the forbidden status dimension.
status = ["planned", "executing", "blocked", "passed", "failed"]

[states.prepare]
prompt = "Prepare the inputs."

[states.done]
prompt = "Finish the run."

[[transitions]]
from = "prepare"
to = "done"
