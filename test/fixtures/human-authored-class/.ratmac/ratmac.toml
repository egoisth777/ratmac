# Human-authored and reviewed Machine Class for PT-004-01.
# The Scheduler consumes this file; it must never generate or rewrite it.

[states.prepare]
prompt = "Prepare the reviewed inputs."

[states.done]
prompt = "The reviewed inputs are complete."

[[transitions]]
from = "prepare"
to = "done"
