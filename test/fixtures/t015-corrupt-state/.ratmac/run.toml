state = "build"
status = "executing"
goal_revision = "goal-r1"
input_revision = "input-r1"
output_revision = "output-r0"
active_refs = [".arca/issue/i-001/index.md"]
blocker = ""
# Deliberately malformed TOML: the array never closes.
corrupt_refs = ["unterminated"
