# Human-authored Machine Class: runtime state must never be written here.
[states.prepare]
prompt = "Prepare the run inputs."

[states.complete]
prompt = "Confirm the run is complete."

[[transitions]]
from = "prepare"
to = "complete"
